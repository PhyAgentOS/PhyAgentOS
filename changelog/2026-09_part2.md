## v2.9.0 (2026-09-03 15:58) - codex

### 变更摘要 / Change Summary
- [完成] [docs] [docs] 汇总近期关于 PAOS 五类状态文件、权威持久化边界、抓取放置接入顺序与自主进化影响的诊断，并提出供用户审核的下一步实现方向；同步维护既有 RoboTwin adapter 执行诊断文档的状态文件协议与路线说明。
- [Completed] [docs] [docs] Consolidated the recent diagnosis of PAOS state-file concepts, authoritative persistence boundaries, pick-place integration order, and autonomous-evolution impact, and proposed the next implementation direction for user review; maintained the existing RoboTwin adapter execution diagnosis with the state-file protocol and roadmap clarification.

### 预期影响文件 / Expected Files
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` [新增 / added]
- `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` [修改 / modified]
- `docs/README.md` [修改 / modified：增加诊断文档索引 / add diagnosis link]
- `CHANGELOG.md` [修改 / modified：更新最近版本与 Archive / update latest version and Archive]
- `changelog/2026-09_part2.md` [新增 / added：本记录 / this record]

### 实际变更 / Actual Changes

- [docs] [docs] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1-L240` 新增双语诊断：记录 PAOS 权威持久化边界，说明 Markdown queue Runtime 已移除，映射五类报告文件，定义输入/projection 规则，并提出“先冻结最小上层契约 → 抓取放置证据闭环 → 文件适配 → 受控自主进化”的审核路线。
- [Docs] [Docs] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1-L240` adds the bilingual diagnosis, PAOS authority boundaries, removal of the Markdown queue runtime, five-file mapping, input/projection rules, and the review roadmap: freeze the minimal upper-layer contract, close the pick-place evidence loop, add file adapters, then enable guarded evolution.
- [docs] [docs] `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md:L390-L411` 修改既有执行诊断，增加状态文件协议审核结论和四阶段实施顺序；`L457-L526` 同步当前 GraspGen adapter 进度与抓取位姿消费边界。
- [Docs] [Docs] `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md:L390-L411` updates the execution diagnosis with the state-file decision and four-stage order; `L457-L526` records the current GraspGen adapter status and grasp-pose consumption boundary.
- [docs] [docs] `docs/README.md:L29-L30,L63-L64` 增加中英文诊断文档索引。
- [Docs] [Docs] `docs/README.md:L29-L30,L63-L64` adds Chinese and English index links to the diagnosis.
- [docs] [docs] `CHANGELOG.md:L5-L35` 增加 `v2.9.0` 诊断记录和 Archive，并恢复此前被误删的 `v2.8.12` 历史记录；既有更旧版本继续保留在根索引或月度归档中。
- [Docs] [Docs] `CHANGELOG.md:L5-L35` adds the `v2.9.0` diagnosis entry and Archive, and restores the previously omitted `v2.8.12` historical record; older versions remain in the root index or monthly archive.

### 关键 Diff / Key Diff

**新增诊断文档 / New diagnosis document**

```text
Before: no document captured the five report files against PAOS persistence.
After: Markdown is explicitly an input/projection; SQLite, Gateway, Evidence,
       Runtime state, and experience.sqlite3 remain the machine authorities.
```

**执行路线 / Execution order**

```text
Before: implementation order was documented without the state-file authority decision.
After:  freeze minimal contracts -> pick-place evidence closure -> Markdown adapters
        -> guarded autonomous evolution.
```

### 验证 / Validation

- `git diff --check` passed.
- Markdown line references, cross-document links, bilingual entries, and root changelog rolling window were inspected.
- No source code, runtime behavior, hardware IO, or motion authorization changed.

### Git 提交 / Git Commit

- Commit: `2065f9b` (documentation content)
- Follow-up log commit: `76389b3` (completion status and commit record)
- Branch: `feature/long-horizon-workflow`

## v3.8.0 (2026-09-04 09:26) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [feat] 为 Verification 真实模型评估增加“provider 配置文件 + 独立 API key 文件”凭据源；严格拒绝同时配置环境变量和 key 文件、符号链接、宽松权限、超限或空白 key，并确保密钥内容不进入 manifest、metrics、日志或 Git。
- [Planned] [Eval] [Feat] Add a provider-config plus separate API-key-file credential source for real-model Verification evaluation; strictly reject simultaneous environment/file sources, symlinks, permissive modes, oversized or blank keys, and keep key contents out of manifests, metrics, logs, and Git.
- [计划] [eval] [config] 提供 OpenAI API URL/model 的版本化门禁配置、可提交的 provider 示例，以及本地忽略的可填写 provider/key 文件；不修改 Gateway/Dora、Action 或硬件边界。
- [Planned] [Eval] [Config] Provide a versioned OpenAI API URL/model gate config, a committable provider example, and ignored local provider/key files for user input; do not change Gateway/Dora, Action, or hardware boundaries.
- [计划] [eval] [test] 覆盖 key 文件成功解析、相对路径、权限/符号链接/双凭据源失败、产物脱敏与 placeholder blocker，并完成实现代码审查和回归。
- [Planned] [Eval] [Test] Cover key-file parsing, relative paths, permission/symlink/dual-source failures, artifact redaction, and placeholder blocking, followed by implementation review and regression tests.

### 预期影响文件 / Expected Files

- `PhyAgentOS/verification/evaluation.py`
- `tests/test_verification_model_evaluation.py`
- `evals/verification/evaluation_config_sol_high_v1.json`
- `evals/verification/provider.sol_high.example.json`
- `evals/verification/provider.sol_high.local.json`（本地忽略 / locally ignored）
- `evals/verification/.secrets/verification-model.key`（本地忽略 / locally ignored）
- `docs/forge/VERIFICATION_MODEL_EVALUATION.md`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`
- `.gitignore`
- `CHANGELOG.md`

### 实际结果 / Actual Results

- [完成] [eval] [docs] 完整 held-out + hazard 真实模型运行 `20260904T034715.434600Z-42a21625` 绑定提交 `2722d78d1f21d43f12c0213811376ee8f8bf57a8`，7 个 case 全部完成，`quality_gate_eligible=true`、`quality_gate_passed=true`。
- [Completed] [Eval] [Docs] The complete held-out + hazard real-model run `20260904T034715.434600Z-42a21625` is bound to commit `2722d78d1f21d43f12c0213811376ee8f8bf57a8`; all 7 cases completed with `quality_gate_eligible=true` and `quality_gate_passed=true`.
- [完成] [eval] [review] 总体 contract/criterion/recovery-context 为 `1.0`，success false-positive rate 为 `0`，verdict accuracy 为 `0.8571428571428571`；held-out 有一个 replan case 返回 `inconclusive`，保留为后续质量风险。
- [Completed] [Eval] [Review] Overall contract/criterion/recovery-context scores are `1.0`, success false-positive rate is `0`, and verdict accuracy is `0.8571428571428571`; one held-out replan case returned `inconclusive` and remains a follow-up quality risk.
- [完成] [policy] [docs] 关闭 Verification 语义质量门禁，但明确下一步为 Gateway/Dora 无动作 wiring conformance，抓取放置闭环和自主进化继续后置。
- [Completed] [Policy] [Docs] Closed the Verification semantic-quality gate while making Gateway/Dora no-motion wiring conformance the next step; pick-place closure and autonomous evolution remain deferred.

### 文件变更详情 / File Change Details

- [修改 / Modified] `docs/forge/VERIFICATION_MODEL_EVALUATION.md:L94-L115`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L287-L312`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L370-L383`：写入完整评估证据、逐 case 审核和执行顺序更新。
- [修改 / Modified] `CHANGELOG.md:L8-L49`、`changelog/2026-09_part2.md:L132-L150`：增加 v3.8.3 双语记录和精确运行证据。

### 验证 / Validation

- manifest/metrics/results 逐文件审核通过；7 个结果均为合法结构化 verdict，evidence refs 未越权。
- 凭据扫描未发现 API key、Bearer 或 session token 值。
- 未启动 Gateway、Dora、Action、硬件或运动路径。
- `changelog/2026-09_part2.md`

### 实际结果 / Actual Results

- [完成] [eval] [feat] 新增 `api_key_file` 文件凭据源、`O_NOFOLLOW`/owner/0600/size/placeholder/token 校验；provider config 与 key 文件均已加入 Git 忽略。
- [Completed] [Eval] [Feat] Added the `api_key_file` credential source with `O_NOFOLLOW`, owner, 0600, size, placeholder, and single-token checks; both provider config and key files are Git-ignored.
- [完成] [eval] [config] 创建 `evals/verification/evaluation_config_sol_high_v1.json`、`provider.sol_high.example.json`、`provider.sol_high.local.json` 和 `.secrets/verification-model.key`；配置为 `https://api.shuaiapi.com/v1`、`gpt-5.6-sol`、`reasoning_effort=high`。
- [Completed] [Eval] [Config] Created `evaluation_config_sol_high_v1.json`, `provider.sol_high.example.json`, `provider.sol_high.local.json`, and `.secrets/verification-model.key`; configured `https://api.shuaiapi.com/v1`, `gpt-5.6-sol`, and `reasoning_effort=high`.
- [完成] [eval] [test] 单 case 真实连通性：根 URL 的 `/models` 返回 451；切换 `/v1` 后模型返回合法 `replan_required`，contract/verdict/criterion/recovery-context 均为 `1.0`；因 `--max-cases 1` 固定不具备门禁资格。
- [Completed] [Eval] [Test] Single-case live connectivity: root `/models` returned 451; after switching to `/v1`, the model returned a valid `replan_required` with contract/verdict/criterion/recovery-context all at `1.0`; `--max-cases 1` keeps the run gate-ineligible.
- [完成] [eval] [fix] 收紧生产 prompt，明确 `recovery_context` 只能包含 `unmet_criteria`、`preserved_constraints`、`guidance`，保留严格 `extra=forbid`。
- [Completed] [Eval] [Fix] Tightened the production prompt to state that `recovery_context` may contain only `unmet_criteria`, `preserved_constraints`, and `guidance`, retaining strict `extra=forbid`.

### 文件变更详情 / File Change Details

- [新增 / Added] `PhyAgentOS/verification/evaluation.py:L6-L18,L137-L190,L254-L329,L514-L548`、`evals/verification/evaluation_config_sol_high_v1.json:L1-L25`、`evals/verification/provider.sol_high.example.json:L1-L13`、`tests/test_verification_model_evaluation.py:L21-L22,L196-L207,L300-L413`。
- [修改 / Modified] `PhyAgentOS/verification/service.py:L64-L68`、`tests/test_verifier_semantic_conformance.py:L10,L43-L49`、`.gitignore:L30-L31`、`docs/forge/VERIFICATION_MODEL_EVALUATION.md:L16-L87`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L252-L274`。
- [新增 / Added, ignored] `evals/verification/provider.sol_high.local.json:L1-L13`、`evals/verification/.secrets/verification-model.key:L1`。

### 关键 Diff / Key Diff

```text
修改前 / Before: api_key 仅支持环境变量；prompt 对 recovery guidance 字段名不够明确。
修改后 / After:  key 可由权限受控的独立文件提供且只记录引用；prompt 明确三字段，严格 schema 继续拒绝额外字段。
```

### 验证 / Validation

- `tests/test_verification_model_evaluation.py` + semantic conformance + service process → `26 passed`。
- `gpt-5.6-sol/high` `--max-cases 1` → `status=completed`、contract/verdict/criterion/recovery-context `1.0`，质量门禁 eligibility `false`。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -p pytest_asyncio.plugin -q` → `147 passed`。
- `python -m ruff check PhyAgentOS tests scripts/evaluate_verification_model.py`、compileall、`git diff --check` → 通过。

## v3.8.1 (2026-09-04 10:35) - codex

### 预期修改 / Planned Changes

- [完成] [docs] [fix] 校正 v3.8.0 变更日志中的实际配置/key 文件名，删除评估文档中过时的 Codex blocker 表述，并在根 CHANGELOG 增加完整双语 v3.8.0 条目。
- [Completed] [Docs] [Fix] Corrected the actual config/key filenames in the v3.8.0 changelog, removed the stale Codex blocker wording from the evaluation guide, and added a complete bilingual v3.8.0 entry to the root CHANGELOG.
- [完成] [config] [feat] 将同一套受限独立 key 文件能力接入 `paos agent` 主配置；支持相对路径、权限/所有者/符号链接/空白校验，并避免解析后的 key 被保存回配置。
- [Completed] [Config] [Feat] Extended the same restricted API-key-file capability to the `paos agent` main config; support relative paths and owner/permission/symlink/blank validation while preventing resolved keys from being written back.

### 预期影响文件 / Expected Files

- `changelog/2026-09_part2.md`
- `docs/forge/VERIFICATION_MODEL_EVALUATION.md`
- `CHANGELOG.md`
- `PhyAgentOS/config/schema.py`
- `PhyAgentOS/config/loader.py`
- `PhyAgentOS/cli/commands.py`
- `tests/test_config_api_key_file.py`
- `docs/zh/04-forge-configuration-reference.md`
- `README.md`

### 实际结果 / Actual Results

- [完成] [config] [feat] `ProviderConfig.apiKeyFile` 已接入 `paos agent` 主配置；相对路径、owner、普通文件、O_NOFOLLOW、0600、大小、UTF-8、单 token 和 placeholder 校验共用安全边界，解析后的 key 不回写配置。
- [Completed] [Config] [Feat] `ProviderConfig.apiKeyFile` is now wired into the `paos agent` main config; relative paths, owner, regular-file, O_NOFOLLOW, 0600, size, UTF-8, single-token, and placeholder checks share the security boundary, and resolved keys are never written back.
- [完成] [eval] [test] `paos status` 显示 `Custom: ✓`，无工具 Agent 连通性检查成功返回；未启动 Gateway、Dora、Action 或硬件。
- [Completed] [Eval] [Test] `paos status` reports `Custom: ✓`, and a no-tool Agent connectivity check returned successfully; Gateway, Dora, Action, and hardware were not started.

### 文件变更详情 / File Change Details

- [新增 / Added] `PhyAgentOS/config/credentials.py:L1-L48`：严格独立 key 文件读取器。
- [修改 / Modified] `PhyAgentOS/config/schema.py:L394-L417,L547-L612`、`PhyAgentOS/config/loader.py:L43-L52`、`PhyAgentOS/cli/commands.py:L285-L337,L1650-L1657`：主配置字段、配置路径绑定、运行时解析与 status/provider 接入。
- [新增 / Added] `tests/test_config_api_key_file.py:L1-L52`：成功解析、相对路径、双凭据、符号链接和权限回归。
- [修改 / Modified] `README.md:L196-L200`、`docs/zh/04-forge-configuration-reference.md:L70-L76`、`docs/forge/VERIFICATION_MODEL_EVALUATION.md:L42-L101`：配置和当前状态说明。

### Git 提交 / Git Commit

- Commit: `9c1b955`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-04 11:00 (Asia/Shanghai)

## v3.8.2 (2026-09-04 11:02) - codex

### 预期修改 / Planned Changes

- [计划] [chore] [docs] 回写 v3.8.1 实现提交 `9c1b955` 到月度日志和根 CHANGELOG，并核对当前分支推送状态；不修改实现或评估行为。
- [Planned] [Chore] [Docs] Record v3.8.1 implementation commit `9c1b955` in the monthly log and root CHANGELOG, and verify the current branch push status; do not modify implementation or evaluation behavior.

### 预期影响文件 / Expected Files

- `changelog/2026-09_part2.md`
- `CHANGELOG.md`

### 实际结果 / Actual Results

- [完成] [chore] [docs] 已将实现提交 `9c1b955` 回写到 v3.8.1 记录；当前分支保持 `feature/long-horizon-workflow`。
- [Completed] [Chore] [Docs] Recorded implementation commit `9c1b955` in the v3.8.1 entry; the current branch remains `feature/long-horizon-workflow`.

### 文件变更详情 / File Change Details

- [修改 / Modified] `CHANGELOG.md:L8-L62`、`changelog/2026-09_part2.md:L111-L174`：补全 v3.8.1 commit 与 v3.8.2 日志维护记录。

### 验证 / Validation

- `git diff --check`、UTF-8 日志显示和当前分支核对通过。

## v3.8.3 (2026-09-04 11:10) - codex

### 预期修改 / Planned Changes

- [完成] [eval] [docs] 审核并记录完整 `gpt-5.6-sol/high` held-out + hazard 真实模型评估证据，更新 Verification 门禁状态、逐 case 残余风险和执行顺序；不连接 Gateway、Dora、Action 或硬件。
- [Completed] [Eval] [Docs] Reviewed and recorded the complete `gpt-5.6-sol/high` held-out + hazard real-model evaluation evidence, updated Verification gate status, per-case residual risk, and execution order; did not connect Gateway, Dora, Actions, or hardware.

### 预期影响文件 / Expected Files

- `docs/forge/VERIFICATION_MODEL_EVALUATION.md`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`
- `CHANGELOG.md`
- `changelog/2026-09_part2.md`

### 实际结果 / Actual Results

- [完成] [eval] [docs] 完整 held-out + hazard 真实模型运行 `20260904T034715.434600Z-42a21625` 绑定提交 `2722d78d1f21d43f12c0213811376ee8f8bf57a8`，7 个 case 全部完成，`quality_gate_eligible=true`、`quality_gate_passed=true`。
- [Completed] [Eval] [Docs] The complete held-out + hazard real-model run `20260904T034715.434600Z-42a21625` is bound to commit `2722d78d1f21d43f12c0213811376ee8f8bf57a8`; all 7 cases completed with `quality_gate_eligible=true` and `quality_gate_passed=true`.
- [完成] [eval] [review] 总体 contract/criterion/recovery-context 为 `1.0`，success false-positive rate 为 `0`，verdict accuracy 为 `0.8571428571428571`；held-out 有一个 replan case 返回 `inconclusive`，保留为后续质量风险。
- [Completed] [Eval] [Review] Overall contract/criterion/recovery-context scores are `1.0`, success false-positive rate is `0`, and verdict accuracy is `0.8571428571428571`; one held-out replan case returned `inconclusive` and remains a follow-up quality risk.
- [完成] [policy] [docs] 关闭 Verification 语义质量门禁，但明确下一步为 Gateway/Dora 无动作 wiring conformance，抓取放置闭环和自主进化继续后置。
- [Completed] [Policy] [Docs] Closed the Verification semantic-quality gate while making Gateway/Dora no-motion wiring conformance the next step; pick-place closure and autonomous evolution remain deferred.

### 文件变更详情 / File Change Details

- [修改 / Modified] `docs/forge/VERIFICATION_MODEL_EVALUATION.md:L94-L115`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L287-L312`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L370-L383`：写入完整评估证据、逐 case 审核和执行顺序更新。
- [修改 / Modified] `CHANGELOG.md:L8-L49`、`changelog/2026-09_part2.md:L184-L220`：增加 v3.8.3 双语记录和精确运行证据。

### 验证 / Validation

- manifest/metrics/results 逐文件审核通过；7 个结果均为合法结构化 verdict，evidence refs 未越权。
- 凭据扫描未发现 API key、Bearer 或 session token 值。
- 未启动 Gateway、Dora、Action、硬件或运动路径。

### Git 提交 / Git Commit

- Commit: `bccdd6f`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-04 11:25 (Asia/Shanghai)

## v3.8.4 (2026-09-04 11:26) - codex

### 预期修改 / Planned Changes

- [完成] [chore] [docs] 回写 v3.8.3 Verification 语义质量门禁审计提交 `bccdd6f` 到月度日志和根 CHANGELOG，并核对当前分支推送状态；不修改实现、评估结果或执行顺序。
- [Completed] [Chore] [Docs] Recorded the v3.8.3 Verification semantic-quality gate audit commit `bccdd6f` in the monthly log and root CHANGELOG, and verified the current branch push status; implementation, evaluation results, and execution order were unchanged.

### 预期影响文件 / Expected Files

- `changelog/2026-09_part2.md`
- `CHANGELOG.md`

### 实际结果 / Actual Results

- [完成] [chore] [docs] 已在 v3.8.3 记录中写入 `bccdd6f`，根 CHANGELOG 同步完成。
- [Completed] [Chore] [Docs] Recorded `bccdd6f` in the v3.8.3 entry and synchronized the root CHANGELOG.

### 文件变更详情 / File Change Details

- [修改 / Modified] `CHANGELOG.md:L10-L28`、`changelog/2026-09_part2.md:L226-L252`：补全 v3.8.3 审计提交 hash 和 v3.8.4 维护记录。

### 验证 / Validation

- `git diff --check`、UTF-8 日志显示和当前分支状态核对通过。

## v3.9.0 (2026-09-04 12:10) - codex

### 预期修改 / Planned Changes

- [完成] [comm] [feat] 完成 Gateway/Dora 无动作 wiring conformance：新增 provider-neutral CapabilityRuntimeTransport，复用 CapabilityRuntime 与 ForgeToolClient 覆盖 discovery/context、Query、Action/Session admission、pending/terminal、cancel/stop、timeout/unknown、并发、身份关联和 no-POST recovery；不连接真实 Dora、Gateway、Action 或硬件。
- [Completed] [Comm] [Feat] Completed Gateway/Dora no-motion wiring conformance: added provider-neutral CapabilityRuntimeTransport reusing CapabilityRuntime and ForgeToolClient for discovery/context, Query, Action/Session admission, pending/terminal, cancel/stop, timeout/unknown, concurrency, identity correlation, and no-POST recovery; no real Dora, Gateway, Actions, or hardware connected.
- [完成] [eval] [review] 已按架构集成、失败路径、权威边界、配置和可维护性五个维度完成代码审查并修复 malformed JSON 映射等 Major；验收对象为本阶段 Gateway/Dora wiring，不是五个 Markdown 状态文件。
- [Completed] [Eval] [Review] Completed code review across architecture integration, failure paths, authority boundaries, configuration, and maintainability, fixing the malformed-JSON mapping Major; the acceptance target was this Gateway/Dora wiring stage, not the five Markdown state files.

### 预期影响文件 / Expected Files

- `CHANGELOG.md`
- `changelog/2026-09_part2.md`
- `PhyAgentOS/forge/capability_runtime/runtime.py`
- `PhyAgentOS/forge/tool_client.py`
- `PhyAgentOS/skill_runtime/manager.py`
- `tests/test_gateway_dora_no_motion_conformance.py`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`

### 实际结果 / Actual Results

- [完成] [comm] [feat] `CapabilityRuntime` 现在执行 deadline→unknown、cancel→cancelled、Session stop→stopped 的显式 reconciliation；Session 拒绝 timeout，终态不会重复推进。
- [Completed] [Comm] [Feat] `CapabilityRuntime` now explicitly reconciles deadline→unknown, cancel→cancelled, and Session stop→stopped; Sessions reject timeouts and terminal states do not advance again.
- [完成] [comm] [test] 新增正式 `CapabilityRuntimeTransport` HTTP conformance，验证 Gateway identity、caller/invocation/attempt 关联、Query/Action/Session 路由、malformed JSON 400 fail-closed、并发与 no-POST recovery。
- [Completed] [Comm] [Test] Added formal `CapabilityRuntimeTransport` HTTP conformance covering Gateway identity, caller/invocation/attempt correlation, Query/Action/Session routes, malformed-JSON 400 fail-closed behavior, concurrency, and no-POST recovery.
- [完成] [eval] [review] 五维审查无 Blocker/Major 遗留；保留真实 Dora/Gateway 进程互操作、Action executor、硬件和抓取放置闭环为后置门禁。
- [Completed] [Eval] [Review] Five-dimension review has no remaining Blocker/Major findings; real Dora/Gateway process interoperability, Action executor, hardware, and pick-place closure remain deferred gates.

### 文件变更详情 / File Change Details

- [新增 / Added] `PhyAgentOS/forge/capability_runtime/http_transport.py:L1-L95`：新增 provider-neutral HTTP Gateway transport。
- [修改 / Modified] `PhyAgentOS/forge/capability_runtime/runtime.py:L9-L12,L57-L70,L94-L100,L180-L223,L260-L313`：加入 clock/deadline、cancel/stop reconciliation 与 Session timeout 边界。
- [修改 / Modified] `PhyAgentOS/forge/capability_runtime/__init__.py:L27-L45`：导出 HTTP transport。
- [新增 / Added] `tests/test_gateway_dora_no_motion_conformance.py:L1-L117`：新增无动作 HTTP conformance 与失败路径验收。
- [修改 / Modified] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/fake_gateway.py:L401-L412,L631-L669`、`examples/forge-skills/pick-place-workflow/tests/test_generic_capability_runtime.py:L16-L145`：同步 stop 语义和 Runtime 回归。
- [修改 / Modified] `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`：记录五维验收结论并澄清不重复验收五个 Markdown 状态文件。

### 关键 Diff / Key Diff

```text
修改前 / Before: timeout 只被保存，cancel 可能长期停在 cancel_requested，HTTP conformance 依赖 example FakeGateway。
修改后 / After: deadline/cancel/stop 均有明确终态 reconciliation；核心目录提供可复用 HTTP transport，malformed JSON 400 fail-closed，重复读取不发 POST。
```

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -p pytest_asyncio.plugin -q tests/test_gateway_dora_no_motion_conformance.py examples/forge-skills/pick-place-workflow/tests/test_generic_capability_runtime.py` → `11 passed`。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -p pytest_asyncio.plugin -q tests` → `150 passed`。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-skills/pick-place-workflow/src python -m pytest -p pytest_asyncio.plugin -q examples/forge-skills/pick-place-workflow/tests` → `243 passed`。
- `python -m ruff check PhyAgentOS/forge/capability_runtime tests/test_gateway_dora_no_motion_conformance.py`、`python -m compileall -q PhyAgentOS tests`、`git diff --check` → 通过。

### Git 提交 / Git Commit

- Commit: `dd1ee70`
- Branch: `feature/long-horizon-workflow`
- Follow-up log commit: `83185bc`


## v3.10.0 (2026-09-04 14:00) - codex

### 预期修改 / Planned Changes

- [计划] [comm] [feat] 按已批准执行顺序推进 pick-place 证据闭环：强化 acquire→place 的显式绑定、terminal reconciliation、post-release evidence、失败/取消/unknown 和 no-motion admission；不连接真实机器人或硬件。
- [Planned] [Comm] [Feat] Advance the pick-place evidence closure per the approved order: strengthen explicit acquire→place binding, terminal reconciliation, post-release evidence, failure/cancel/unknown handling, and no-motion admission; do not connect real robots or hardware.
- [计划] [eval] [review] 完成后按架构集成、失败路径、权威边界、配置、可维护性五个维度审查并修复 Blocker/Major。
- [Planned] [Eval] [Review] After implementation, review across architecture integration, failure paths, authority boundaries, configuration, and maintainability, fixing Blocker/Major findings.

### 预期影响文件 / Expected Files

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/object_acquire.py`
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/object_place.py`
- `PhyAgentOS/forge/capability_runtime/observation.py`
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/fake_gateway.py`
- `PhyAgentOS/forge/task.py`
- `examples/forge-skills/pick-place-workflow/tests/`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`
- `CHANGELOG.md`

### 修改前置状态 / Pre-change State

本条目先记录计划；实现、验证、精确行号和 Git hash 将在修改完成后回写。

This entry records the plan before edits; implementation, validation, exact line ranges, and the Git hash will be recorded after completion.

### 实际结果 / Actual Results

- [完成] [comm] [feat] `LongHorizonWorkflow.record_terminal_response()` 复用标准 Gateway terminal response，自动提取并校验六步 workflow 的 opaque refs；place 必须绑定同一 acquire invocation 与完整 post-release evidence。
- [Completed] [Comm] [Feat] `LongHorizonWorkflow.record_terminal_response()` now consumes standard Gateway terminal responses, automatically extracting and validating opaque refs for the six-step workflow; place must bind the same acquire invocation and complete post-release evidence.
- [完成] [eval] [review] 按架构集成、失败路径、权威边界、配置、可维护性五个维度完成审查，无 Blocker/Major 遗留；真实 Action executor、Dora/机器人互操作与硬件保持后置。
- [Completed] [Eval] [Review] Completed the five-dimension review across architecture integration, failure paths, authority boundaries, configuration, and maintainability with no remaining Blocker/Major; real Action executor, Dora/robot interoperability, and hardware remain deferred.

### 文件变更详情 / File Change Details

- [修改 / Modified] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/long_horizon.py:L24-L27,L78-L89,L194-L231,L301-L316`：新增 artifact/destination schema、terminal response adapter、acquire identity equality 与 post-release evidence gate。
- [修改 / Modified] `examples/forge-skills/pick-place-workflow/tests/test_long_horizon.py:L59-L70,L123-L145`：补充 acquire/place 绑定、post-release evidence 缺失和标准 terminal response 回放测试。
- [修改 / Modified] `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`：记录抓取放置阶段和五维复审结论。

### 关键 Diff / Key Diff

```text
修改前 / Before: place reducer 只验证 invocation scheme，未强制与前一步 acquire 相等，也可在没有 post-release evidence 时宣称成功。
修改后 / After: terminal response 自动提取 refs；place 必须引用同一成功 acquire，并携带完整 post-release evidence artifact。
```

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -p pytest_asyncio.plugin -q tests` → `151 passed`。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-skills/pick-place-workflow/src python -m pytest -p pytest_asyncio.plugin -q examples/forge-skills/pick-place-workflow/tests` → `245 passed`。
- `python -m ruff check PhyAgentOS tests examples/forge-skills/pick-place-workflow/src examples/forge-skills/pick-place-workflow/tests`、`python -m compileall -q PhyAgentOS tests examples/forge-skills/pick-place-workflow/src`、`git diff --check` → 通过。

### Git 提交 / Git Commit

- Commit: `a847cd7`
- Branch: `feature/long-horizon-workflow`

## v3.7.2 (2026-09-04 00:43) - codex

### 预期修改 / Planned Changes

- [完成] [chore] [docs] 回写 v3.7.1 审计维护提交 `d88fd3a`，并更新入口最近版本记录；未修改实现或运行证据。
- [Completed] [Chore] [Docs] Recorded v3.7.1 audit-maintenance commit `d88fd3a` and updated the root recent-version record; implementation and run evidence were unchanged.

### 预期影响文件 / Expected Files

- `CHANGELOG.md`
- `changelog/2026-09_part2.md`

### 实际结果 / Actual Results

- [完成] [docs] [docs] `CHANGELOG.md:L10-L18` 新增 v3.7.2 记录；v3.7.1 的提交 hash 已写入入口和月度日志。
- [Completed] [Docs] [Docs] Added the v3.7.2 record at `CHANGELOG.md:L10-L18`; the v3.7.1 commit hash is now recorded in both root and monthly changelogs.

### 关键 Diff / Key Diff

```text
修改前 / Before: v3.7.1 审计维护内容已推送，但日志未记录其 commit hash。
修改后 / After:  v3.7.1 commit `d88fd3a` 已进入可审计的双语 changelog。
```

### 验证 / Validation

- `git diff --check` 与 UTF-8 日志显示检查通过。

### Git 提交 / Git Commit

- Commit: `d88fd3a` (v3.7.1 audit maintenance being recorded)
- Branch: `feature/long-horizon-workflow`

## v3.7.1 (2026-09-04 00:40) - codex

### 预期修改 / Planned Changes

- [完成] [docs] [docs] 回写 v3.7.0 实现提交 `8775073`，并用提交后 real-model preflight 的终态 blocked run 替换旧的预提交运行引用；未改评估代码、阈值或执行顺序。
- [Completed] [Docs] [Docs] Recorded v3.7.0 implementation commit `8775073` and replaced the old pre-commit run reference with the terminal blocked run from the post-commit real-model preflight; evaluation code, thresholds, and execution order were unchanged.

### 预期影响文件 / Expected Files

- `CHANGELOG.md`
- `changelog/2026-09_part2.md`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`

### 实际结果 / Actual Results

- [完成] [eval] [exp] 提交后 preflight 生成 `artifacts/evals/verification/20260903T163926.458050Z-db095983/`：manifest 绑定完整 commit `8775073eccb26791a5ffd0215794c49fd46f3f82`，状态 `blocked`、门禁不合格，`results.jsonl` 为 0 bytes。
- [Completed] [Eval] [Exp] The post-commit preflight created `artifacts/evals/verification/20260903T163926.458050Z-db095983/`: its manifest binds full commit `8775073eccb26791a5ffd0215794c49fd46f3f82`, status is `blocked`, the run is gate-ineligible, and `results.jsonl` is 0 bytes.
- [完成] [docs] [docs] `CHANGELOG.md:L10-L18,L50` 新增 v3.7.1 审计摘要并回写实现 commit；`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L245-L248` 更新提交后运行路径与 commit identity。
- [Completed] [Docs] [Docs] `CHANGELOG.md:L10-L18,L50` adds the v3.7.1 audit summary and records the implementation commit; `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L245-L248` updates the post-commit run path and commit identity.

### 关键 Diff / Key Diff

```text
修改前 / Before: 文档引用提交前 blocked run，未记录 v3.7.0 implementation commit。
修改后 / After:  文档引用绑定 8775073 的提交后 terminal blocked run，并明确无模型请求/质量分数。
```

### 验证 / Validation

- real-model preflight → exit `2`、`status=blocked`、`quality_gate_eligible=false`、`quality_gate_passed=false`。
- manifest/metrics 凭据扫描无 access token、refresh token、session token、Bearer 或 API key 值；`results.jsonl` 为空。
- `git diff --check` 与 UTF-8 日志显示检查通过。

## v3.7.0 (2026-09-03 22:50) - codex

### 预期修改 / Planned Changes

- [完成] [eval] [feat] 新增可复现的 Verification Service 真实模型语义质量评估：版本化 development/held-out/hazard 数据集、严格 case/schema、唯一运行目录、脱敏 provider 元数据、逐 case 原始 verdict、质量/选择性校准/安全假阳性指标和阈值门禁。
- [Completed] [Eval] [Feat] Added a reproducible real-model semantic-quality evaluation for Verification Service: a versioned development/held-out/hazard dataset, strict case schema, unique run directories, redacted provider metadata, per-case raw verdicts, quality/selective-calibration/safety false-positive metrics, and threshold gates.
- [完成] [eval] [test] 通过正式 `VerificationServiceProcess` 与独立 OpenAI-compatible HTTP stub 验证 runner；真实模型凭据不可用时只记录 blocker，不把 fixture 分数冒充真实模型质量。
- [Completed] [Eval] [Test] Validated the runner through the formal `VerificationServiceProcess` and an independent OpenAI-compatible HTTP stub; when real-model credentials are unavailable, only a blocker is recorded and fixture scores cannot represent real-model quality.
- [完成] [docs] [docs] 更新状态文件实现审查与独立评估操作文档，明确概率校准尚无 confidence 输出，当前只评估 abstention/selective calibration。
- [Completed] [Docs] [Docs] Updated the state-file implementation review and standalone evaluation guide, explicitly noting that probability calibration is unavailable without confidence outputs and that this gate evaluates abstention/selective calibration only.
- [完成] [eval] [fix] 修复代码审查发现的质量门禁资格漏洞：`evaluation_mode=real_model` 不再单独构成正式门禁资格；必须匹配版本化 provider identity binding，且 `--max-cases` 部分运行永不具备门禁资格。
- [Completed] [Eval] [Fix] Closed the quality-gate eligibility gap found during code review: `evaluation_mode=real_model` alone no longer confers formal gate eligibility; the run must match a versioned provider identity binding, and `--max-cases` partial runs are never gate-eligible.

### 预期影响文件 / Expected Files

- `PhyAgentOS/verification/evaluation.py`
- `PhyAgentOS/verification/validation.py`
- `PhyAgentOS/verification/request_builder.py`
- `PhyAgentOS/agent/session_verifier.py`
- `scripts/evaluate_verification_model.py`
- `evals/verification/semantic_verifier_v1.json`
- `evals/verification/evaluation_config_v1.json`
- `evals/verification/provider.openai_codex.example.json`
- `tests/test_verification_model_evaluation.py`
- `docs/forge/VERIFICATION_MODEL_EVALUATION.md`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`
- `.gitignore`
- `CHANGELOG.md`
- `changelog/2026-09_part2.md`

### 实际结果 / Actual Results

- [完成] [eval] [feat] 已实现评估 runner、版本化数据集/配置、provider 配置脱敏、唯一运行目录、逐 case JSONL、指标与质量阈值门禁；真实模型预检因 Codex OAuth 不可用而生成 blocked 产物。
- [Completed] [Eval] [Feat] Implemented the evaluation runner, versioned dataset/config, redacted provider config, unique run directories, per-case JSONL, metrics, and quality thresholds; the real-model preflight produced a blocked artifact because Codex OAuth was unavailable.
- [完成] [eval] [refactor] 将 criteria/evidence-ref 校验下沉到 `verification/validation.py`，消除评估模块对 Agent 层的反向依赖；启动异常写入 terminal error manifest/metrics。
- [Completed] [Eval] [Refactor] Moved criteria/evidence-reference validation into `verification/validation.py` to remove the evaluation module's reverse dependency on the Agent layer; startup failures now persist terminal error manifests/metrics.
- [完成] [eval] [review] 按架构集成、失败路径、权威边界、配置和可维护性复审：修复 4 个 Major（反向依赖、非终态启动错误、fixture 身份冒充、部分 case 门禁绕过）；最终无遗留 Blocker/Major。
- [Completed] [Eval] [Review] Re-reviewed architecture integration, failure paths, authority boundaries, configuration, and maintainability: fixed four Major findings (reverse dependency, non-terminal startup errors, fixture identity masquerading, and partial-case gate bypass); no Blocker or Major remains.

### 文件变更详情 / File Change Details

- [新增 / Added] `PhyAgentOS/verification/evaluation.py:L1-L675`、`PhyAgentOS/verification/validation.py:L1-L34`。
- [修改 / Modified] `PhyAgentOS/verification/request_builder.py:L35-L52,L389`、`PhyAgentOS/agent/session_verifier.py:L29-L32,L178-L192`。
- [新增 / Added] `scripts/evaluate_verification_model.py:L1-L37`、`evals/verification/semantic_verifier_v1.json:L1-L299`、`evals/verification/evaluation_config_v1.json:L1-L23`、`evals/verification/provider.openai_codex.example.json:L1-L11`、`tests/test_verification_model_evaluation.py:L1-L449`。
- [修改 / Modified] `.gitignore:L29`、`docs/forge/VERIFICATION_MODEL_EVALUATION.md:L1-L75`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L214-L250`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L315-L320`、`CHANGELOG.md:L10-L38`。

### 关键代码 Diff / Key Code Diff

```text
修改前 / Before:
quality_gate_eligible = provider_config.evaluation_mode == "real_model"
评估模块复用 Agent-private verdict validator；启动异常可能留下 status=running。

修改后 / After:
quality_gate_eligible = real_model + versioned non-custom provider binding + full case set
criteria/evidence validation 由 Verification 层拥有；blocked/error 均写 terminal manifest/metrics 和不合格原因。
```

### 验证 / Validation

- Verification/evaluation focused suite → `57 passed`；全仓库 → `136 passed`。
- Pick-place workflow + RoboTwin adapter → `310 passed`；使用现有 PAOS site-packages 与系统 NumPy，未修改环境或依赖。
- `python scripts/evaluate_verification_model.py --config evals/verification/evaluation_config_v1.json --provider-config evals/verification/provider.openai_codex.example.json` → exit `2`，生成 blocked run；原因已写入 manifest/metrics，未调用模型。
- `python -m ruff check PhyAgentOS tests scripts/evaluate_verification_model.py`、compileall、`git diff --check`、Verification→Agent 反向依赖扫描、凭据产物审查 → 通过。

## v3.6.2 (2026-09-03 22:40) - codex

### 预期修改 / Planned Changes

- [计划] [chore] [docs] 校正 v3.6.0 在入口 CHANGELOG 中记录的服务与测试精确行号。
- [Planned] [Chore] [Docs] Correct the exact service and test line ranges recorded for v3.6.0 in the top-level CHANGELOG.

### 实际结果 / Actual Results

- [完成] [chore] [docs] 已将入口 `CHANGELOG.md` 的 v3.6.0 行号更新为 `service.py:L28,L282-L314,L405-L418` 和 `test_verification_service_process.py:L1-L230`。
- [Completed] [Chore] [Docs] Updated the v3.6.0 ranges in the top-level `CHANGELOG.md` to `service.py:L28,L282-L314,L405-L418` and `test_verification_service_process.py:L1-L230`.

### Git 提交 / Git Commit

- Commit: `f702322` (line-range correction being recorded)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)

## v3.2.0 (2026-09-03 18:30) - codex

### 变更摘要 / Change Summary

- [完成] [sense] 完成 `ENVIRONMENT.md` 的严格 projection 适配：定义 snapshot/provenance schema，替换宽松 SceneGraph loader，并保持 Evidence 为唯一语义事实源；不接入动作或运动。
- [Completed] [Sense] Completed strict `ENVIRONMENT.md` projection adaptation: defined the snapshot/provenance schema, replaced the permissive SceneGraph loader, and kept Evidence as the sole semantic authority; no action or motion integration.

### 预期影响文件 / Expected Files

- `PhyAgentOS/state_io/adapters.py` [修改 / modified：Environment projection schema/parse / schema and parser]
- `PhyAgentOS/state_io/__init__.py` [修改 / modified：导出严格环境 API / export strict environment API]
- `PhyAgentOS/agent/tools/scene_graph.py` [修改 / modified：严格消费 projection / strict projection consumer]
- `PhyAgentOS/templates/ENVIRONMENT.md` [修改 / modified：同步 paos.state-file.v1 模板 / align template]
- `tests/test_state_file_adapter.py` [修改 / modified：环境 schema、漂移和 fail-closed 测试 / schema, drift, fail-closed tests]
- `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md` [修改 / modified：记录环境边界 / record environment boundary]
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` [修改 / modified：记录阶段 B 状态 / record phase-B status]
- `CHANGELOG.md` [修改 / modified：滚动版本索引 / rolling version index]

### 实施计划 / Implementation Plan

1. 定义带 `scene_revision`、`snapshot_ref`、`phase`、`captured_at`、`source_id`、`frame`、`calibration_ref` 和 scene graph 的严格 schema。
2. 让 `render_environment_projection()` 和 `parse_environment_projection()` 只接受合法 schema，并拒绝错误 revision/provenance。
3. 将 `SceneGraphQueryTool` 从宽松 loader 切换到严格 parser；缺失或损坏文件显式返回 bounded error。
4. 补充 projection drift、快照来源、模板兼容和 no-motion 测试；不改变 Verifier/Evidence 权威边界。

### 实际变更 / Actual Changes

- [sense] [feat] `PhyAgentOS/state_io/adapters.py:L88-L148,L330-L344,L563-L574` 新增 `EnvironmentProjectionData`、严格 parser 和 revision/provenance 校验；projection 仍为非权威视图。
- [Sense] [Feat] `PhyAgentOS/state_io/adapters.py:L88-L148,L330-L344,L563-L574` adds `EnvironmentProjectionData`, a strict parser, and revision/provenance checks; the projection remains non-authoritative.
- [comm] [refactor] `PhyAgentOS/agent/tools/scene_graph.py:L11-L64` 改用严格 environment parser；缺失或损坏文件返回 bounded error，不再静默返回空场景。
- [Comm] [Refactor] `PhyAgentOS/agent/tools/scene_graph.py:L11-L64` switches to the strict environment parser; missing or damaged files return a bounded error instead of silently producing an empty scene.
- [docs] [feat] `PhyAgentOS/templates/ENVIRONMENT.md:L1-L34` 同步 `paos.state-file.v1` projection 模板；`state_io/__init__.py:L3-L55` 导出严格环境 API。
- [Docs] [Feat] `PhyAgentOS/templates/ENVIRONMENT.md:L1-L34` aligns the template with the `paos.state-file.v1` projection; `state_io/__init__.py:L3-L55` exports the strict environment API.
- [eval] [exp] `tests/test_state_file_adapter.py:L264-L335,L406-L414` 覆盖合法 schema、来源/校准 URI、时间戳、revision mismatch、SceneGraph 严格消费和旧版文件拒绝；全量本地 tests 共 26 项通过。
- [Eval] [Exp] `tests/test_state_file_adapter.py:L264-L335,L406-L414` covers valid schema, source/calibration URIs, timestamps, revision mismatch, strict SceneGraph consumption, and legacy-file rejection; all 26 local tests pass.

### 关键 Diff / Key Diff

```text
Before: ENVIRONMENT.md used a legacy permissive loader and generic projection mapping.
After:  projection data carries snapshot/provenance/revision metadata, SceneGraph consumes only the strict
        paos.state-file.v1 form, and malformed or legacy files fail closed. Evidence remains authoritative.
```

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests` → `26 passed`。
- `ruff check PhyAgentOS/state_io PhyAgentOS/agent/tools/scene_graph.py tests/test_state_file_adapter.py`、`python -m compileall -q PhyAgentOS/state_io PhyAgentOS/agent/tools/scene_graph.py tests/test_state_file_adapter.py` 和 `git diff --check` 均通过。
- 未调用硬件、仿真器、Gateway、Watchdog 或 Action；未接入 Evidence producer 或 Verifier authority。

### Git 提交 / Git Commit

- Commit: `00437e7` (ENVIRONMENT strict projection implementation, tests, documentation, and changelog)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)
## v3.1.1 (2026-09-03 18:00) - codex

### 变更摘要 / Change Summary

- [完成] [eval] 对最近三个 `TARGETS.md` candidate 功能执行实现审查和完整测试；补齐 profile identity、审批失败、baseline 差异及 no-motion 边界，并修复审查发现的高风险问题。
- [Completed] [Eval] Reviewed and fully tested the three recent `TARGETS.md` candidate features; covered profile identity, approval failures, baseline differences, and no-motion boundaries, and fixed the high-risk review finding.

### 预期影响文件 / Expected Files

- `PhyAgentOS/state_io/adapters.py` [修改 / modified：profile identity fail-closed 校验 / profile identity fail-closed validation]
- `tests/test_state_file_adapter.py` [修改 / modified：完整 candidate review tests / complete candidate review tests]
- `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md` [修改 / modified：审查和测试边界 / review and test boundary]
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` [修改 / modified：记录审查结论 / record review findings]
- `CHANGELOG.md` [修改 / modified：滚动版本索引 / rolling version index]

### 实施计划 / Implementation Plan

1. 审查审批模型、候选返回值和 promotion 入口的 schema、身份、漂移和副作用边界。
2. 补充 malformed profile、非法审批、baseline 差异批准、输入文件不变和 no-motion 测试。
3. 对 Blocker/Major 问题直接修复，运行全量本地 tests、ruff、compileall 和 diff 检查。

### 审查发现 / Review Findings

- [Major fixed] `targets.profile_id` 原先允许路径分隔符，可能造成 capability identity 混淆；现已在 shadow validation 中拒绝路径不安全值。
- [Major fixed] `TargetProfileApproval`、candidate 返回值和 promotion 的失败边界已补齐；无效 decision、无时区时间、baseline drift 和非法 candidate 均 fail-closed。
- [Minor remaining] `TargetProfileCandidate` 是 frozen dataclass，但嵌套 `data` 映射仍可变；当前 candidate 仅用于非权威比较/replay，未来跨进程缓存前需定义深度不可变序列化契约。

### 实际变更 / Actual Changes

- [policy] [fix] `PhyAgentOS/state_io/adapters.py:L184-L191` 增加 `profile_id` path-safe 校验，阻断路径语义污染。
- [Policy] [Fix] `PhyAgentOS/state_io/adapters.py:L184-L191` adds path-safe `profile_id` validation to prevent path-semantic identity pollution.
- [eval] [exp] `tests/test_state_file_adapter.py:L82-L180` 新增 4 项审查测试，覆盖非法 profile、显式 baseline 差异、输入文件不变、审批 decision/时间戳和 candidate no-motion；定向套件共 18 项通过。
- [Eval] [Exp] `tests/test_state_file_adapter.py:L82-L180` adds four review tests covering invalid profiles, explicit baseline differences, input immutability, approval decision/timestamp, and candidate no-motion; the focused suite passes all 18 tests.
- [docs] [docs] `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md:L50-L56` 和 `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L246-L255` 记录审查结果及剩余 Minor 风险。
- [Docs] [Docs] `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md:L50-L56` and `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L246-L255` record review results and the remaining Minor risk.

### 关键 Diff / Key Diff

```text
Before: profile_id accepted path-like identities and review coverage focused on the happy path.
After:  path-unsafe profiles fail closed; approval, drift, explicit-difference, immutability, and no-motion
        tests exercise the candidate boundary. The nested candidate mapping remains a documented Minor risk.
```

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests/test_state_file_adapter.py` → `18 passed`。
- `ruff check PhyAgentOS/state_io PhyAgentOS/forge/task.py tests/test_state_file_adapter.py`、`python -m compileall -q PhyAgentOS/state_io PhyAgentOS/forge/task.py tests/test_state_file_adapter.py` 和 `git diff --check` 均通过。
- 未调用硬件、仿真器、Gateway、Watchdog 或 Action；未增加运动授权。

### Git 提交 / Git Commit

- Commit: `767cf6d` (review fixes, tests, documentation, and changelog)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)

## v3.0.0 (2026-09-03 17:00) - codex

### 变更摘要 / Change Summary

- [完成] [comm] 在既有无副作用适配器之上增加人工确认绑定、幂等 `SESSIONS.md` 编译入口；仅通过 `AgentTaskCoordinator.create_task()` 写入既有 SQLite 事实源，不直接写库、不调度 Watchdog、不调用 Gateway 或授权运动。
- [Completed] [Comm] Added human-approval binding and an idempotent `SESSIONS.md` compilation entry on top of the side-effect-free adapters; writes only through `AgentTaskCoordinator.create_task()` to the existing SQLite authority, with no direct DB writes, Watchdog dispatch, Gateway calls, or motion authorization.

### 预期影响文件 / Expected Files

- `PhyAgentOS/state_io/adapters.py` [修改 / modified：人工确认、编译器和幂等结果 / approval, compiler, idempotent result]
- `PhyAgentOS/state_io/__init__.py` [修改 / modified：导出受限编译 API / export bounded compiler API]
- `PhyAgentOS/forge/task.py` [修改 / modified：保存可选 parent_task_id 并提供幂等查询 / persist optional parent_task_id and idempotent lookup]
- `tests/test_state_file_adapter.py` [修改 / modified：编译、审批、冲突和 no-motion 测试 / compiler, approval, conflict, and no-motion tests]
- `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md` [修改 / modified：记录 promotion 边界 / record promotion boundary]
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` [修改 / modified：记录阶段 C 实现状态 / record phase-C implementation status]
- `CHANGELOG.md` [修改 / modified：滚动版本索引 / rolling version index]

### 实施计划 / Implementation Plan

1. 增加严格的 `SessionCompileApproval`，审批凭据必须绑定源文件 digest、审批人和 ISO-8601 时间。
2. 增加异步 `compile_sessions_to_agent_tasks()`，默认只接受单个 session；编译前检查全局非终态任务，重复源/session 返回已有任务，冲突 fail-closed。
3. 通过 `AgentTaskCoordinator.create_task()` 创建任务；把 acceptance criteria、retry limit 和 parent identity 映射到现有任务契约，并明确不执行、不调度。
4. 增加 Fake coordinator/store 测试覆盖审批缺失、digest 漂移、重复编译、活动任务冲突、多 session 原子性和 no-motion 边界。

### 实际变更 / Actual Changes

- [comm] [feat] `PhyAgentOS/state_io/adapters.py:L43-L372` 新增不可变 `SessionCompileApproval`、digest 绑定校验、单 session 幂等 compiler、活动任务/父任务检查和固定 no-motion 结果；`activation_id` 仅转发给受管 Runtime，不创建 Skill activation。
- [Comm] [Feat] `PhyAgentOS/state_io/adapters.py:L43-L372` adds immutable `SessionCompileApproval`, digest binding, single-session idempotent compilation, active/parent checks, and fixed no-motion results; `activation_id` is forwarded to managed Runtimes but no Skill activation is created.
- [model] [feat] `PhyAgentOS/forge/task.py:L153-L181,L300-L315,L420-L527` 为 `AgentTaskRecord` 增加可选 `parent_task_id`、`retry_limit`，并提供不依赖 SQLite JSON1 扩展的 origin-key 查询；`AgentTaskCoordinator.create_task()` 接受并保存这些声明式字段。
- [Model] [Feat] `PhyAgentOS/forge/task.py:L153-L181,L300-L315,L420-L527` adds optional `parent_task_id` and `retry_limit` to `AgentTaskRecord`, provides an origin-key lookup without SQLite JSON1, and accepts/persists the declarative fields through `AgentTaskCoordinator.create_task()`.
- [docs] [feat] `PhyAgentOS/state_io/__init__.py:L3-L39` 导出受限 promotion API；`docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md:L6-L59`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L228-L244` 更新阶段 C 边界、审批和幂等规则。
- [Docs] [Feat] `PhyAgentOS/state_io/__init__.py:L3-L39` exports the bounded promotion API; `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md:L6-L59` and `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L228-L244` update phase-C boundaries, approval, and idempotency rules.
- [eval] [exp] `tests/test_state_file_adapter.py:L207-L322` 新增 4 个测试，覆盖审批 digest 不匹配、重复复用、活动任务/多 session 冲突、未知父任务和 no-motion；定向套件共 13 项通过。
- [Eval] [Exp] `tests/test_state_file_adapter.py:L207-L322` adds four tests covering approval digest mismatch, idempotent reuse, active-task/multi-session conflicts, unknown parents, and no-motion; the focused suite passes all 13 tests.

### 关键 Diff / Key Diff

```text
Before: SESSIONS.md stopped at parse/preview and could not cross the approved input boundary.
After:  a digest-bound human approval permits one-session promotion through AgentTaskCoordinator;
        repeated source/session compilation reuses the existing record, while active conflicts fail closed.
        No Watchdog, Gateway, Action, or motion path is introduced.
```

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests/test_state_file_adapter.py` → `13 passed`。
- `ruff check PhyAgentOS/state_io PhyAgentOS/forge/task.py tests/test_state_file_adapter.py`、`python -m compileall -q PhyAgentOS/state_io PhyAgentOS/forge/task.py tests/test_state_file_adapter.py` 和 `git diff --check` 均通过。
- 现有 `examples/forge-skills/pick-place-workflow/tests` 仍因环境未注入 `pick_place_workflow` 模块而无法收集；该失败不计入本次适配器通过率。
- Existing `examples/forge-skills/pick-place-workflow/tests` remain uncollectable because this environment does not inject the `pick_place_workflow` module; this is not counted as an adapter pass.
- 未调用硬件、仿真器、Gateway、Watchdog 或 Action；未增加运动授权。

### Git 提交 / Git Commit

- Commit: `647a71d` (implementation, tests, documentation, and changelog)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)

## v2.10.0 (2026-09-03 16:40) - codex

### 变更摘要 / Change Summary

- [完成] [docs] [feat] 按审核结论新增 State File Adapter 功能引用卡，并实现第一阶段的严格 Markdown 结构化解析、projection 元数据/漂移校验、`TARGETS.md` shadow validation 与 `SESSIONS.md` dry-run 预览；不写入 AgentTask 生命周期、不启动 Watchdog、不触发 Gateway 或物理动作。
- [Completed] [docs] [feat] Following the reviewed direction, added the State File Adapter feature reference card and implemented phase-one strict Markdown parsing, projection metadata/drift checks, `TARGETS.md` shadow validation, and `SESSIONS.md` dry-run preview; no AgentTask lifecycle state, Watchdog work, Gateway call, or motion is triggered.

### 预期影响文件 / Expected Files

- `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md` [新增 / added：功能引用卡 / feature reference card]
- `PhyAgentOS/state_io/` [新增 / added：生产适配模块 / production adapter module]
- `tests/test_state_file_adapter.py` [新增 / added：解析、校验、projection、dry-run 测试 / parser, validation, projection, dry-run tests]
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` [修改 / modified：记录首阶段已实现边界 / record implemented phase-one boundary]
- `changelog/2026-09_part2.md` [修改 / modified：本记录 / this record]

### 实际变更 / Actual Changes

- [model] [feat] `PhyAgentOS/state_io/protocol.py:L1-L224` 新增 `paos.state-file.v1` 严格 envelope 解析、opaque URI 元数据校验、有限 JSON 值校验、canonical SHA-256 和原子 projection 写入；漂移通过 `StateFileDriftError` 显式拒绝。
- [Model] [Feat] `PhyAgentOS/state_io/protocol.py:L1-L224` adds strict `paos.state-file.v1` envelope parsing, opaque-URI metadata validation, finite JSON checks, canonical SHA-256, and atomic projection writes; drift is explicitly rejected with `StateFileDriftError`.
- [model] [feat] `PhyAgentOS/state_io/adapters.py:L1-L214` 新增 `TARGETS.md` shadow validation、`SESSIONS.md` 确定性 dry-run preview，以及 Runtime/Environment/Lessons projection 入口；所有结果固定 no-motion，未写入 AgentTask 或 Gateway。
- [Model] [Feat] `PhyAgentOS/state_io/adapters.py:L1-L214` adds `TARGETS.md` shadow validation, deterministic `SESSIONS.md` dry-run previews, and Runtime/Environment/Lessons projection entry points; all results remain no-motion and no AgentTask or Gateway state is written.
- [docs] [feat] `PhyAgentOS/state_io/__init__.py:L1-L35` 导出受限 API；`docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md:L1-L61` 固定规范来源、所有权、失败语义、验收和非目标。
- [Docs] [Feat] `PhyAgentOS/state_io/__init__.py:L1-L35` exports the bounded API; `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md:L1-L61` fixes normative references, ownership, failure semantics, acceptance, and non-goals.
- [eval] [exp] `tests/test_state_file_adapter.py:L1-L198` 覆盖合法/非法 envelope、限幅、drift、projection、确定性 dry-run、重复/不安全身份和 no-motion 边界；`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L162-L228` 记录阶段状态与下一道 promotion gate；`docs/README.md:L30,L65` 增加引用卡索引。
- [Eval] [Exp] `tests/test_state_file_adapter.py:L1-L198` covers valid/invalid envelopes, limits, drift, projections, deterministic dry-runs, duplicate/unsafe identities, and no-motion boundaries; `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L162-L228` records phase status and the next promotion gate; `docs/README.md:L30,L65` indexes the feature card.

### 关键 Diff / Key Diff

```text
Before: no strict PAOS state-file adapter; ENVIRONMENT.md fallback could silently return {}.
After:  strict paos.state-file.v1 envelope; malformed input fails closed, projections are atomic,
        TARGETS is shadow-only, and SESSIONS is deterministic dry-run only.
```

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests/test_state_file_adapter.py` → `9 passed`。
- `ruff check PhyAgentOS/state_io tests/test_state_file_adapter.py`、`python -m compileall -q PhyAgentOS/state_io tests/test_state_file_adapter.py` 和 `git diff --check` 均通过。
- 尝试合并运行现有 `examples/forge-skills/pick-place-workflow/tests` 未采纳：当前系统 Python 缺少 `loguru`，且未注入 `pick_place_workflow` 的独立测试路径；因此不把该环境失败归因于本次适配器。
- A combined run of the existing `examples/forge-skills/pick-place-workflow/tests` was not accepted because the system Python lacks `loguru` and the independent `pick_place_workflow` test path was not injected; this environment failure is not attributed to the adapter.
- 未调用硬件、仿真器、Gateway、Watchdog 或 AgentTask store；未增加动作或运动授权。

### Git 提交 / Git Commit

- Commit: `dbf6b4a` (implementation and documentation update)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)

## v2.9.1 (2026-09-03 16:22) - codex

### 变更摘要 / Change Summary

- [完成] [docs] [docs] 审核确认“先做文件适配、后做抓取放置闭环”符合 PAOS 扩展原则，并将执行文档调整为先实现无副作用的 projection、解析、dry-run、shadow validation 和 drift 检查，再推进抓取放置。
- [Completed] [docs] [docs] Confirmed that “file adapters before the pick-place closure” conforms to PAOS extension principles and updated the execution documents to implement side-effect-free projections, parsing, dry-runs, shadow validation, and drift checks before advancing pick-place.

### 预期影响文件 / Expected Files

- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` [修改 / modified：更新审核结论与执行顺序 / update review decision and execution order]
- `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` [修改 / modified：同步 RoboTwin 执行顺序 / synchronize RoboTwin order]
- `changelog/2026-09_part2.md` [修改 / modified：本记录 / this record]

### 实际变更 / Actual Changes

- [docs] [docs] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L162-L208,L249-L257` 审核确认“受限文件适配先行”符合 PAOS 扩展原则，并将执行顺序改为：冻结契约 → 无副作用适配与回放 → 输入边界提升 → 抓取放置闭环 → 受控自主进化。
- [Docs] [Docs] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L162-L208,L249-L257` confirms that restricted file adapters first conform to PAOS extension principles and changes the order to contract freeze → side-effect-free adapters and replay → input-boundary promotion → pick-place closure → guarded evolution.
- [docs] [docs] `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md:L398-L414,L543-L547` synchronizes the RoboTwin execution order, explicitly limits adapters to projection/shadow/dry-run behavior, and adds the English summary.
- [Docs] [Docs] `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md:L398-L414,L543-L547` synchronizes the RoboTwin execution order, limits adapters to projection/shadow/dry-run behavior, and adds the English summary.
- [docs] [docs] `CHANGELOG.md:L36-L63` adds the v2.9.1 review result and revised order; the existing uncommitted v2.8.17 grasp-proposal entry was not staged or altered by this task.
- [Docs] [Docs] `CHANGELOG.md:L36-L63` adds the v2.9.1 review result and revised order; the existing uncommitted v2.8.17 grasp-proposal entry was not staged or altered by this task.

### 关键 Diff / Key Diff

```text
Before: freeze contract -> pick-place evidence closure -> Markdown adapters -> evolution
After:  freeze contract -> restricted file adapters/replay -> approved input promotion
        -> pick-place evidence closure -> guarded evolution
```

适配层的硬性边界 / Adapter hard boundary:

```text
Allowed: parse, schema validation, projection, dry-run, shadow validation, drift report, replay
Forbidden: Watchdog queue, AgentTask lifecycle ownership, Gateway execution, Action admission, motion
```

### 验证 / Validation

- `git diff --check` passed.
- Reviewed against PAOS ownership, provider-neutral ToolSpec, no-second-execution-protocol, projection authority, and no-motion principles.
- Confirmed no source code, runtime behavior, hardware IO, or motion authorization changed.

### Git 提交 / Git Commit

- Commit: `29e70b9` (documentation-only order update)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)

## v3.1.0 (2026-09-03 17:30) - codex

### 变更摘要 / Change Summary

- [完成] [policy] 增加 `TARGETS.md` 的已验证 Capability Profile 候选对象和人工确认绑定；候选层只用于 shadow comparison/replay，不改变 Action admission 或运动授权。
- [Completed] [Policy] Added a validated Capability Profile candidate and human-approval binding for `TARGETS.md`; the candidate remains limited to shadow comparison/replay and cannot change Action admission or motion authorization.

### 预期影响文件 / Expected Files

- `PhyAgentOS/state_io/adapters.py` [修改 / modified：TARGETS 候选模型和 promotion 入口 / target candidate model and promotion entry]
- `PhyAgentOS/state_io/__init__.py` [修改 / modified：导出候选 API / export candidate API]
- `tests/test_state_file_adapter.py` [修改 / modified：候选、审批、baseline 漂移和 no-motion 测试 / candidate, approval, baseline drift, and no-motion tests]
- `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md` [修改 / modified：记录 TARGETS candidate 边界 / record TARGETS candidate boundary]
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` [修改 / modified：记录阶段 C TARGETS promotion / record phase-C TARGETS promotion]
- `CHANGELOG.md` [修改 / modified：滚动版本索引 / rolling version index]

### 实施计划 / Implementation Plan

1. 增加严格的 `TargetProfileApproval`，审批凭据绑定 `TARGETS.md` 源 digest 与 baseline digest。
2. 增加 `promote_targets_candidate()`，仅在 shadow validation 通过且差异显式确认后返回不可变候选对象。
3. 候选对象固定 `motion_authorized=false`，不写 Runtime/Profile 权威配置、不调用 Gateway/Watchdog、不进入 Action admission。
4. 增加失败闭环和 replay 测试，验证 malformed input、baseline drift、审批不匹配和 candidate no-motion 边界。

### 实际变更 / Actual Changes

- [policy] [feat] `PhyAgentOS/state_io/adapters.py:L24-L145,L253-L300` 新增 `TargetProfileApproval`、`TargetProfileCandidate` 和 `promote_targets_candidate()`；候选必须通过 shadow validation，并同时绑定 source/baseline digest。
- [Policy] [Feat] `PhyAgentOS/state_io/adapters.py:L24-L145,L253-L300` adds `TargetProfileApproval`, `TargetProfileCandidate`, and `promote_targets_candidate()`; candidates must pass shadow validation and bind both source and baseline digests.
- [docs] [feat] `PhyAgentOS/state_io/__init__.py:L3-L42` 导出候选 API；`docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md:L24-L47`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L240-L247` 明确 candidate 不是 admission。
- [Docs] [Feat] `PhyAgentOS/state_io/__init__.py:L3-L42` exports the candidate API; `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md:L24-L47` and `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L240-L247` state that the candidate is not admission.
- [eval] [exp] `tests/test_state_file_adapter.py:L61-L130` 新增 candidate 成功和 baseline drift 失败测试；定向套件共 15 项通过。
- [Eval] [Exp] `tests/test_state_file_adapter.py:L61-L130` adds successful candidate and baseline-drift failure tests; the focused suite passes all 15 tests.

### 关键 Diff / Key Diff

```text
Before: TARGETS.md produced only a shadow report.
After:  an explicitly approved, source/baseline-bound candidate can be returned for comparison/replay;
        it remains non-authoritative and cannot alter Runtime/Profile admission or motion limits.
```

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests/test_state_file_adapter.py` → `15 passed`。
- `ruff check PhyAgentOS/state_io PhyAgentOS/forge/task.py tests/test_state_file_adapter.py`、`python -m compileall -q PhyAgentOS/state_io PhyAgentOS/forge/task.py tests/test_state_file_adapter.py` 和 `git diff --check` 均通过。
- 未调用硬件、仿真器、Gateway、Watchdog 或 Action；candidate 的 `motion_authorized` 固定为 `false`。

### Git 提交 / Git Commit

- Commit: `0a7669b` (TARGETS candidate implementation, tests, documentation, and changelog)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)
## v3.3.0 (2026-09-03 19:00) - codex

### 变更摘要 / Change Summary
- [完成] [sense] [feat] 增加 Environment producer：从已捕获的 `ObservationSnapshot` 和显式环境元数据生成严格 `ENVIRONMENT.md` projection，并绑定 opaque snapshot reference；不调用 Gateway、Watchdog、Action 或硬件，不替代 Evidence/Verifier 权威。
- [Completed] [Sense] [Feat] Added an Environment producer that renders a strict `ENVIRONMENT.md` projection from a captured `ObservationSnapshot` and explicit environment metadata, binding an opaque snapshot reference; it does not call Gateway, Watchdog, Action, or hardware, and does not replace Evidence/Verifier authority.

### 预期影响文件 / Expected Files
- `PhyAgentOS/forge/environment_projection.py` [新增 / added]
- `PhyAgentOS/forge/__init__.py` [修改 / modified：导出 producer / export producer]
- `tests/test_environment_projection_producer.py` [新增 / added]
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` [修改 / modified：更新 Environment producer 状态 / update producer status]
- `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md` [修改 / modified：记录 producer 边界 / record producer boundary]
- `CHANGELOG.md` [修改 / modified：更新最近版本索引 / update rolling version index]

### 实际结果 / Actual Results
- [完成] [sense] [feat] 新增 `EnvironmentProjectionInput` 与 `EnvironmentProjectionProducer`，支持 adapter revision binding、原子 projection、幂等和 drift guard；扩展环境 renderer 的 `expected_sha256`；补充 fail-closed、无副作用和 revision 绑定测试。
- [Completed] [Sense] [Feat] Added `EnvironmentProjectionInput` and `EnvironmentProjectionProducer` with adapter revision binding, atomic projection, idempotency, and drift guard; extended the environment renderer with `expected_sha256`; added fail-closed, no-side-effect, and revision-binding tests.

### 文件变更详情 / File Change Details

#### [新增 / Added] `PhyAgentOS/forge/environment_projection.py` L1-L180

**新增代码 / Added:**
```python
class EnvironmentProjectionProducer:
    def publish(...): ...
    def publish_from_adapter(...): ...
```
**说明 / Description:** 只消费已捕获 snapshot 和显式 provenance；不执行传感器采集、Gateway、任务调度或动作授权。

#### [修改 / Modified] `PhyAgentOS/state_io/adapters.py` L543-L607

**修改前 / Before:** `render_environment_projection()` 不接受 drift guard。

**修改后 / After:** `render_environment_projection(..., expected_sha256=...)` 将 digest guard 传递到原子 projection writer。

#### [新增 / Added] `tests/test_environment_projection_producer.py` L1-L144

**说明 / Description:** 覆盖成功、幂等、drift、空 observation、非法 metadata、adapter revision mismatch 和 no-capture 边界。

### 预期验证 / Planned Validation
- 覆盖 before/after producer 成功链路、snapshot reference/scene revision/frame/calibration 校验、缺失或错误元数据 fail-closed、输出 drift/幂等，以及 producer 不产生任务或动作副作用。
- 运行 PAOS 测试、Ruff、compileall、`git diff --check`；不宣称 Verifier 语义验收或真实抓取放置完成。

### 验证 / Validation
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests` → `33 passed`（含状态文件适配与 producer 测试）。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests/test_state_file_adapter.py tests/test_environment_projection_producer.py` → `33 passed`（定向重跑）。
- `ruff check PhyAgentOS/forge/environment_projection.py PhyAgentOS/state_io/adapters.py PhyAgentOS/forge/__init__.py tests/test_environment_projection_producer.py` → passed。
- `python -m compileall -q ...` and `git diff --check` → passed。
- 未运行完整 pick-place live provider；未产生 Gateway invocation、AgentTask、Evidence verdict 或运动授权。
- Full pick-place live providers were not run; no Gateway invocation, AgentTask, Evidence verdict, or motion authorization was produced.

### Git 提交 / Git Commit
- Commit: `a970daa` (Environment projection producer implementation and validation)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)
## v3.4.0 (2026-09-03 20:00) - codex

### 预期修改 / Planned Changes
- [计划] [sense] [feat] 增加 `ForgeEvidenceWriter` 到 `EnvironmentProjectionProducer` 的受限关联：校验 writer 生成的 before/after snapshot manifest，生成稳定 `evidence://` snapshot reference，并由 producer 自动注入或拒绝不匹配引用；保持 Evidence/Verifier 为权威，不创建任务或动作。
- [Planned] [Sense] [Feat] Add a bounded association from `ForgeEvidenceWriter` to `EnvironmentProjectionProducer`: validate writer-produced before/after snapshot manifests, derive a stable `evidence://` snapshot reference, and have the producer inject or reject mismatched references; keep Evidence/Verifier authoritative and create no tasks or actions.

### 预期影响文件 / Expected Files
- `PhyAgentOS/forge/evidence.py` [修改 / modified：snapshot provenance URI / snapshot provenance URI]
- `PhyAgentOS/forge/environment_projection.py` [修改 / modified：evidence-writer producer 入口 / evidence-writer producer entry point]
- `tests/test_environment_projection_producer.py` [修改 / modified：writer association and failure tests]
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` [修改 / modified：记录 snapshot 关联状态 / record association status]
- `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md` [修改 / modified：记录 writer association boundary]
- `CHANGELOG.md` [修改 / modified：更新最近版本索引 / update rolling version index]

### 预期验证 / Planned Validation
- 覆盖 writer 生成的 before/after manifest、稳定 evidence URI、phase/path mismatch、缺失 manifest、错误 URI、幂等和 no-side-effect；运行根 tests、Ruff、compileall、`git diff --check`。
- 不宣称 Verifier 语义验收、跨环境 replay 或抓取放置闭环完成。

### 实际结果 / Actual Results
- [完成] [sense] [feat] `ForgeEvidenceWriter` 现在记录安全 namespace，验证 writer-owned before/after manifest、拒绝 malformed manifest 和同 phase 内容覆盖；`EnvironmentProjectionProducer.publish_from_evidence_writer()` 自动关联 snapshot phase 与稳定 `evidence://` reference，并拒绝不匹配 metadata。
- [Completed] [Sense] [Feat] `ForgeEvidenceWriter` now records a safe namespace, validates writer-owned before/after manifests, rejects malformed manifests and content replacement within a phase; `EnvironmentProjectionProducer.publish_from_evidence_writer()` automatically associates snapshot phase and stable `evidence://` references and rejects mismatched metadata.

### 文件变更详情 / File Change Details

#### [修改 / Modified] `PhyAgentOS/forge/evidence.py` L27-L143

**修改前 / Before:** `write_snapshot()` 可覆盖同一 phase 的既有 snapshot，且没有稳定 opaque projection reference。

**修改后 / After:** writer 对已有 snapshot 做内容一致性检查；新增 `snapshot_projection_identity()` 和 `snapshot_projection_ref()`，仅为 writer-owned manifest 生成稳定 `evidence://` URI。

#### [修改 / Modified] `PhyAgentOS/forge/environment_projection.py` L30-L237

**修改前 / Before:** producer 只能接收调用方提供的 snapshot reference。

**修改后 / After:** `publish_from_evidence_writer()` 读取并验证 writer manifest，自动注入 phase/reference，并在 mismatch 或非法引用时 fail-closed。

#### [修改 / Modified] `tests/test_environment_projection_producer.py` L1-L194

**说明 / Description:** 增加 writer 关联、snapshot 不可覆盖、phase/reference mismatch 和非 writer 路径失败测试。

### 验证 / Validation
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests` → `36 passed`。
- `PYTHONPATH=examples/forge-skills/pick-place-workflow/src python -m pytest -q examples/forge-skills/pick-place-workflow/tests` → `241 passed`。
- `PYTHONPATH=examples/forge-adapters/robotwin20/src PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q examples/forge-adapters/robotwin20/tests/test_adapter.py examples/forge-adapters/robotwin20/tests/test_perception_profile.py examples/forge-adapters/robotwin20/tests/test_process_worker.py` → `16 passed`。
- Ruff、compileall、`git diff --check` 通过；完整 RoboTwin collection 仍受当前环境缺少 `numpy`/包路径限制。
- 未调用 Gateway、Watchdog、Action，未创建新 AgentTask，未产生运动授权或 Verifier verdict。

### Git 提交 / Git Commit
- Commit: `506be28` (Evidence snapshot and Environment projection association)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)
## v3.4.1 (2026-09-03 20:30) - codex

### 预期修改 / Planned Changes
- [计划] [eval] [test] 增加 Verifier/Evidence 边界 conformance：证明 `ENVIRONMENT.md` projection 不能作为 Evidence Bundle，且 verifier verdict 不能以 projection URI 冒充 evidence reference；不改变 Verifier 权威实现。
- [Planned] [Eval] [Test] Add Verifier/Evidence boundary conformance proving that an `ENVIRONMENT.md` projection cannot be used as an Evidence Bundle and a verifier verdict cannot use a projection URI as an evidence reference; do not change Verifier authority implementation.

### 预期影响文件 / Expected Files
- `tests/test_verifier_evidence_boundary.py` [新增 / added]
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` [修改 / modified：记录基础 verifier boundary conformance]
- `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md` [修改 / modified：更新 acceptance boundary]
- `CHANGELOG.md` [修改 / modified：更新最近版本索引 / update rolling version index]

### 预期验证 / Planned Validation
- 运行根 tests、Ruff、compileall、`git diff --check`；不宣称完整语义验收、跨环境 replay 或抓取放置闭环完成。

### 实际结果 / Actual Results
- [完成] [eval] [test] 新增 Verifier/Evidence boundary conformance：projection 无法作为 Evidence Bundle，projection URI 无法作为 verifier evidence reference；未修改 Verifier 的权威逻辑。
- [Completed] [Eval] [Test] Added Verifier/Evidence boundary conformance: projections cannot be used as Evidence Bundles, and projection URIs cannot be used as verifier evidence references; no Verifier authority logic was changed.

### 文件变更详情 / File Change Details

#### [新增 / Added] `tests/test_verifier_evidence_boundary.py` L1-L59

**新增代码 / Added:**
```python
builder._load_evidence("ENVIRONMENT.md", ...)
ForgeTaskVerifier._validate_generic_verdict(...)
```
**说明 / Description:** 验证 ENVIRONMENT projection 与 Evidence/verdict 事实边界，并保持 fail-closed。

#### [修改 / Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` L271-L283

**修改后 / After:** 记录基础 verifier boundary conformance 已完成，完整语义验收和 replay/failure conformance 仍待后续阶段。

### 验证 / Validation
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests` → `38 passed`。
- `ruff check PhyAgentOS/forge PhyAgentOS/state_io PhyAgentOS/agent/session_verifier.py PhyAgentOS/verification tests/test_verifier_evidence_boundary.py` → passed。
- `python -m compileall -q PhyAgentOS tests` and `git diff --check` → passed。
- 未调用 Gateway、Watchdog、Action，未创建 AgentTask，未产生运动授权；完整语义 verifier/live replay 仍未完成。
- No Gateway, Watchdog, or Action was called; no AgentTask or motion authorization was produced; full semantic verifier/live replay remains incomplete.

### Git 提交 / Git Commit
- Commit: `0b4ac55` (Verifier/Evidence boundary conformance)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)
## v3.4.2 (2026-09-03 21:00) - codex

### 预期修改 / Planned Changes
- [计划] [eval] [test] 增加状态文件适配的 Fake Store/Fake Gateway replay、失败回滚、projection drift 和 no-motion conformance；明确可选的 `SKILLRUNTIME.md`/`LESSONS.md` projection 不在本轮实现范围。
- [Planned] [Eval] [Test] Add Fake Store/Fake Gateway replay, failure rollback, projection drift, and no-motion conformance for state-file adapters; keep optional `SKILLRUNTIME.md`/`LESSONS.md` projections out of this iteration.

### 预期影响文件 / Expected Files
- `tests/test_state_file_replay_conformance.py` [新增 / added]
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` [修改 / modified：区分必要边界与可选 projection / distinguish required boundaries from optional projections]
- `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md` [修改 / modified：记录 replay/failure conformance / record replay and failure conformance]
- `CHANGELOG.md` [修改 / modified：更新最近版本窗口 / update rolling latest-version window]

### 实际结果 / Actual Results
- [完成] [eval] [test] 新增跨工作区确定性 replay、未知字段 fail-closed、Fake Store 编译失败无残留、projection drift 保留旧内容以及 TARGETS/SESSIONS no-motion conformance；未新增生产 Gateway、Watchdog 或 Runtime 路径。
- [Completed] [Eval] [Test] Added cross-workspace deterministic replay, unknown-field fail-closed behavior, Fake Store compilation failure without residual state, projection-drift preservation, and TARGETS/SESSIONS no-motion conformance; no production Gateway, Watchdog, or Runtime path was added.

### 文件变更详情 / File Change Details

#### [新增 / Added] `tests/test_state_file_replay_conformance.py` L1-L215

**新增代码 / Added:** replay helper、recording Gateway sentinel、failing coordinator/store fake，以及五个 conformance tests。

**修改说明 / Description:** 证明状态文件适配在跨工作区回放时保持 digest/preview identity，未知字段和 store 失败不会产生生命周期残留，projection drift 不覆盖旧内容，并持续固定 `motion_authorized=false`。

#### [修改 / Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` L280-L288

**修改说明 / Description:** 明确 replay/failure conformance 的边界，并将 `SKILLRUNTIME.md` 与 `LESSONS.md` producer 标记为可选 projection，而不是阶段 B 的核心前置条件。

### 验证 / Validation
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests` → `43 passed`。
- `PYTHONPATH=examples/forge-skills/pick-place-workflow/src python -m pytest -q examples/forge-skills/pick-place-workflow/tests` → `241 passed`。
- Ruff、compileall、`git diff --check` 通过。
- 未调用真实 Gateway、Watchdog 或 Action；测试中的 Gateway 仅为 no-call sentinel，未产生运动授权。
- No real Gateway, Watchdog, or Action was called; the test Gateway is only a no-call sentinel and no motion authorization was produced.

### Git 提交 / Git Commit
- Commit: `ae4549b` (state-file replay and failure-boundary conformance)
- Follow-up log/index commit: `a2324de` (rolling CHANGELOG update)
- Follow-up feature-card commit: `5d2b3d4` (adapter acceptance record)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)
## v3.4.3 (2026-09-03 21:30) - codex

### 预期修改 / Planned Changes
- [计划] [eval] [test] 补齐不可变 Evidence Bundle 的语义验收与跨工作区 replay/failure conformance；验证完整性、capture window、artifact digest/retention、证据策略和 verdict 绑定，不改变 Verifier 权威逻辑。
- [Planned] [Eval] [Test] Complete semantic acceptance and cross-workspace replay/failure conformance for immutable Evidence Bundles; cover completeness, capture windows, artifact digest/retention, evidence policy, and verdict binding without changing Verifier authority logic.

### 预期影响文件 / Expected Files
- `tests/test_evidence_semantic_replay_conformance.py` [新增 / added]
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` [修改 / modified：记录 Evidence 语义验收状态 / record Evidence semantic acceptance status]
- `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md` [修改 / modified：更新 Evidence conformance / update Evidence conformance]
- `CHANGELOG.md` [修改 / modified：更新最近版本窗口 / update rolling latest-version window]

### 实际结果 / Actual Results
- [完成] [eval] [test] 增加 Evidence request-level conformance：跨工作区不可变 bundle replay、capture window 顺序、必需 kind/source、authoritative association、retention、digest/size、媒体类型和结构化 JSON 校验；未修改 Verifier 的语义权威逻辑。
- [Completed] [Eval] [Test] Added Evidence request-level conformance for cross-workspace immutable bundle replay, capture-window ordering, required kind/source, authoritative association, retention, digest/size, media type, and structured JSON validation; no Verifier semantic authority logic was changed.

### 文件变更详情 / File Change Details

#### [新增 / Added] `tests/test_evidence_semantic_replay_conformance.py` L1-L184

**新增代码 / Added:** immutable `EvidenceBundle` fixtures and five replay/failure tests covering request-builder validation and cross-workspace artifact facts.

**修改说明 / Description:** 证明完整 Evidence Bundle 才能进入 request builder；损坏、过期、来源不符、digest/retention 失败均 fail-closed，结构化 artifact 仍是数据而不是 verdict。

#### [修改 / Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` L280-L287

**修改说明 / Description:** 记录 Evidence request-level conformance 的完成范围，并明确其不等同于 LLM 语义 verdict 或真实 Gateway replay。

#### [修改 / Modified] `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md` L59-L61

**修改说明 / Description:** 增加 Evidence request conformance 的测试覆盖说明。

### 验证 / Validation
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests` → `48 passed`。
- `PYTHONPATH=examples/forge-skills/pick-place-workflow/src python -m pytest -q examples/forge-skills/pick-place-workflow/tests` → `241 passed`（上一轮未改动 workflow 代码）。
- Ruff、compileall、`git diff --check` 通过。
- 未调用真实 Gateway、Watchdog 或 Action；未产生运动授权。
- No real Gateway, Watchdog, or Action was called; no motion authorization was produced.

### Git 提交 / Git Commit
- Commit: `242260e` (Evidence semantic request replay conformance)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)
## v3.4.4 (2026-09-03 22:00) - codex

### 预期修改 / Planned Changes
- [计划] [docs] 校正 RoboTwin/PAOS 执行顺序文档：区分必要文件边界与可选 projection，记录受限 promotion 与 replay conformance 的实际先后，并明确抓取放置尚未启动。
- [Planned] [Docs] Correct the RoboTwin/PAOS execution-order documents: distinguish required file boundaries from optional projections, record the actual ordering of bounded promotion versus replay conformance, and state that pick-place has not started.

### 预期影响文件 / Expected Files
- `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` [修改 / modified：同步执行顺序 / align execution order]
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` [修改 / modified：修正阶段 B 状态表述 / correct Phase-B status wording]
- `CHANGELOG.md` [修改 / modified：更新最近版本窗口 / update rolling latest-version window]

### 实际结果 / Actual Results
- [完成] [docs] 执行顺序已校正：必要文件边界与可选 projection 分离，明确 v3.0.0 promotion 早于 v3.4.2 replay conformance 的历史事实，并确认抓取放置/自主进化尚未启动。
- [Completed] [Docs] Execution order is aligned: required file boundaries are separated from optional projections, the historical v3.0.0 promotion before v3.4.2 replay conformance is recorded, and pick-place/autonomous evolution remain unstarted.

### 文件变更详情 / File Change Details

#### [修改 / Modified] `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` L398-L421

**修改说明 / Description:** 将 `SKILLRUNTIME.md`/`LESSONS.md` 标为按需 projection，并记录 promotion 顺序审查与后续冻结条件。

#### [修改 / Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` L275-L283

**修改说明 / Description:** 将 Evidence request-level conformance 与仍未完成的 LLM semantic verdict/真实 Gateway replay 区分开。

### 验证 / Validation
- `git diff --check` 通过；本轮仅修改文档。
- 未调用 Gateway、Watchdog、Action、AgentTask 或运动授权。
- `git diff --check` passed; documentation-only changes in this iteration.
- No Gateway, Watchdog, Action, AgentTask, or motion authorization was used.

### Git 提交 / Git Commit
- Commit: `5fef3dd` (execution-order documentation alignment)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)
## v3.4.5 (2026-09-03 22:30) - codex

### 预期修改 / Planned Changes
- [计划] [eval] [test] 增加 ForgeTaskVerifier 模型 verdict 的本地 conformance：成功、replan、criteria 完整性、unknown evidence 和 malformed response；不启动验证服务、不调用模型或 Gateway。
- [Planned] [Eval] [Test] Add local ForgeTaskVerifier model-verdict conformance for success, replan, criterion completeness, unknown evidence, and malformed responses; do not start the verification service or call a model or Gateway.

### 预期影响文件 / Expected Files
- `tests/test_verifier_semantic_conformance.py` [新增 / added]
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` [修改 / modified：记录本地 verdict 校验状态 / record local verdict validation status]
- `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md` [修改 / modified：更新 verifier conformance / update verifier conformance]
- `CHANGELOG.md` [修改 / modified：更新最近版本窗口 / update rolling latest-version window]

### 实际结果 / Actual Results
- [完成] [eval] [test] 新增本地 ForgeTaskVerifier verdict contract conformance：success/replan 不变量、criteria 精确绑定、unknown evidence、malformed response 和 no-service 边界；未启动验证服务或调用模型/Gateway。
- [Completed] [Eval] [Test] Added local ForgeTaskVerifier verdict contract conformance for success/replan invariants, exact criterion binding, unknown evidence, malformed responses, and the no-service boundary; no verification service or model/Gateway was started.

### 文件变更详情 / File Change Details

#### [新增 / Added] `tests/test_verifier_semantic_conformance.py` L1-L126

**新增代码 / Added:** deterministic verifier seam fixture and five verdict acceptance/rejection tests.

**修改说明 / Description:** 验证模型输出只能在 criteria、evidence reference 和 recovery contract 全部满足时通过；测试不绕过生产 Verifier 校验逻辑，也不产生执行副作用。

#### [修改 / Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` L280-L290

**修改说明 / Description:** 区分本地 verdict contract conformance 与仍待验收的 provider-backed LLM 语义质量和真实服务 replay。

#### [修改 / Modified] `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md` L59-L62

**修改说明 / Description:** 增加 local verifier verdict conformance 覆盖范围。

### 验证 / Validation
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests` → `54 passed`。
- Ruff、compileall、`git diff --check` 通过。
- 未启动 Verification Service，未调用模型、Gateway、Watchdog 或 Action；未产生运动授权。
- No Verification Service, model, Gateway, Watchdog, or Action was started or called; no motion authorization was produced.

### Git 提交 / Git Commit
- Commit: `92c3990` (local verifier verdict contract conformance)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)
## v3.4.6 (2026-09-03 23:00) - codex

### 预期修改 / Planned Changes
- [计划] [eval] [test] 增加 Verification Service HTTP replay/failure conformance：授权边界、请求 schema、provider-backed deterministic verdict、invalid model normalization、provider failure 和重复 replay；不连接外部模型。
- [Planned] [Eval] [Test] Add Verification Service HTTP replay/failure conformance for authorization, request schema, provider-backed deterministic verdicts, invalid-model normalization, provider failures, and repeated replay; do not connect to external models.

### 预期影响文件 / Expected Files
- `tests/test_verification_service_replay.py` [新增 / added]
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` [修改 / modified：记录服务级 replay 状态 / record service-level replay status]
- `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md` [修改 / modified：更新 Verification Service conformance / update Verification Service conformance]
- `CHANGELOG.md` [修改 / modified：更新最近版本窗口 / update rolling latest-version window]

### 实际结果 / Actual Results
- [完成] [eval] [test] 新增 Verification Service HTTP replay/failure conformance：授权、请求 schema、deterministic provider verdict、重复 replay、invalid-response normalization 和 provider failure；未启动生产验证子进程或连接外部模型。
- [Completed] [Eval] [Test] Added Verification Service HTTP replay/failure conformance for authorization, request schema, deterministic provider verdicts, repeated replay, invalid-response normalization, and provider failure; no production verification subprocess or external model was started.

### 文件变更详情 / File Change Details

#### [新增 / Added] `tests/test_verification_service_replay.py` L1-L117

**新增代码 / Added:** in-process HTTP server fixture with deterministic provider and authorization/request/failure replay tests.

**修改说明 / Description:** 覆盖真实 `_handler` + `VerificationEngine` 组合边界，验证 provider-backed response 的归一化和失败传播，同时保持无外部副作用。

#### [修改 / Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` L285-L296

**修改说明 / Description:** 记录 HTTP service-level conformance 的完成范围，并保留 provider-spec 子进程启动与真实模型质量为独立环境门禁。

#### [修改 / Modified] `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md` L60-L63

**修改说明 / Description:** 增加 Verification Service HTTP conformance 覆盖说明。

### 验证 / Validation
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests` → `58 passed`。
- Ruff、compileall、`git diff --check` 通过。
- 未启动生产 Verification Service，未调用外部模型、Gateway、Watchdog 或 Action；未产生运动授权。
- No production Verification Service, external model, Gateway, Watchdog, or Action was started or called; no motion authorization was produced.

### Git 提交 / Git Commit
- Commit: `cc59b11` (Verification Service HTTP replay/failure conformance)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)

## v3.5.0 (2026-09-03 19:45) - codex

### 预期修改 / Planned Changes
- [完成] [docs] [docs] 固化五类状态文件、Evidence、Verifier 与 Verification Service 的代码审查发现，并重排修复、复审和后续 provider 验证顺序。
- [Completed] [Docs] [Docs] Persisted the code-review findings for the five state-file roles, Evidence, Verifier, and Verification Service, and reordered remediation, re-review, and later provider validation.
- [完成] [policy] [fix] 修复 `SESSIONS.md` promotion 将验收准则静默置为 `mode=off` 的语义缺口，持久化 digest-bound 人工审批回执，并将 origin 幂等性下沉到 SQLite 事务边界。
- [Completed] [Policy] [Fix] Prevented `SESSIONS.md` promotion from silently disabling acceptance criteria, persisted the digest-bound human approval receipt, and enforced origin idempotency at the SQLite transaction boundary.
- [完成] [sense] [fix] 对 Observation Snapshot manifest 施加严格 schema、writer-owned 路径、size/digest 和内容身份校验；将 Evidence Bundle 改为稳定身份、幂等写入和差异拒绝。
- [Completed] [Sense] [Fix] Enforced strict schema, writer-owned paths, size/digest, and content identity for Observation Snapshot manifests; made Evidence Bundle writes stable, idempotent, and drift-rejecting.
- [完成] [env] [fix] 防止未经严格解析的 `ENVIRONMENT.md` 原文进入 system prompt，并移除旧 loader 的 fail-open 空字典回退。
- [Completed] [Env] [Fix] Prevented raw, unvalidated `ENVIRONMENT.md` text from entering the system prompt and removed the legacy loader's fail-open empty-dictionary fallback.
- [完成] [policy] [fix] 收紧 `TARGETS.md` 限幅联合结构、空数组和 candidate 深度不可变边界，保持 `motion_authorized=false` 且不接入动作准入。
- [Completed] [Policy] [Fix] Tightened `TARGETS.md` limit unions, rejected empty arrays, and made candidates deeply immutable while preserving `motion_authorized=false` and no Action-admission wiring.
- [完成] [model] [fix] 为 Verification Service 实现父/子进程共享的 fail-closed provider-spec/service schema、未知 provider 拒绝、HTTP 请求上限和对外错误规范化。
- [Completed] [Model] [Fix] Implemented shared fail-closed provider/service schemas for parent and child processes, rejected unknown providers, bounded HTTP requests, and normalized externally returned errors.
- [完成] [docs] [refactor] 明确 `SKILLRUNTIME.md` / `LESSONS.md` 为可选 projection，消除未接入的通用 renderer 与真实 Experience projection 之间的双协议歧义；删除未接入任何调用链的 Fake Gateway 验收表述。
- [Completed] [Docs] [Refactor] Kept `SKILLRUNTIME.md` and `LESSONS.md` optional, removed the dual-protocol ambiguity between unused generic renderers and the real Experience projection, and removed Fake Gateway acceptance claims that are not wired to a call path.
- [完成] [eval] [test] 使用真实 Store/writer/request/service 公开边界验证修复，然后按架构集成、失败路径、权威边界、配置与可维护性再次代码审查；不把脱离调用链的 fixture 当作实现证据。
- [Completed] [Eval] [Test] Validated fixes through the real Store, writer, request, and service public boundaries, then re-reviewed architecture integration, failure paths, authority boundaries, configuration, and maintainability without treating detached fixtures as implementation evidence.
- [完成] [policy] [fix] 修复最终复审发现的权威写入缺口：`AgentTaskStore.update()` 在提交前重新验证整个聚合，防止可变回调把非法 task 内容持久化到 SQLite。
- [Completed] [Policy] [Fix] Closed the authoritative-write gap found during final review by revalidating the full aggregate in `AgentTaskStore.update()` before committing mutable callback results to SQLite.
- [完成] [sense] [fix] 对 robot-state artifact 使用严格 JSON 编解码，拒绝 `NaN`/`Infinity` 等非标准数值，避免非法传感器状态进入 Evidence 或 verifier prompt。
- [Completed] [Sense] [Fix] Enforced strict JSON encoding and decoding for robot-state artifacts, rejecting non-standard values such as `NaN` and `Infinity` before they can enter Evidence or verifier prompts.
- [完成] [model] [fix] 让配置入口与直接 HTTP handler 也执行 fail-closed 校验：拒绝 verification 配置未知字段，并禁止 request-size 参数被静默钳位。
- [Completed] [Model] [Fix] Extended fail-closed validation to the configuration entry point and direct HTTP handler by rejecting unknown verification fields and refusing to silently clamp request-size settings.
- [完成] [eval] [fix] 修复 Evidence retention 删除边界：执行删除前重新加载并验证持久化 Bundle，只允许删除 RequestBuilder 已验真的精确 artifact 集合，拒绝构建后对象篡改和 owner 目录外路径。
- [Completed] [Eval] [Fix] Fixed the Evidence-retention deletion boundary by reloading and validating the persisted Bundle before deletion, allowing only the exact artifact set admitted by RequestBuilder and rejecting post-build mutation or paths outside the owner directory.
- [完成] [sense] [fix] 将已配置的 Evidence retention 接入 `AgentTaskCoordinator.finalize_task()` 的真实 verifier 调用链；保留语义 verdict，retention 失败只生成可审计的 bounded task error/event。
- [Completed] [Sense] [Fix] Wired the configured Evidence-retention policy into the real verifier path in `AgentTaskCoordinator.finalize_task()` while preserving the semantic verdict and recording bounded task errors/events when retention fails.

### 预期影响文件 / Expected Files
- `PhyAgentOS/state_io/adapters.py`
- `PhyAgentOS/state_io/__init__.py`
- `PhyAgentOS/forge/task.py`
- `PhyAgentOS/forge/evidence.py`
- `PhyAgentOS/forge/environment_projection.py`
- `PhyAgentOS/agent/context.py`
- `PhyAgentOS/utils/helpers.py`
- `PhyAgentOS/verification/service.py`
- `PhyAgentOS/config/schema.py`
- `PhyAgentOS/agent/experience/evolution.py`
- `tests/` 中的相关真实边界验收 / related real-boundary acceptance tests under `tests/`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md` [新增 / added]
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`
- `docs/forge/STATE_FILE_ADAPTER_FEATURE_CARD.md`
- `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md`
- `CHANGELOG.md`

### 实际结果 / Actual Results

- [完成] [policy] [fix] 完成 AgentTask origin approval、SQLite 双列迁移/唯一 dedup、不可变 origin、严格 TARGETS/SESSIONS schema 与编译异常分类；保持 `motion_authorized=false`。
- [Completed] [Policy] [Fix] Implemented AgentTask origin approval, re-entrant SQLite migration for both origin columns, immutable origins, strict TARGETS/SESSIONS schemas, and specific compiler failure handling; preserved `motion_authorized=false`.
- [完成] [sense] [fix] 完成 v2 snapshot manifest、写入前 schema/immutability preflight、writer-owned evidence 目录、稳定 Bundle identity、AgentTask Bundle binding 和 retention-stable identity。
- [Completed] [Sense] [Fix] Implemented v2 snapshot manifests, pre-write schema/immutability preflight, writer-owned evidence directories, stable Bundle identity, AgentTask Bundle binding, and retention-stable identity.
- [完成] [env] [fix] state data 递归拒绝控制字符；ENVIRONMENT prompt 仅保留严格 projection identity，失败只返回稳定 code；旧 loader fail-closed。
- [Completed] [Env] [Fix] Recursively rejected control characters in state data; limited ENVIRONMENT prompt content to strict projection identity with a stable failure code; made the legacy loader fail closed.
- [完成] [model] [fix] 完成 parent/child provider/service schema、URL/header/timeout/request-size 校验、HTTP 413/415/500/504 稳定错误和 invalid-response 脱敏。
- [Completed] [Model] [Fix] Added shared parent/child provider/service schemas, URL/header/timeout/request-size validation, stable HTTP 413/415/500/504 errors, and invalid-response redaction.
- [完成] [docs] [refactor] 移除未接入生产 owner 的 SKILLRUNTIME/LESSONS generic renderer 公共导出，明确真实 Skill lessons 仅由 Experience ledger projection 生成。
- [Completed] [Docs] [Refactor] Removed public exports for generic SKILLRUNTIME/LESSONS renderers without production owners and documented that real Skill lessons are generated only from the Experience ledger projection.
- [完成] [policy] [fix] 最终复审后让 `AgentTaskStore.update()` 在提交前重新验证完整聚合，拒绝非法 task mutation；retention 只在终态消费重新验证的持久化 Bundle 和已验真 artifact 集合。
- [Completed] [Policy] [Fix] After final review, made `AgentTaskStore.update()` revalidate the full aggregate before commit and reject invalid task mutations; retention now operates only on terminal tasks after revalidating the persisted Bundle and admitted artifact set.
- [完成] [sense] [fix] robot-state 与 verifier structured JSON 均拒绝 `NaN`/`Infinity` 等非标准常量；provider endpoint/header 和直接 handler request-size 校验进一步 fail-closed。
- [Completed] [Sense] [Fix] Rejected non-standard `NaN`/`Infinity` constants in robot-state and verifier structured JSON; tightened provider endpoint/header and direct-handler request-size validation to fail closed.
- [完成] [sense] [fix] `AgentTaskCoordinator.finalize_task()` 仅在终态调用 Evidence retention，recovery 中间态保留证据；retention 失败只写入 bounded task error/event，不改写语义 verdict。
- [Completed] [Sense] [Fix] Wired `AgentTaskCoordinator.finalize_task()` to call Evidence retention only for terminal outcomes, preserving evidence during recovery; retention failures create bounded task errors/events without rewriting the semantic verdict.

### 文件变更详情 / File Change Details

- [修改 / Modified] `PhyAgentOS/forge/task.py:L45-L56,L190-L244,L286-L361,L393-L445,L1193-L1240`：origin approval binding、双列 migration/backfill/index、origin immutability、完整 aggregate revalidation、terminal retention wiring。
- [修改 / Modified] `PhyAgentOS/state_io/adapters.py:L275-L322,L390-L405,L429-L510,L548-L632`：strict TARGETS/SESSIONS validation and bounded promotion。
- [修改 / Modified] `PhyAgentOS/state_io/protocol.py:L80-L95`、`PhyAgentOS/agent/context.py:L187-L220`：control-character rejection and bounded ENVIRONMENT prompt projection。
- [修改 / Modified] `PhyAgentOS/forge/evidence.py:L31-L115,L118-L152,L165-L244,L301-L350,L570-L583`：writer-owned manifest、pre-write immutability、strict robot-state JSON。
- [修改 / Modified] `PhyAgentOS/verification/contracts.py:L174-L207`、`PhyAgentOS/verification/request_builder.py:L27-L32,L198-L227,L253-L318`：stable identity、same-bundle artifact boundary、strict structured JSON and unique paths。
- [修改 / Modified] `PhyAgentOS/verification/service.py:L56-L206,L345-L418`、`PhyAgentOS/config/schema.py:L341-L361`：shared provider/service schema and HTTP failure contract。
- [新增 / Added] `tests/test_state_file_authority_boundaries.py:L1-L476`：real Store/writer/request/context/retention authority-boundary review coverage。
- [修改 / Modified] `examples/forge-skills/pick-place-workflow/tests/test_task_verification_recovery.py:L25-L125`、`test_task_verification_context.py:L60-L268`：验证终态 retention wiring 与 recovery 中间态保留证据。
- [修改 / Modified] related adapter, evidence, environment, verifier and provider test files：更新真实边界断言和 failure cases。
- [新增 / Added] `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L1-L176`：第一轮发现、修复记录、三轮五维 review 与延后项。

### 关键 Diff / Key Diff

```text
Before: origin migration was incomplete; malformed evidence/provider failures could cross owner boundaries; generic SKILLRUNTIME/LESSONS renderers looked production-ready.
After:  both origin columns migrate idempotently and remain immutable; evidence and provider requests fail closed at writer/consumer boundaries; only owned projections are represented as implemented.
```

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -p pytest_asyncio.plugin -q tests` → `105 passed`。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-skills/pick-place-workflow/src python -m pytest -p pytest_asyncio.plugin -q examples/forge-skills/pick-place-workflow/tests` → `241 passed`。
- `python -m ruff check PhyAgentOS tests/...`、`python -m compileall -q PhyAgentOS tests`、`git diff --check` → 通过。
- Unguarded `python -m pytest -q tests` is blocked before collection by the system ROS `launch_testing` plugin missing `lark`; isolated commands explicitly load only `pytest_asyncio.plugin`. No production Verification Service subprocess, external model, Gateway, Watchdog, Action, or hardware motion was used.
- The provider-spec production subprocess gate, real-model semantic quality gate, and pick-place closure remain intentionally pending.

### Git 提交 / Git Commit

- Commit: `e4cdac5` (state-file authority-boundary fixes and third-round review)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)

## v3.5.1 (2026-09-03 21:32) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [fix] 补全 `AgentTaskStore.update()` 的聚合身份不可变约束，禁止 mutation 改写 `task_id` 后造成 SQLite 主键与 `record_json` 身份分裂。
- [Planned] [Policy] [Fix] Complete aggregate-identity immutability in `AgentTaskStore.update()` so mutations cannot rewrite `task_id` and split the SQLite primary key from `record_json` identity.
- [计划] [policy] [fix] 让 `AgentTaskStore.create()` 与 `update()` 共用提交前完整 schema 校验，拒绝已构造后被非法修改的 Pydantic record 污染权威库。
- [Planned] [Policy] [Fix] Make `AgentTaskStore.create()` and `update()` share full pre-commit schema validation so a Pydantic record mutated after construction cannot corrupt the authoritative store.
- [计划] [env] [fix] 让 JSON/YAML 状态文件协议拒绝重复 mapping key，避免后一个值静默覆盖已审核字段。
- [Planned] [Env] [Fix] Reject duplicate mapping keys in JSON/YAML state files so later values cannot silently overwrite reviewed fields.
- [计划] [model] [fix] 让 Verification Service HTTP 请求使用严格 JSON 解码，拒绝 `NaN`/`Infinity` 等非标准常量。
- [Planned] [Model] [Fix] Use strict JSON decoding at the Verification Service HTTP boundary and reject non-standard constants such as `NaN` and `Infinity`.
- [计划] [model] [fix] 收紧 `VerificationServiceProcess` 父进程构造器的 host、secret、port、timeout 和 request-size 类型，阻止隐式类型转换绕过配置边界。
- [Planned] [Model] [Fix] Tighten `VerificationServiceProcess` parent-constructor types for host, secret, port, timeout, and request size so implicit coercion cannot bypass configuration boundaries.
- [计划] [policy] [fix] 将 `AgentTaskRecord.created_at` 纳入 Store-owned immutable identity，防止可变回调篡改任务 provenance。
- [Planned] [Policy] [Fix] Include `AgentTaskRecord.created_at` in the Store-owned immutable identity so mutable callbacks cannot rewrite task provenance.
- [计划] [policy] [fix] 对 ToolExecutionRecord 的 arguments/response/error 与 task event payload 施加 finite JSON 校验，阻止非标准数值污染 SQLite 事实。
- [Planned] [Policy] [Fix] Enforce finite JSON validation for ToolExecutionRecord arguments/response/error and task event payloads so non-standard numbers cannot corrupt SQLite facts.
- [计划] [eval] [test] 增加三个真实生产边界的回归测试，完成第三轮五维代码审查并重跑仓库与 pick-place 全量验收；不启动真实 provider、Gateway、Action 或硬件。
- [Planned] [Eval] [Test] Add regression tests through the three production boundaries, complete a third five-dimension code review, and rerun repository plus pick-place acceptance without starting a real provider, Gateway, Action, or hardware.

### 预期影响文件 / Expected Files

- `PhyAgentOS/forge/task.py`
- `PhyAgentOS/state_io/protocol.py`
- `PhyAgentOS/verification/service.py`
- `tests/test_state_file_authority_boundaries.py`
- `tests/test_state_file_adapter.py`
- `tests/test_verification_service_replay.py`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`
- `CHANGELOG.md`
- `changelog/2026-09_part2.md`

### 实际结果 / Actual Results

- [完成] [policy] [fix] `AgentTaskRecord` 现在校验 revision、active revision 和 execution lineage 关系；`AgentTaskStore.create()` 与 `update()` 在写入前都重建并验证完整聚合，并拒绝 `task_id`/`created_at`/origin identity mutation。
- [Completed] [Policy] [Fix] `AgentTaskRecord` now validates revision, active-revision, and execution-lineage relationships; `AgentTaskStore.create()` and `update()` both rebuild and validate the full aggregate before writing and reject `task_id`/`created_at`/origin-identity mutation.
- [完成] [env] [fix] PAOS JSON/YAML state block 使用唯一键 loader，重复 mapping key 在解析阶段 fail-closed。
- [Completed] [Env] [Fix] PAOS JSON/YAML state blocks now use unique-key loaders, failing closed on duplicate mapping keys during parsing.
- [完成] [model] [fix] Verification Service request/response JSON 使用严格常量与重复键校验；父进程构造器拒绝隐式 host/secret/port/timeout/request-size 类型转换。
- [Completed] [Model] [Fix] Verification Service request/response JSON now rejects non-standard constants and duplicate keys; the parent constructor rejects implicit host/secret/port/timeout/request-size coercion.
- [完成] [policy] [fix] `ToolExecutionRecord` 的 arguments/response/error 与 task event payload 施加 finite JSON 校验，阻止非标准数值污染 SQLite 事实。
- [Completed] [Policy] [Fix] Enforced finite JSON validation for ToolExecutionRecord arguments/response/error and task event payloads to prevent non-standard numbers from corrupting SQLite facts.
- [完成] [eval] [test] 通过真实 Store、state protocol 和 HTTP handler 边界回归测试；仓库 `123 passed`，pick-place `241 passed`，Ruff、compileall、diff check 通过。
- [Completed] [Eval] [Test] Validated through real Store, state-protocol, and HTTP-handler boundary regressions; repository `123 passed`, pick-place `241 passed`, with Ruff, compileall, and diff check passing.

### 文件变更详情 / File Change Details

- [修改 / Modified] `PhyAgentOS/forge/task.py:L83-L125,L182-L262,L381-L411,L417-L463,L571-L586`：finite execution payload、聚合关系 schema、create/update pre-commit validation、task identity immutability。
- [修改 / Modified] `PhyAgentOS/state_io/protocol.py:L30-L62,L137-L151`：JSON/YAML duplicate-key rejection.
- [修改 / Modified] `PhyAgentOS/verification/service.py:L30-L55,L188-L224,L322-L343,L390-L425`：strict JSON decoding and strict parent constructor types.
- [新增 / Added] `tests/test_state_file_authority_boundaries.py:L206-L331`：create/identity/aggregate relation and finite payload rejection.
- [新增 / Added] `tests/test_state_file_adapter.py:L42-L75`、`tests/test_verification_service_replay.py:L71-L120`、`tests/test_verification_service_config.py:L165-L189`：duplicate-key, strict JSON, and constructor-type regressions.
- [修改 / Modified] `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L117-L176`：第三轮五维 review findings, fixes, validation, and remaining gates.

### 关键 Diff / Key Diff

```text
Before: mutable or duplicate-key inputs could cross Store/protocol/HTTP authority boundaries.
After:  aggregate identities and relationships are revalidated before commit, duplicate keys and
        non-standard JSON fail closed, and parent service configuration accepts no implicit types.
```

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -p pytest_asyncio.plugin -q tests` → `123 passed`。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-skills/pick-place-workflow/src python -m pytest -p pytest_asyncio.plugin -q examples/forge-skills/pick-place-workflow/tests` → `241 passed`。
- `python -m ruff check PhyAgentOS tests/...`、`python -m compileall -q PhyAgentOS tests`、`git diff --check` → 通过。
- 未启动生产 Verification Service 子进程、真实 provider、外部模型、Gateway、Watchdog、Action 或硬件；provider-spec 子进程门禁、真实模型语义质量和抓取放置闭环仍后置。
- No production Verification Service subprocess, real provider, external model, Gateway, Watchdog, Action, or hardware was started; provider-spec subprocess, real-model semantic quality, and pick-place closure remain deferred.

### Git 提交 / Git Commit

- Commit: `e4cdac5` (third-round Store/protocol/Verification boundary fixes)
- Branch: `feature/long-horizon-workflow`

## v3.5.2 (2026-09-03 21:53) - codex

### 预期修改 / Planned Changes

- [计划] [chore] [docs] 回写 v3.5.0/v3.5.1 的真实 Git commit hash，完成提交日志闭环并验证分支推送状态。
- [Planned] [Chore] [Docs] Write the real v3.5.0/v3.5.1 Git commit hashes back into the changelog, complete the commit-log loop, and verify branch push status.

### 预期影响文件 / Expected Files

- `CHANGELOG.md`
- `changelog/2026-09_part2.md`

### 实际结果 / Actual Results

- [完成] [chore] [docs] 已将首个实现提交记录为 `e4cdac5`，并保持当前分支为 `feature/long-horizon-workflow`。
- [Completed] [Chore] [Docs] Recorded the implementation commit as `e4cdac5` and kept the current branch as `feature/long-horizon-workflow`.

### 文件变更详情 / File Change Details

- [修改 / Modified] `CHANGELOG.md`、`changelog/2026-09_part2.md`：补全 v3.5.0/v3.5.1 commit hash 与本次维护记录。

### 验证 / Validation

- `git status --short --branch`、`git log -1 --oneline` 已核对提交 hash 与当前分支。

### Git 提交 / Git Commit

- Commit: `ad10c8f` (log maintenance)
- Branch: `feature/long-horizon-workflow`

## v3.6.0 (2026-09-03 22:20) - codex

### 预期修改 / Planned Changes

- [完成] [eval] [feat] 增加真实 `VerificationServiceProcess` provider-spec 子进程门禁：由父进程启动正式子进程，使用外部 OpenAI-compatible HTTP provider stub 验证配置传递、healthz readiness、鉴权请求、结构化 verdict、provider 失败、超时和进程清理；不连接外部模型、Gateway、Watchdog、Action 或硬件。
- [Completed] [Eval] [Feat] Add a production `VerificationServiceProcess` provider-spec subprocess gate: start the formal child process from the parent, use an external OpenAI-compatible HTTP provider stub to verify config transfer, healthz readiness, authenticated requests, structured verdicts, provider failure, timeout, and process cleanup; do not connect external models, Gateway, Watchdog, Actions, or hardware.

### 预期影响文件 / Expected Files

- `tests/test_verification_service_process.py`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`
- `CHANGELOG.md`
- `changelog/2026-09_part2.md`

### 实际结果 / Actual Results

- [完成] [eval] [feat] 已完成真实 `VerificationServiceProcess` provider-spec 子进程门禁：父进程启动正式子进程，独立 OpenAI-compatible HTTP stub 验证配置传递、私有 readiness、鉴权请求、结构化 verdict、provider 失败、超时和 stop 清理；未连接外部模型、Gateway、Watchdog、Action 或硬件。
- [Completed] [Eval] [Feat] Completed the real `VerificationServiceProcess` provider-spec subprocess gate: the parent starts the formal child process, and an independent OpenAI-compatible HTTP stub verifies config transfer, private readiness, authenticated requests, structured verdicts, provider failure, timeout, and stop cleanup; no external model, Gateway, Watchdog, Action, or hardware was connected.

### 文件变更详情 / File Change Details

- [修改 / Modified] `PhyAgentOS/verification/service.py:L28,L282-L314,L405-L418`：增加服务标识和带 token 的 `/readyz` 私有 readiness，严格校验 JSON/服务身份，保留 `/healthz` liveness。
- [新增 / Added] `tests/test_verification_service_process.py:L1-L230`：覆盖正式子进程启动、provider-spec 参数传递、真实 HTTP provider stub、失败/超时映射、readiness 鉴权和进程清理。
- [修改 / Modified] `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L161-L212`：记录 provider 子进程门禁完成、代码审查结论、验证结果和剩余门禁。

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -p pytest_asyncio.plugin -q tests` → `127 passed`。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-skills/pick-place-workflow/src python -m pytest -p pytest_asyncio.plugin -q examples/forge-skills/pick-place-workflow/tests` → `241 passed`。
- `python -m ruff check PhyAgentOS/verification/service.py tests/test_verification_service_process.py`、`python -m compileall -q PhyAgentOS/verification/service.py tests/test_verification_service_process.py`、`git diff --check` → 通过。

### Git 提交 / Git Commit

- Commit: `cfef665`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 22:31 (Asia/Shanghai)

## v3.6.1 (2026-09-03 22:32) - codex

### 变更摘要 / Change Summary

- [完成] [chore] [docs] 回写 v3.6.0 provider-spec 子进程门禁的 Git commit hash，完成实现日志闭环。
- [Completed] [Chore] [Docs] Recorded the v3.6.0 provider-spec subprocess gate Git commit hash to close the implementation log loop.

### 文件变更详情 / File Change Details

- [修改 / Modified] `CHANGELOG.md`、`changelog/2026-09_part2.md`：补全 v3.6.0 commit hash 与日志维护记录。

### Git 提交 / Git Commit

- Commit: `cfef665` (implementation commit being recorded)
- Branch: `feature/long-horizon-workflow`
## v3.10.2 (2026-09-04 15:00) - codex

### 变更摘要 / Change Summary

- [完成] [comm] [feat] 完成 EnvironmentAdapter/provider-neutral `scene.observe` 核心 seam：显式 ToolSpec、注入 ObservationSource、严格 freshness/frame/calibration/artifact 校验、provider failure 与 sensor unavailable 的 fail-closed 投影；保持 no-motion，不连接真实机器人、Dora 或硬件。
- [Completed] [Comm] [Feat] Completed the EnvironmentAdapter/provider-neutral `scene.observe` core seam: explicit ToolSpec, injected ObservationSource, strict freshness/frame/calibration/artifact validation, and fail-closed projection for provider failures and unavailable sensors; kept no-motion with no real robot, Dora, or hardware connection.
- [完成] [eval] [review] 按架构集成、失败路径、权威边界、配置、可维护性五个维度完成代码审查，无 Blocker/Major；明确完整 RoboTwin runtime、真实 Gateway/Dora、geometry consumer 与 Action executor 仍后置。
- [Completed] [Eval] [Review] Completed the five-dimension code review across architecture integration, failure paths, authority boundaries, configuration, and maintainability with no Blocker/Major; full RoboTwin runtime, real Gateway/Dora, geometry consumers, and Action executor remain deferred.

### 预期影响文件 / Expected Files

- `PhyAgentOS/forge/capability_runtime/observation.py`
- `PhyAgentOS/forge/capability_runtime/__init__.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/adapter.py`
- `tests/test_environment_adapter_observation.py`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`
- `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md`

### 文件变更详情 / File Change Details

- [新增 / Added] `PhyAgentOS/forge/capability_runtime/observation.py:L1-L260`：新增 provider-neutral `OBSERVATION_TOOL_SPEC`、ObservationEndpoint、严格观测投影与统一错误时钟。
- [修改 / Modified] `PhyAgentOS/forge/capability_runtime/__init__.py:L14-L18,L57-L60`：导出 observation ToolSpec 与 endpoint。
- [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/adapter.py:L124-L139`：保持 AdapterConfigurationError/AdapterSensorError fail-closed，不泄漏仿真真值。
- [新增 / Added] `tests/test_environment_adapter_observation.py:L1-L106`：覆盖成功、stale、provider failure、sensor unavailable、绑定/artifact/calibration/timestamp/输入失败与显式 runtime registration。
- [修改 / Modified] `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`、`docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md`：记录阶段状态、五维审查和后置边界。

### 关键 Diff / Key Diff

```text
修改前 / Before: observation endpoint 只有孤立实现；provider exception 直接抛出，错误时间戳使用系统时钟，ToolSpec 未在核心 owner 中导出。
修改后 / After: 核心 owner 提供显式 provider-neutral ToolSpec；provider/传感器失败统一为稳定 unavailable/invalid 结果，错误与 freshness 使用注入时钟，仍不隐式注册或授权动作。
```

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -p pytest_asyncio.plugin -q tests/test_environment_adapter_observation.py` → `10 passed`。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -p pytest_asyncio.plugin -q tests` → `161 passed`。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-adapters/robotwin20/src python -m pytest -p pytest_asyncio.plugin -q examples/forge-adapters/robotwin20/tests/test_adapter.py examples/forge-adapters/robotwin20/tests/test_preflight.py examples/forge-adapters/robotwin20/tests/test_process_worker.py` → `16 passed`。
- Ruff、compileall、`git diff --check` 通过；完整 RoboTwin 测试受当前环境缺少 `numpy` 与 `pick_place_workflow` 路径影响，未宣称通过。

### Git 提交 / Git Commit

- Commit: `c46a35a`
- Branch: `feature/long-horizon-workflow`

## v3.10.3 (2026-09-04 15:10) - codex

### 实际结果 / Actual Results

- [完成] [chore] [docs] 已回写 v3.10.2 EnvironmentAdapter observation seam 的实现提交 `c46a35a`，实现内容与验证结果保持不变。
- [Completed] [Chore] [Docs] Recorded implementation commit `c46a35a` for the v3.10.2 EnvironmentAdapter observation seam; implementation and validation results are unchanged.

### 文件变更详情 / File Change Details

- [修改 / Modified] `CHANGELOG.md`、`changelog/2026-09_part2.md`：补全 v3.10.2 hash 与当前版本维护记录。

### Git 提交 / Git Commit

- Commit: 待提交 / pending
- Branch: `feature/long-horizon-workflow`

## v3.10.4 (2026-09-04 15:20) - codex

### 实际结果 / Actual Results

- [完成] [comm] [fix] RoboTwin source 现在仅将传感器失败转换为 `None`；adapter 配置错误继续显式抛出并由核心 endpoint 统一 fail-closed，避免直接 source 使用时静默掩盖部署错误。
- [Completed] [Comm] [Fix] RoboTwin source now converts only sensor failures to `None`; adapter configuration errors remain explicit and the core endpoint still fails closed, preventing silent masking of deployment faults for direct source consumers.

### 文件变更详情 / File Change Details

- [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/adapter.py:L129-L132`：收窄异常捕获范围。

### 验证 / Validation

- Observation seam `10 passed`；RoboTwin adapter subset `7 passed`；Ruff 与 `git diff --check` 通过。

### Git 提交 / Git Commit

- Commit: `69c00d7`
- Branch: `feature/long-horizon-workflow`
## v3.10.5 (2026-09-04 15:25) - codex

### 预期修改 / Planned Changes

- [计划] [chore] [docs] 回写 v3.10.4 adapter failure-path 修复提交 hash，完成日志闭环，不修改实现行为。
- [Planned] [Chore] [Docs] Record the v3.10.4 adapter failure-path fix commit hash to close the log loop; do not change implementation behavior.

### 实际结果 / Actual Results

- [完成] [chore] [docs] 已记录 v3.10.4 修复提交 `69c00d7`。
- [Completed] [Chore] [Docs] Recorded v3.10.4 fix commit `69c00d7`.

### Git 提交 / Git Commit

- Commit: `5324225`
- Branch: `feature/long-horizon-workflow`

## v3.10.6 (2026-09-04 15:30) - codex

### 预期修改 / Planned Changes

- [计划] [chore] [docs] 回写 v3.10.5 日志维护提交 hash，完成全部日志闭环，不修改实现行为。
- [Planned] [Chore] [Docs] Record the v3.10.5 log-maintenance commit hash to close the complete changelog loop; do not change implementation behavior.

### 实际结果 / Actual Results

- [完成] [chore] [docs] 已回写 v3.10.5 提交 `5324225`。
- [Completed] [Chore] [Docs] Recorded v3.10.5 commit `5324225`.

### Git 提交 / Git Commit

- Commit: 待提交 / pending
- Branch: `feature/long-horizon-workflow`
## v3.10.7 (2026-09-04 16:00) - codex

### 预期修改 / Planned Changes

- [计划] [comm] [fix] 强化 `scene.understand` 对 observation/artifact 的正式绑定：拒绝无 provenance、跨 frame spatial envelope、provider 修改原始请求及重复错误字段；保持 Query/no-motion。
- [Planned] [Comm] [Fix] Strengthen `scene.understand` binding to formal observations/artifacts: reject provenance-free claims, cross-frame spatial envelopes, provider mutation of the original request, and duplicate error fields; keep Query/no-motion.
- [计划] [eval] [review] 完成后按架构集成、失败路径、权威边界、配置、可维护性五个维度复审，并运行核心、pick-place 与 adapter 回归。
- [Planned] [Eval] [Review] After implementation, repeat the five-dimension review across architecture integration, failure paths, authority boundaries, configuration, and maintainability, with core, pick-place, and adapter regressions.
## v3.10.8 (2026-09-04 16:20) - codex

### 变更摘要 / Change Summary

- [完成] [comm] [fix] 加固 `scene.understand` 对 `scene.observe` identity 与 artifact lineage 的消费边界：强制 observation_ref/revision/frame 绑定，拒绝重复 artifact、空/重复 provenance、跨 frame envelope，并隔离 provider 请求变更。
- [Completed] [Comm] [Fix] Hardened `scene.understand` consumption of `scene.observe` identity and artifact lineage: enforce observation_ref/revision/frame binding, reject duplicate artifacts, empty/duplicate provenance, cross-frame envelopes, and isolate provider request mutation.
- [完成] [eval] [review] 按五个维度复审通过，无 Blocker/Major；保持 Query/no-motion，不连接真实 Gateway、Dora、Action 或硬件。
- [Completed] [Eval] [Review] Passed the five-dimension review with no Blocker/Major; kept Query/no-motion with no real Gateway, Dora, Action, or hardware connection.

### 文件变更详情 / File Change Details

- [修改 / Modified] `PhyAgentOS/forge/capability_runtime/understanding.py:L9-L13,L255-L286,L319-L325,L470-L495,L530-L535`：补充 identity、artifact、provenance、frame 绑定与 deep-copy request。
- [新增 / Added] `examples/forge-skills/pick-place-workflow/tests/test_scene_understand.py:L128-L175`：新增绑定、重复 provenance、跨 frame 和 provider mutation 回归。
- [修改 / Modified] `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`：记录实现与五维审查结果。

### 关键 Diff / Key Diff

```text
修改前 / Before: understanding request identity and provenance had permissive gaps; provider could mutate the request object.
修改后 / After: observation identity, artifact refs, provenance, and frame are deterministic and provider input is isolated by deep copy.
```

### 验证 / Validation

- `test_scene_understand.py` → `21 passed`。
- 根仓库 → `161 passed`；pick-place 全量 → `250 passed`。
- RoboTwin scene-understand/single-view provider → `7 passed, 1 skipped`（显式加入两个 source root）。
- Ruff、compileall、`git diff --check` 通过。

### Git 提交 / Git Commit

- Commit: 待提交 / pending
- Branch: `feature/long-horizon-workflow`
## v3.10.9 (2026-09-04 16:30) - codex

### 预期修改 / Planned Changes

- [计划] [chore] [docs] 回写 v3.10.8 scene.understand observation binding 修复提交 hash，完成日志闭环，不修改实现行为。
- [Planned] [Chore] [Docs] Record the v3.10.8 scene.understand observation-binding fix commit hash to close the log loop; do not change implementation behavior.

### 实际结果 / Actual Results

- [完成] [chore] [docs] 已记录 v3.10.8 实现提交 `2ba3a21`。
- [Completed] [Chore] [Docs] Recorded v3.10.8 implementation commit `2ba3a21`.

### Git 提交 / Git Commit

- Commit: 待提交 / pending
- Branch: `feature/long-horizon-workflow`
## v3.11.0 (2026-09-04 17:00) - codex

### 变更摘要 / Change Summary

- [完成] [comm] [fix] 加固 `grasp.propose` 对正式 geometry artifact 的消费：严格绑定 observation/revision/frame/calibration/entity，拒绝重复/空 artifact provenance，限制 candidate provenance 到输入 observation evidence，并隔离 provider 请求变更；保持 Query/no-motion。
- [Completed] [Comm] [Fix] Hardened `grasp.propose` consumption of formal geometry artifacts: strictly bind observation/revision/frame/calibration/entity, reject duplicate/empty artifact provenance, constrain candidate provenance to input observation evidence, and isolate provider request mutation; kept Query/no-motion.
- [完成] [eval] [review] 按架构集成、失败路径、权威边界、配置、可维护性五维复审通过，无 Blocker/Major；未连接真实 Action、Dora 或硬件。
- [Completed] [Eval] [Review] Passed the five-dimension review with no Blocker/Major; no real Action, Dora, or hardware connected.

### 文件变更详情 / File Change Details

- [修改 / Modified] `PhyAgentOS/forge/capability_runtime/grasp_proposal.py:L8-L10,L329-L363,L369-L408,L426-L500,L533-L558,L613-L677`：补充 provenance/identity 校验、provider request deep copy 和兼容 validator 参数。
- [新增 / Added] `examples/forge-skills/pick-place-workflow/tests/test_grasp_propose.py:L354-L380`：新增 observation binding、candidate provenance 和 provider mutation 回归。
- [修改 / Modified] `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`：记录本阶段实现、五维审查和后置边界。

### 关键 Diff / Key Diff

```text
修改前 / Before: grasp target/candidate provenance validation allowed unbound or duplicate references, and provider could mutate request state.
修改后 / After: geometry/candidate provenance is bounded to the current observation evidence, identity/frame is strict, and provider input is isolated.
```

### 验证 / Validation

- `test_grasp_propose.py` → `61 passed`；根仓库 → `161 passed`；pick-place 全量 → `253 passed`。
- adapter GraspGen 专项因当前 PAOS 环境缺少可选 `numpy` 无法收集，未宣称 live checkpoint 验收。
- Ruff、compileall、`git diff --check` 通过。

### Git 提交 / Git Commit

- Commit: 待提交 / pending
- Branch: `feature/long-horizon-workflow`
## v3.11.1 (2026-09-04 17:10) - codex

### 预期修改 / Planned Changes

- [计划] [chore] [docs] 回写 v3.11.0 grasp.propose geometry binding 提交 hash，完成日志闭环，不修改实现行为。
- [Planned] [Chore] [Docs] Record the v3.11.0 grasp.propose geometry-binding commit hash to close the log loop; do not change implementation behavior.

### 实际结果 / Actual Results

- [完成] [chore] [docs] 已记录 v3.11.0 实现提交 `88267b4`。
- [Completed] [Chore] [Docs] Recorded v3.11.0 implementation commit `88267b4`.

### Git 提交 / Git Commit

- Commit: 待提交 / pending
- Branch: `feature/long-horizon-workflow`
## v4.0.0 (2026-09-04 18:00) - codex

### 预期修改 / Planned Changes

- [完成] [comm] [fix] 加固 `manipulation.prepare` 对正式 candidate/observation contract 的消费：严格绑定 observation 与 candidate-set identity、candidate provenance 和 provider request；保持 Query/no-motion，不复制 Hephaestus executor。
- [Planned] [Comm] [Fix] Harden `manipulation.prepare` consumption of formal candidate/observation contracts: strictly bind observation and candidate-set identity, candidate provenance, and provider requests; keep Query/no-motion without copying the Hephaestus executor.
- [完成] [eval] [review] 完成功能后按架构集成、失败路径、权威边界、配置、可维护性五维复审并回归全量测试。
- [Planned] [Eval] [Review] Repeat the five-dimension review and full regression after implementation.

### 实际结果 / Actual Results

- [完成] [comm] [fix] `manipulation.prepare` 严格绑定 observation 与 candidate-set 的 revision/frame identity，拒绝重复 prepared candidate，并对 provider 请求使用深拷贝隔离；保持所有输出 `motion_authorized=false`。
- [Completed] [Comm] [Fix] `manipulation.prepare` now strictly binds observation and candidate-set revision/frame identity, rejects duplicate prepared candidates, and isolates provider requests with a deep copy; all outputs keep `motion_authorized=false`.
- [完成] [eval] [review] 按五个维度复审无 Blocker/Major：复用核心 capability runtime 与 candidate validator；provider/stale/invalid/binding/evidence 失败均 fail-closed；不生成 Action invocation、不接入 Hephaestus executor、Dora 或硬件。
- [Completed] [Eval] [Review] Five-dimension review found no Blocker/Major: the implementation reuses the core capability runtime and candidate validator; provider/stale/invalid/binding/evidence failures fail closed; no Action invocation, Hephaestus executor, Dora, or hardware is connected.

### 文件变更详情 / File Change Details

- [修改 / Modified] `PhyAgentOS/forge/capability_runtime/manipulation_prepare.py:L8-L11,L245-L270,L300-L317,L365-L405`：增加严格 observation identity、prepared candidate 去重及 provider request 深拷贝隔离。
- [新增 / Added] `examples/forge-skills/pick-place-workflow/tests/test_manipulation_prepare.py:L252-L271,L461-L462`：覆盖 revision/frame 漂移、provider mutation 与重复 candidate 失败路径。
- [修改 / Modified] `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`：记录本阶段实现、五维验收、执行顺序和 Hephaestus clean-room 参考边界。

### 关键 Diff / Key Diff

```text
Before: preparation accepted loosely bound observation/candidate identities and exposed the provider's mutable request object.
After:  observation_ref and candidate_set_ref are deterministic revision/frame bindings, prepared refs are unique, and provider input is isolated.
```

### 验证 / Validation

- `test_manipulation_prepare.py` → `60 passed`；根仓库 → `161 passed`；pick-place 全量 → `256 passed`。
- Ruff、compileall、`git diff --check` → 通过。
- Hephaestus 代码未作为运行时依赖接入；仅参考其 Query/no-motion、安全门禁、失败语义和证据边界，未复制 executor、receipt、state store 或 provider payload。

## v4.0.1 (2026-09-04 18:20) - codex

### 实际结果 / Actual Results

- [完成] [chore] [docs] 已回写 v4.0.0 实现提交 `385eb7a`；实现、测试和执行顺序均未改变。
- [Completed] [Chore] [Docs] Recorded v4.0.0 implementation commit `385eb7a`; implementation, tests, and execution order are unchanged.

### Git 提交 / Git Commit

- Commit: `385eb7a`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-04 18:20 (Asia/Shanghai)

## v4.1.0 (2026-09-04 19:00) - codex

### 预期修改 / Planned Changes

- [完成] [comm] [feat] 在独立 RoboTwin20 adapter 中实现 provider-neutral `ReadinessEvaluator` conformance，并在 PAOS preparation endpoint 增加 mapping normalization；保持 IK/碰撞/workspace 结果为证据、默认 no-motion，不连接真实执行器。
- [Planned] [Comm] [Feat] Implement provider-neutral `ReadinessEvaluator` conformance in the independent RoboTwin20 adapter and add mapping normalization in the PAOS preparation endpoint; keep IK/collision/workspace results as evidence with no-motion by default and connect no real executor.
- [完成] [eval] [review] 覆盖 identity、candidate binding、provider failure、unknown/fail checks、evidence 和 mutation 隔离，并按五个维度复审。
- [Planned] [Eval] [Review] Cover identity, candidate binding, provider failure, unknown/fail checks, evidence, and mutation isolation, followed by the five-dimension review.

### 实际结果 / Actual Results

- [完成] [comm] [feat] 新增独立 `robotwin20_adapter.RoboTwinReadinessEvaluator`，验证公共 request identity/candidate binding，隔离 evaluator request，并将合法 readiness evidence 映射为 PAOS 可消费的 mapping；PAOS `normalize_snapshot` 保持严格 `provider_available` 字段要求。
- [Completed] [Comm] [Feat] Added the independent `robotwin20_adapter.RoboTwinReadinessEvaluator`, validating public request identity/candidate binding, isolating evaluator requests, and mapping valid readiness evidence to a PAOS-consumable mapping; PAOS `normalize_snapshot` retains the strict `provider_available` requirement.
- [完成] [eval] [review] 五维复审无 Blocker/Major；失败、unknown/fail checks、重复/越权 candidate、非法 evidence、provider-specific 字段和 identity 漂移均 fail-closed；没有接入 Hephaestus executor、真实 IK/碰撞引擎、Action、Gateway、Dora 或硬件。
- [Completed] [Eval] [Review] Five-dimension review found no Blocker/Major; failures, unknown/failing checks, duplicate/unbound candidates, invalid evidence, provider-specific fields, and identity drift all fail closed; no Hephaestus executor, real IK/collision engine, Action, Gateway, Dora, or hardware was connected.

### 文件变更详情 / File Change Details

- [新增 / Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/readiness.py:L1-L169`：独立 readiness adapter/provider boundary、identity/evidence validation 与 deep-copy isolation。
- [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py:L22-L25,L78-L82`、`PhyAgentOS/forge/capability_runtime/manipulation_prepare.py:L8,L28-L30,L354-L367,L473-L495`：导出 adapter contract 并接收严格 mapping snapshot normalization。
- [新增 / Added] `examples/forge-adapters/robotwin20/tests/test_readiness.py:L1-L122`：覆盖成功、PAOS 集成、失败路径、绑定漂移和 no-motion 边界。
- [修改 / Modified] `examples/forge-adapters/robotwin20/README.md:L191-L202`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L461-L485`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L444-L448`：记录 adapter conformance、五维验收、执行顺序与 Hephaestus 参考边界。

### 关键 Diff / Key Diff

```text
Before: manipulation.prepare had only a PAOS-side provider protocol; no independent adapter implementation consumed readiness evidence.
After:  RoboTwin20 exposes a clean-room evaluator seam with strict identity/evidence checks, while PAOS remains the projection and no-motion owner.
```

### 验证 / Validation

- adapter readiness → `14 passed`；dependency-free adapter subset → `30 passed`；PAOS preparation → `60 passed`；根仓库 → `161 passed`；pick-place → `256 passed`。
- Ruff、compileall、`git diff --check` → 通过。
- 完整 RoboTwin20 adapter 测试因当前 PAOS 环境缺少可选 `numpy`，收集阶段无法运行 GraspGen/数值感知测试；该依赖边界未被误报为 readiness conformance 失败。
- The full RoboTwin20 adapter suite cannot collect optional NumPy-backed GraspGen/numeric perception tests in the current PAOS environment; this dependency boundary is not reported as a readiness conformance failure.

## v4.1.1 (2026-09-04 19:20) - codex

### 实际结果 / Actual Results

- [完成] [chore] [docs] 已回写 v4.1.0 实现提交 `4b6ab2b`，完成日志闭环；实现、测试和执行顺序未改变。
- [Completed] [Chore] [Docs] Recorded v4.1.0 implementation commit `4b6ab2b` to close the changelog loop; implementation, tests, and execution order are unchanged.

### Git 提交 / Git Commit

- Commit: `4b6ab2b`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-04 19:20 (Asia/Shanghai)

## v4.1.2 (2026-09-04 19:30) - codex

### 实际结果 / Actual Results

- [完成] [chore] [docs] 修正日志索引：v4.1.0 的英文计划项已在实际结果中完成，v4.1.1 的日志维护提交为 `68bacaf`；未修改实现、测试或执行顺序。
- [Completed] [Chore] [Docs] Corrected the log index: v4.1.0 English plan items are completed in the actual result, and v4.1.1's maintenance commit is `68bacaf`; implementation, tests, and execution order are unchanged.

### Git 提交 / Git Commit

- Commit: `68bacaf`
- Branch: `feature/long-horizon-workflow`

## v4.2.0 (2026-09-04 20:00) - codex

### 预期修改 / Planned Changes

- [完成] [comm] [feat] 增加独立 readiness replay worker/profile，通过现有 JSONL process boundary 读取外部 immutable evidence fixture，并接入 `RoboTwinReadinessEvaluator`；明确 replay 不代表真实 IK/碰撞质量。
- [Completed] [Comm] [Feat] Add an independent readiness replay worker/profile that reads an external immutable evidence fixture through the existing JSONL process boundary and feeds `RoboTwinReadinessEvaluator`; explicitly keep replay separate from real IK/collision quality.
- [完成] [eval] [test] 覆盖 request identity、fixture digest/路径边界、worker identity、malformed/unknown evidence、shutdown/timeout 和 `motion_authorized=false`，完成五维代码审查。
- [Completed] [Eval] [Test] Covered request identity, fixture digest/path boundaries, worker identity, malformed/unknown evidence, shutdown/timeout, and `motion_authorized=false`, followed by the five-dimension review.
- [完成] [comm] [refactor] 暴露 readiness adapter 的显式 `release()` 生命周期接口，避免调用方依赖内部 evaluator 属性释放进程 worker。
- [Completed] [Comm] [Refactor] Expose an explicit `release()` lifecycle method on the readiness adapter so callers do not depend on an internal evaluator attribute to release the process worker.

### 实际结果 / Actual Results

- [完成] [comm] [feat] 新增 `readiness_replay_worker.py`、`ReadinessReplayClient` 和 `readiness-replay.yaml`，通过现有 JSONL process boundary 回放 hash-pinned、no-motion readiness evidence；未知 case、重复 case、digest/path/schema/identity 错误均拒绝。
- [Completed] [Comm] [Feat] Added `readiness_replay_worker.py`, `ReadinessReplayClient`, and `readiness-replay.yaml` to replay hash-pinned, no-motion readiness evidence through the existing JSONL process boundary; unknown/duplicate cases and digest/path/schema/identity errors are rejected.
- [完成] [comm] [refactor] `RoboTwinReadinessEvaluator.release()` 提供公开 teardown 端口，回放测试不再依赖内部 `evaluator` 属性释放 worker。
- [Completed] [Comm] [Refactor] `RoboTwinReadinessEvaluator.release()` exposes a public teardown port, and replay tests no longer depend on the internal `evaluator` attribute to release the worker.
- [完成] [eval] [review] 五维复审无 Blocker/Major；PAOS 仍拥有最终 projection 与 `motion_authorized=false`，回放不被表述为真实 IK、碰撞、轨迹或物理成功。
- [Completed] [Eval] [Review] Five-dimension review found no Blocker/Major; PAOS retains final projection and `motion_authorized=false`, and replay is not presented as real IK, collision, trajectory, or physical success.

### 文件变更详情 / File Change Details

- [新增 / Added] `examples/forge-adapters/robotwin20/runtime/readiness_replay_worker.py:L1-L147`：独立 fixture loader、case identity matching 和 JSONL no-motion worker。
- [新增 / Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/readiness_profile.py:L1-L131`：SHA-256/path/permission profile gate 与 worker client identity validation。
- [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/readiness.py:L118-L125`：增加公开 `release()` teardown，保持 process-backed evaluator 生命周期由 adapter owner 管理。
- [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py:L42-L52,L119-L131`：导出 replay profile；复用既有 `manipulation_prepare.py` 的 PAOS mapping normalization owner。
- [新增 / Added] `examples/forge-adapters/robotwin20/tests/test_readiness_replay.py:L1-L170`、`examples/forge-adapters/robotwin20/profiles/robotwin20/readiness-replay.yaml:L1-L15`：进程回放、digest、路径、identity、shutdown 和 no-motion conformance。
- [修改 / Modified] `examples/forge-adapters/robotwin20/README.md:L201-L214`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L486-L506`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L449-L454`：记录 replay 证据边界和下一步顺序。

### 验证 / Validation

- replay/readiness/process 专项 → `28 passed`；依赖隔离 adapter 子集 → `38 passed`；根仓库 → `161 passed`；pick-place → `256 passed`。
- Ruff、compileall、`git diff --check` → 通过。
- 完整 RoboTwin20 adapter 仍因当前 PAOS 环境缺少可选 `numpy`，且未为该命令注入 pick-place 源路径，无法收集数值感知/GraspGen 及集成测试；依赖隔离子集已单独通过。未连接真实 IK、碰撞、Action、Gateway、Dora 或硬件。

## v4.2.1 (2026-09-04 20:30) - codex

### 预期修改 / Planned Changes

- [完成] [chore] [docs] 回写 v4.2.0 readiness replay 实现提交哈希，并同步根 CHANGELOG；不修改实现、测试或执行顺序。
- [Completed] [Chore] [Docs] Recorded the v4.2.0 readiness replay implementation commit hash and synchronized the root CHANGELOG; implementation, tests, and execution order were unchanged.

### 实际结果 / Actual Results

- [完成] [chore] [docs] v4.2.0 实现提交为 `103db24`；根 CHANGELOG 已同步，未修改实现、测试或执行顺序。
- [Completed] [Chore] [Docs] v4.2.0 implementation commit recorded as `103db24`; root CHANGELOG synchronized with no implementation, test, or execution-order changes.

### 文件变更详情 / File Change Details

- [修改 / Modified] `CHANGELOG.md:L10-L30`、`changelog/2026-09_part2.md:L1823-L1835`：补全 v4.2.0 提交元数据和维护记录。

### 验证 / Validation

- `git diff --check`、日志 UTF-8 显示和当前分支状态核对通过。

## v4.3.3 (2026-09-04 22:00) - codex

### 预期修改 / Planned Changes

- [完成] [comm] [fix] 将 `calibration_ref` 纳入 readiness replay fixture/request/manifest 的完整 identity，修复跨校准证据可能被误绑定的边界。
- [Completed] [Comm] [Fix] Included `calibration_ref` in the complete readiness replay fixture/request/manifest identity to prevent cross-calibration evidence from being misbound.
- [完成] [eval] [test] 回归 readiness replay、adapter 子集、静态检查，并按五个维度复审。
- [Completed] [Eval] [Test] Re-ran readiness replay, the dependency-free adapter subset, static checks, and the five-dimension review.

### 实际结果 / Actual Results

- [完成] [comm] [fix] fixture case、request 和 evidence manifest 现在共同绑定 `calibration_ref`；校准漂移和跨 case evidence 均 fail-closed。
- [Completed] [Comm] [Fix] Fixture cases, requests, and evidence manifests now bind `calibration_ref` together; calibration drift and cross-case evidence fail closed.
- [完成] [eval] [review] 五维审查无 Blocker/Major；未连接真实 IK、碰撞、Action、Gateway、Dora 或硬件。
- [Completed] [Eval] [Review] Five-dimension review found no Blocker/Major; no real IK, collision, Action, Gateway, Dora, or hardware was connected.

### 文件变更详情 / File Change Details

- [修改 / Modified] `examples/forge-adapters/robotwin20/runtime/readiness_replay_worker.py:L52-L236`：将 calibration identity 纳入 fixture、request、manifest 和 evidence binding。
- [修改 / Modified] `examples/forge-adapters/robotwin20/tests/test_readiness_replay.py:L54-L229`：更新 fixture schema 并覆盖 calibration drift。

### 验证 / Validation

- readiness/replay/process 专项 → `34 passed`；依赖隔离 adapter 子集 → `44 passed`。
- Ruff、compileall、`git diff --check` → 通过。

### Git 提交 / Git Commit

- Commit: `20c6ad6`
- Branch: `feature/long-horizon-workflow`

## v4.4.0 (2026-09-04 22:45) - codex

### 预期修改 / Planned Changes

- [计划] [comm] [feat] 新增 adapter-local readiness replay artifact writer/loader，将独立 worker 的 no-motion 结果以 canonical JSON、稳定 digest 和 fixture/manifest identity 固化；不把该 artifact 提升为 PAOS Verifier EvidenceBundle。
- [Planned] [Comm] [Feat] Add an adapter-local readiness replay artifact writer/loader that persists independent worker no-motion results as canonical JSON with stable digest and fixture/manifest identity; do not promote this artifact to a PAOS Verifier EvidenceBundle.
- [计划] [eval] [test] 覆盖 artifact schema、digest、不可变写入、request/result binding、worker identity、路径权限和 malformed replay，完成五维代码审查。
- [Planned] [Eval] [Test] Cover artifact schema, digest, immutable writes, request/result binding, worker identity, path permissions, and malformed replay, followed by the five-dimension review.

### 实际结果 / Actual Results

- [完成] [comm] [feat] 新增 `readiness_replay.py` 的 canonical JSON、SHA-256 content ID、严格 schema、request/result candidate binding、timezone 和 no-motion 校验；新增不可变 atomic writer/loader，拒绝相异覆盖、篡改、symlink、宽松权限和 malformed replay。
- [Completed] [Comm] [Feat] Added canonical JSON, SHA-256 content IDs, strict schema, request/result candidate binding, timezone, and no-motion validation in `readiness_replay.py`; added immutable atomic writer/loader rejecting divergent overwrites, tampering, symlinks, permissive permissions, and malformed replay.
- [完成] [comm] [feat] `ReadinessReplayClient` 绑定 fixture/manifest digest 并提供 `record_replay`；仅允许 `provider_available=true` 的 worker projection 固化，`RoboTwinReadinessEvaluator` 暴露受限转发接口。
- [Completed] [Comm] [Feat] `ReadinessReplayClient` now binds fixture/manifest digests and exposes `record_replay`; only `provider_available=true` worker projections can be persisted, with a restricted forwarding method on `RoboTwinReadinessEvaluator`.
- [完成] [eval] [test] 新增 9 个 replay artifact conformance cases；readiness/replay/process `25 passed`，根仓库 `161 passed`，依赖隔离 adapter 子集 `16 passed`，pick-place `256 passed`；Ruff、compileall、`git diff --check` 全部通过。
- [Completed] [Eval] [Test] Added 9 replay-artifact conformance cases; readiness/replay/process `25 passed`, repository `161 passed`, dependency-free adapter subset `16 passed`, pick-place `256 passed`; Ruff, compileall, and `git diff --check` all passed.
- [完成] [eval] [review] 五维审查无 Blocker/Major：artifact 保持 adapter-local audit/replay 角色，不冒充 PAOS EvidenceBundle、Verifier 物理成功或 Action admission；真实/独立验证 worker 证据仍须人工审核后才可进入 Action/Gateway wiring。
- [Completed] [Eval] [Review] Five-dimension review found no Blocker/Major: the artifact remains adapter-local audit/replay data and is not a PAOS EvidenceBundle, physical-success verdict, or Action admission; real/independently validated worker evidence still requires manual review before Action/Gateway wiring.

### 文件变更详情 / File Change Details

- [新增 / Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/readiness_replay.py:L1-L235`：实现 artifact schema、digest、immutable writer/loader 与 fail-closed 校验。
- [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/readiness_profile.py:L30-L102`：绑定 profile digest，增加受限 `record_replay`，拒绝 unavailable projection。
- [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/readiness.py:L127-L138`、`__init__.py:L36-L55,L128-L134`：暴露受限 replay API 与公共 helper。
- [修改 / Modified] `examples/forge-adapters/robotwin20/tests/test_readiness_replay.py:L130-L206`：覆盖成功写入、canonical/digest、篡改/覆盖、unavailable、路径/权限/symlink 和 timezone 边界。
- [修改 / Modified] `examples/forge-adapters/robotwin20/README.md:L216-L225`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L455-L460`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L504-L507`：记录 artifact 非 EvidenceBundle、人工审核门禁和执行顺序。

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-adapters/robotwin20/src python -m pytest -p pytest_asyncio.plugin -q examples/forge-adapters/robotwin20/tests/test_readiness_replay.py examples/forge-adapters/robotwin20/tests/test_process_worker.py` → `25 passed`。
- 根仓库 → `161 passed`；依赖隔离 adapter 子集 → `16 passed`；pick-place → `256 passed`；Ruff、compileall、`git diff --check` → 通过。

### Git 提交 / Git Commit

- Commit: `9a2af2e`
- Branch: `feature/long-horizon-workflow`

## v4.4.1 (2026-09-04 23:15) - codex

### 预期修改 / Planned Changes

- [计划] [chore] [docs] 回写 v4.4.0 readiness replay artifact 实现提交哈希并同步根 CHANGELOG；不修改实现、测试或执行顺序。
- [Planned] [Chore] [Docs] Record the v4.4.0 readiness replay artifact implementation commit hash and synchronize the root CHANGELOG; do not change implementation, tests, or execution order.

### 实际结果 / Actual Results

- [完成] [chore] [docs] v4.4.0 实现提交为 `a2f972a`；根 CHANGELOG 已同步，未修改实现、测试或执行顺序。
- [Completed] [Chore] [Docs] v4.4.0 implementation commit recorded as `a2f972a`; root CHANGELOG synchronized with no implementation, test, or execution-order changes.

### 文件变更详情 / File Change Details

- [修改 / Modified] `CHANGELOG.md:L10-L23`、`changelog/2026-09_part2.md:L1910-L1925`：补全 v4.4.0 提交元数据。

### 验证 / Validation

- `git diff --check` 和日志 UTF-8 显示核对通过。

### Git 提交 / Git Commit

- Commit: pending
- Branch: `feature/long-horizon-workflow`

## v4.7.0 (2026-09-04 19:48) - codex

### 预期修改 / Planned Changes

- [完成] [comm] [refactor] 按 RoboTwin 官方 embodiment 协议扩展 adapter profile，支持双臂 embodiment 与 `[franka-panda, franka-panda, 0.8]` 双单臂配置；禁止把单臂 Panda 静默当成共享双臂实体，保持公共 PAOS ToolSpec 不变。(local)
- [Completed] [Comm] [Refactor] Extended the adapter profile according to RoboTwin's official embodiment protocol, supporting native dual-arm embodiments and the `[franka-panda, franka-panda, 0.8]` two-single-arm configuration; a single Panda is never silently treated as a shared dual-arm entity, and public PAOS ToolSpecs remain unchanged. (local)
- [完成] [comm] [feat] 为 no-motion readiness worker 增加 robot/gripper identity、embodiment topology 和 profile digest 绑定；fixture、evidence manifest、worker response 与 adapter projection 必须一致，否则 fail-closed。(local)
- [Completed] [Comm] [Feat] Added robot/gripper identity, embodiment topology, and profile-digest bindings to the no-motion readiness worker; fixture, evidence manifest, worker response, and adapter projection must agree or fail closed. (local)
- [完成] [eval] [exp] 以 `blocks_ranking_rgb` 作为 Franka 首个长程 RoboTwin 场景，完成 no-motion scene setup/observation 与 readiness identity 适配；未接入 Action、Gateway、Dora 或硬件运动。(local)
- [Completed] [Eval] [Exp] Used `blocks_ranking_rgb` as the first long-horizon Franka RoboTwin scene and completed no-motion scene/observation plus readiness-identity adaptation; Action, Gateway, Dora, and hardware motion remain disconnected. (local)
- [完成] [docs] [docs] 更新执行顺序、自我审核结论、Franka benchmark 选择依据和后续 `stack_blocks_three` 扩展门槛。(local)
- [Completed] [Docs] [Docs] Updated execution order, self-review conclusions, Franka benchmark rationale, and the promotion gate for later `stack_blocks_three` expansion. (local)

### 实际结果 / Actual Results

- `robotwin_backend.py` 新增 runtime profile loader、finite interval 与 topology 校验、native dual-arm/pair wiring 和 `--profile` CLI；不调用动作 API。
- `preflight.py` 接受单臂 pair，检查每个 embodiment config 并在外部解释器执行 `dual_arm` topology probe。
- readiness worker、manifest、response 和 immutable artifact 统一校验 `embodiment_binding`（robot、gripper、topology、planner、profile digest）。
- 新增 `franka-blocks-ranking.yaml`（`blocks_ranking_rgb` + `[franka-panda, franka-panda, 0.8]`）；readiness profile 绑定其 SHA-256。
- 更新 PAOS state-file diagnosis、adapter diagnosis、implementation review 和 adapter README 的本体替换边界与执行顺序。

### 五维审查 / Five-Dimension Review

- 架构集成 / Architecture integration：本体和 benchmark 保持在 adapter/profile，PAOS 公共 ToolSpec 与生命周期未污染。
- 失败路径 / Failure paths：缺失 config、非法 topology、非 finite 间距、identity/digest 漂移均 fail-closed；无动作路径保持 unavailable。
- 权威边界 / Authority boundaries：RoboTwin 仅提供传感器；readiness 只投影证据，`motion_authorized` 固定为 false。
- 配置 / Configuration：任务、本体、传感器、planner、身份和路径外部化；profile digest 绑定 readiness。
- 可维护性 / Maintainability：新增 profile 即可复用同一 adapter；无 PAOS/Hephaestus 运行时复制和临时动作 fixture。

### 验证 / Validation

- readiness/backend/preflight 专项：`37 passed`；adapter dependency-isolated subset：`71 passed, 1 skipped`；根仓库（`pytest_asyncio` plugin）：`161 passed`。
- `ruff check examples/forge-adapters/robotwin20`、`compileall`、`git diff --check`：passed。
- RoboTwin20 实际 preflight（`blocks_ranking_rgb` + `[franka-panda, franka-panda, 0.8]`）：`ready=true`；实际 no-motion scene capture 生成 RGB/depth/state/calibration，未调用 `play_once`/`check_success`。

## v4.7.1 (2026-09-04 20:10) - codex

### 预期修改 / Planned Changes

- [计划] [chore] [docs] 回写 v4.7.0 本体 profile 与 readiness identity 实现提交哈希，并同步根 CHANGELOG；不改变实现行为。(local)
- [Planned] [Chore] [Docs] Record the v4.7.0 embodiment-profile and readiness-identity implementation commit hash and sync the root CHANGELOG; implementation behavior remains unchanged. (local)

### 实际结果 / Actual Results

- [完成] [chore] [docs] v4.7.0 实现提交为 `30bf3ed`；本条仅维护日志索引与提交元数据。(local)
- [Completed] [Chore] [Docs] v4.7.0 implementation commit recorded as `30bf3ed`; this entry only maintains changelog indexes and commit metadata. (local)

### Git 提交 / Git Commit

- Commit: pending
- Branch: `feature/long-horizon-workflow`

## v4.7.2 (2026-09-04 20:20) - codex

### 预期修改 / Planned Changes

- [计划] [comm] [fix] 让 readiness profile 自动校验其绑定的 runtime profile 文件和 SHA-256，防止 benchmark/本体配置漂移后复用旧 evidence。(local)
- [Planned] [Comm] [Fix] Make the readiness profile verify its bound runtime-profile file and SHA-256 automatically, preventing stale evidence from being reused after benchmark/embodiment drift. (local)

### 实际结果 / Actual Results

- [完成] [comm] [fix] `readiness_profile.py` 新增 runtime profile 绝对路径、权限和 SHA-256 校验；不匹配时在 worker 启动前 fail-closed。(local)
- [Completed] [Comm] [Fix] `readiness_profile.py` now validates the runtime-profile absolute path, permissions, and SHA-256 before worker startup; mismatches fail closed. (local)
- [完成] [docs] [docs] README、状态文件诊断和执行审查文档补充 profile-digest 漂移门禁。(local)
- [Completed] [Docs] [Docs] Updated the README, state-file diagnosis, and execution review with the profile-digest drift gate. (local)

### 验证 / Validation

- Readiness replay: `21 passed`; backend/preflight focused suite remains passing; Ruff and compileall pass。

## v4.7.3 (2026-09-04 20:30) - codex

### 预期修改 / Planned Changes

- [计划] [chore] [docs] 同步根 CHANGELOG 的最新 v4.7.2 readiness profile digest 维护记录，不修改实现行为。(local)
- [Planned] [Chore] [Docs] Synchronize the root CHANGELOG with the latest v4.7.2 readiness profile-digest maintenance record; implementation behavior remains unchanged. (local)

## v4.5.1 (2026-09-05 00:25) - codex

### 变更摘要 / Change Summary

- [完成] [chore] [docs] 回写 v4.5.0 provider no-motion 真实链路验收提交哈希 `9a2af2e`；不改变实现、测试或执行顺序。
- [Completed] [Chore] [Docs] Recorded the v4.5.0 provider no-motion live-chain acceptance commit hash `9a2af2e`; implementation, tests, and execution order are unchanged.

### 文件变更详情 / File Change Details

- [修改 / Modified] `changelog/2026-09_part2.md:L1939-L2002`：补充 v4.5.0 的实现提交元数据。
- [Modified] `changelog/2026-09_part2.md:L1939-L2002`: add the v4.5.0 implementation commit metadata.

### 验证 / Validation

- `git diff --check` 通过；实现提交 `9a2af2e` 已存在于 `feature/long-horizon-workflow`。
- `git diff --check` passed; implementation commit `9a2af2e` exists on `feature/long-horizon-workflow`.

### Git 提交 / Git Commit

- Commit: pending
- Branch: `feature/long-horizon-workflow`

## v4.5.5 (2026-09-05 01:30) - codex

### 变更摘要 / Change Summary

- [计划] [chore] [docs] 回写 v4.5.4 GraspGen 验收日志索引提交哈希；不修改实现、测试或执行顺序。
- [Planned] [Chore] [Docs] Record the v4.5.4 GraspGen acceptance-index commit hash; do not change implementation, tests, or execution order.

### 文件变更详情 / File Change Details

- [计划 / Planned] `changelog/2026-09_part2.md`、`CHANGELOG.md`：补充 v4.5.4 commit metadata 与最近版本索引。

### 实际结果 / Actual Results

- [完成] [chore] [docs] v4.5.4 日志索引提交为 `0cfcd56`；本次仅维护日志索引和提交元数据。
- [Completed] [Chore] [Docs] The v4.5.4 acceptance-index commit is `0cfcd56`; this change only maintains changelog indexes and commit metadata.

### 文件变更详情 / File Change Details

- [修改 / Modified] `CHANGELOG.md:L10-L29`、`changelog/2026-09_part2.md:L1965-L1979`：加入 v4.5.4 commit 引用及最近版本摘要。
- [Modified] `CHANGELOG.md:L10-L29` and `changelog/2026-09_part2.md:L1965-L1979`: add the v4.5.4 commit reference and recent-version summary.

### 验证 / Validation

- `git diff --check` 通过，未修改实现或测试。
- `git diff --check` passed; implementation and tests were not changed.

### Git 提交 / Git Commit

- Commit: pending
- Branch: `feature/long-horizon-workflow`

## v4.6.0 (2026-09-05 02:00) - codex

### 预期修改 / Planned Changes

- [计划] [comm] [feat] 新增独立 readiness worker 的严格请求绑定与 robot/gripper identity gate；worker 只在 observation、scene revision、frame、calibration、candidate-set、candidate/entity 和机器人 profile 完整一致时进入评估，否则返回结构化 unavailable，绝不伪造 IK/collision 通过。
- [Planned] [Comm] [Feat] Add an independent readiness worker with strict request binding and robot/gripper identity gates; enter evaluation only when observation, scene revision, frame, calibration, candidate set, candidate/entity, and robot profile all match, otherwise return structured unavailable without fabricating IK/collision passes.
- [计划] [eval] [exp] 使用当前真实 GraspGen red-block candidates 运行 readiness worker，保存 request/result/raw stdout/stderr、失败原因和 manifest；本阶段不接入 Action、Gateway、Dora 或硬件。
- [Planned] [Eval] [Exp] Run the readiness worker on the current real GraspGen red-block candidates, persisting request/result/raw stdout/stderr, failure reasons, and a manifest; do not connect Action, Gateway, Dora, or hardware in this phase.

## v4.5.2 (2026-09-05 00:45) - codex

### 预期修改 / Planned Changes

- [计划] [comm] [fix] 修复 GraspGen JSONL worker 将第三方模型日志写入 stdout、导致 `JsonlProcessWorkerClient` 无法解析启动事件的问题；模型初始化和推理噪声改由 stderr 承载，stdout 保持逐行 JSON 协议。
- [Planned] [Comm] [Fix] Fix the GraspGen JSONL worker leaking third-party model logs to stdout, which prevents `JsonlProcessWorkerClient` from parsing startup events; route model initialization/inference noise to stderr while keeping stdout line-delimited JSON.
- [计划] [eval] [exp] 通过 adapter `GraspGenProposalProvider` 重放本次 `entity://red-rectangular-block-1` 点云，保存 request/result、worker 原始 stdout/stderr、candidate funnel 和 digest；不接入 IK、碰撞、Action 或 Gateway。
- [Planned] [Eval] [Exp] Replay the saved `entity://red-rectangular-block-1` point cloud through the adapter `GraspGenProposalProvider`, persisting request/result, raw worker stdout/stderr, candidate funnel, and digests; do not connect IK, collision, Action, or Gateway.

### 实际结果 / Actual Results

- [完成] [comm] [fix] `graspgen_worker.py` 将第三方 GraspGen load/inference 日志重定向至 stderr，stdout 仅保留 `paos-grasp-worker/v1` JSONL 事件和响应；新增 worker stdout 隔离回归测试。
- [Completed] [Comm] [Fix] `graspgen_worker.py` now redirects third-party GraspGen load/inference logs to stderr; stdout contains only `paos-grasp-worker/v1` JSONL events and replies, with a regression test for output isolation.
- [完成] [eval] [exp] 使用独立 GraspGen workspace 通过 `GraspGenProposalProvider` 对真实 `entity://red-rectangular-block-1` 点云完成 no-motion `grasp.propose`：24 candidates，funnel `24→24→24→24`，provider_available=true。
- [Completed] [Eval] [Exp] Using the isolated GraspGen workspace, `GraspGenProposalProvider` completed a no-motion `grasp.propose` on the real `entity://red-rectangular-block-1` point cloud: 24 candidates, funnel `24→24→24→24`, and `provider_available=true`.
- [完成] [eval] [exp] 证据目录为 `/home/yanxu/robotwin20-runtime/artifacts/paos-graspgen-live-20260905T0040Z/`，包含 adapter request/result、配置摘要、clean worker stdout/stderr、原始 JSONL request 和 manifest；manifest SHA-256 为 `a7627a6d8583bf4da502dfe1deaf8c3ec1e978f8f274ede545446614f43ae336`。
- [Completed] [Eval] [Exp] Evidence is stored at `/home/yanxu/robotwin20-runtime/artifacts/paos-graspgen-live-20260905T0040Z/`, containing the adapter request/result, configuration summary, clean worker stdout/stderr, raw JSONL request, and manifest; manifest SHA-256 is `a7627a6d8583bf4da502dfe1deaf8c3ec1e978f8f274ede545446614f43ae336`.
- [完成] [eval] [exp] motion flags 全部为 false；未调用 IK、碰撞、Action、Gateway、Dora 或硬件。该证据只证明 grasp provider seam 可用，不证明 readiness、任意抓取或抓取放置闭环。
- [Completed] [Eval] [Exp] All motion flags are false; IK, collision, Action, Gateway, Dora, and hardware were not called. This evidence proves only that the grasp-provider seam is usable, not readiness, arbitrary grasping, or a pick-and-place loop.

### 文件变更详情 / File Change Details

- [修改 / Modified] `examples/forge-adapters/robotwin20/runtime/graspgen_worker.py:L11-L15,L127-L138`：增加 stdout 重定向包装，隔离模型日志与 JSONL 协议。
- [Modified] `examples/forge-adapters/robotwin20/runtime/graspgen_worker.py:L11-L15,L127-L138`: add stdout redirection wrappers to isolate model logs from the JSONL protocol.
- [新增 / Added] `examples/forge-adapters/robotwin20/tests/test_process_worker.py:L57-L98`：验证模型初始化/推理日志不会污染 worker stdout。
- [Added] `examples/forge-adapters/robotwin20/tests/test_process_worker.py:L57-L98`: verify model initialization/inference logs cannot pollute worker stdout.
- [修改 / Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L485-L502`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L524-L532`、`examples/forge-adapters/robotwin20/README.md:L238-L247`：记录 live GraspGen seam、manifest、no-motion 结果和下一道 readiness 门禁。
- [Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L485-L502`, `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L524-L532`, and `examples/forge-adapters/robotwin20/README.md:L238-L247`: record the live GraspGen seam, manifest, no-motion result, and next readiness gate.

### 验证 / Validation

- GraspGen/process/backend focused tests: `14 passed`；完整 adapter suite（显式 async plugin/dependency paths）：`104 passed`；根仓库：`161 passed`；pick-place：`256 passed`。
- `ruff check`、`compileall`、`git diff --check` 均通过；真实 worker stdout 为纯 JSONL，shutdown 正常。
- GraspGen/process/backend focused tests: `14 passed`; full adapter suite with explicit async plugin/dependency paths: `104 passed`; repository: `161 passed`; pick-place: `256 passed`.
- `ruff check`, `compileall`, and `git diff --check` passed; live worker stdout is pure JSONL and shutdown completed normally.

### Git 提交 / Git Commit

- Commit: `aff62a5`
- Branch: `feature/long-horizon-workflow`

## v4.5.0 (2026-09-05 00:05) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [exp] 重新按架构集成、失败路径、权威边界、配置、可维护性五个维度自审 readiness/真实 provider 诊断；若无 Blocker/Major，确认执行顺序仍为真实/独立 readiness evidence → 人工审核 → Action/Gateway wiring → 仿真动作。
- [Planned] [Eval] [Exp] Re-audit the readiness/real-provider diagnosis across architecture integration, failure paths, authority boundaries, configuration, and maintainability; if no Blocker/Major is found, confirm the order remains real/independent readiness evidence → manual review → Action/Gateway wiring → simulated actions.
- [计划] [eval] [exp] 在独立 RoboTwin 环境执行已接入的 no-motion 真实链路，保存唯一运行目录及每阶段 JSON/摘要/digest：preflight、scene.observe、scene.understand、单视角 mask/depth localization；不启动 Action、Dora 或硬件动作。
- [Planned] [Eval] [Exp] Run the integrated no-motion path in the isolated RoboTwin environment and persist a unique run directory plus per-stage JSON/summary/digests for preflight, scene.observe, scene.understand, and single-view mask/depth localization; do not start Actions, Dora, or hardware motion.
- [计划] [docs] [docs] 根据真实链路结果更新诊断和执行文档，明确成功、不可用和环境阻塞，不把单项 provider 证据升级为完整抓取放置能力。
- [Planned] [Docs] [Docs] Update diagnosis and execution documents from the live-chain result, distinguishing success, unavailable, and environment blockers without promoting individual provider evidence to complete pick-and-place capability.
- [追加计划] [env] [fix] 修复 RoboTwin runtime CLI 将第三方 warning 混入 stdout 的可审计性问题；原始 stdout/stderr 仍分别保留，正式 stdout 只输出一个 JSON 文档。
- [Added Plan] [Env] [Fix] Fix the RoboTwin runtime CLI auditability issue where third-party warnings leak into stdout; preserve raw stdout/stderr separately while making formal stdout exactly one JSON document.

### 实际结果 / Actual Results

- [完成] [eval] [exp] 五维自审未发现 Blocker/Major；执行顺序保持为真实或独立 readiness evidence → 人工审核 → Action/Gateway wiring → RoboTwin 仿真动作。当前未进入动作或硬件执行。
- [Completed] [Eval] [Exp] The five-dimension self-review found no Blocker/Major; the order remains real or independently verified readiness evidence → manual review → Action/Gateway wiring → RoboTwin simulated actions. No action or hardware execution was started.
- [完成] [eval] [exp] 独立 RoboTwin20 真实 no-motion run 已保存于 `/home/yanxu/robotwin20-runtime/artifacts/paos-real-chain-20260905T0020Z/`：preflight、scene.observe、GPT scene.understand、LocateAnything/SAM2/RGB-D localization 均形成逐阶段 JSON、原始 stdout/stderr、感知派生 artifact 和人工审核 mask。
- [Completed] [Eval] [Exp] The isolated RoboTwin20 no-motion run is stored at `/home/yanxu/robotwin20-runtime/artifacts/paos-real-chain-20260905T0020Z/`; preflight, scene.observe, GPT scene.understand, and LocateAnything/SAM2/RGB-D localization each produced stage JSON, raw stdout/stderr, derived perception artifacts, and review masks.
- [完成] [eval] [exp] 最终 `run_manifest.json` SHA-256 为 `da7a81bd2efccbf70312428a3adeef10babe2d465734f63f7c90444297389b46`；`motion_authorized`、`simulation_motion_requested`、`simulation_motion_executed` 均为 `false`。
- [Completed] [Eval] [Exp] The final `run_manifest.json` SHA-256 is `da7a81bd2efccbf70312428a3adeef10babe2d465734f63f7c90444297389b46`; `motion_authorized`, `simulation_motion_requested`, and `simulation_motion_executed` are all `false`.
- [完成] [eval] [exp] GraspGen 因 `GRASPGEN_PYTHON` 未配置、readiness 因 `READINESS_FIXTURE` 未配置而 fail-closed 为 unavailable；`object.acquire`/`object.place` 未尝试，因此当前证据不代表任意抓取或抓取放置闭环。
- [Completed] [Eval] [Exp] GraspGen failed closed as unavailable because `GRASPGEN_PYTHON` is not configured, and readiness failed closed as unavailable because `READINESS_FIXTURE` is not configured; `object.acquire`/`object.place` were not attempted, so the evidence does not represent arbitrary grasping or a pick-and-place loop.

### 文件变更详情 / File Change Details

- [修改 / Modified] `examples/forge-adapters/robotwin20/runtime/robotwin_backend.py:L18,L384-L412`：将 runtime 初始化、reset、capture 和 close 放入 `redirect_stdout(sys.stderr)`，保证 stdout 只输出单一 JSON provider 文档；保留原始 stderr 供审计。
- [Modified] `examples/forge-adapters/robotwin20/runtime/robotwin_backend.py:L18,L384-L412`: redirect runtime initialization, reset, capture, and close to `stderr` so stdout contains exactly one JSON provider document while raw stderr remains auditable.
- [新增 / Added] `examples/forge-adapters/robotwin20/tests/test_robotwin_backend_contract.py:L79-L118`：覆盖第三方 stdout warning 重定向及机器可解析单 JSON 输出。
- [Added] `examples/forge-adapters/robotwin20/tests/test_robotwin_backend_contract.py:L79-L118`: cover third-party stdout warning redirection and machine-parseable single-document output.
- [修改 / Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L462-L483`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L509-L522`、`examples/forge-adapters/robotwin20/README.md:L227-L236`：记录真实 no-motion 链路、五维结论、provider unavailable 边界及最终 manifest digest。
- [Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L462-L483`, `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L509-L522`, and `examples/forge-adapters/robotwin20/README.md:L227-L236`: record the live no-motion chain, five-dimension conclusion, provider-unavailable boundaries, and final manifest digest.
- [修改 / Modified] `CHANGELOG.md:L10-L27`：更新最近版本索引，加入 v4.5.0 的双语摘要、文件范围、验证结果和 manifest digest。
- [Modified] `CHANGELOG.md:L10-L27`: update the recent-version index with the bilingual v4.5.0 summary, file scope, validation results, and manifest digest.

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/yanxu/miniconda3/envs/paos/lib/python3.12/site-packages:.:examples/forge-adapters/robotwin20/src:examples/forge-skills/pick-place-workflow/src /usr/bin/python3 -m pytest -p pytest_asyncio.plugin -q examples/forge-adapters/robotwin20/tests` → `103 passed`。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -p pytest_asyncio.plugin -q` → `161 passed`；`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-skills/pick-place-workflow/src python -m pytest -p pytest_asyncio.plugin -q examples/forge-skills/pick-place-workflow/tests` → `256 passed`。
- `ruff check`、`compileall`、`git diff --check` 均通过；真实运行保持三项 motion flags 为 `false`。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/yanxu/miniconda3/envs/paos/lib/python3.12/site-packages:.:examples/forge-adapters/robotwin20/src:examples/forge-skills/pick-place-workflow/src /usr/bin/python3 -m pytest -p pytest_asyncio.plugin -q examples/forge-adapters/robotwin20/tests` → `103 passed`.
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -p pytest_asyncio.plugin -q` → `161 passed`; `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-skills/pick-place-workflow/src python -m pytest -p pytest_asyncio.plugin -q examples/forge-skills/pick-place-workflow/tests` → `256 passed`.
- `ruff check`, `compileall`, and `git diff --check` passed; the live run kept all three motion flags `false`.

### Git 提交 / Git Commit

- Commit: `9a2af2e`
- Branch: `feature/long-horizon-workflow`

## v4.3.4 (2026-09-04 22:15) - codex

### 预期修改 / Planned Changes

- [完成] [chore] [docs] 回写 v4.3.3 readiness calibration identity 修复提交哈希；不修改实现、测试或执行顺序。
- [Completed] [Chore] [Docs] Recorded the v4.3.3 readiness calibration-identity fix commit hash; implementation, tests, and execution order were unchanged.

### 实际结果 / Actual Results

- [完成] [chore] [docs] v4.3.3 实现提交为 `20c6ad6`。
- [Completed] [Chore] [Docs] v4.3.3 implementation commit recorded as `20c6ad6`.

### 文件变更详情 / File Change Details

- [修改 / Modified] `changelog/2026-09_part2.md:L1890-L1910`：回写 v4.3.3 提交元数据。

### 验证 / Validation

- `git diff --check` 和当前分支状态核对通过。

### Git 提交 / Git Commit

- Commit: pending
- Branch: `feature/long-horizon-workflow`

### Git 提交 / Git Commit

- Commit: `a2c3c78`
- Branch: `feature/long-horizon-workflow`

## v4.3.0 (2026-09-04 21:00) - codex

### 预期修改 / Planned Changes

- [完成] [comm] [feat] 为 readiness replay 增加独立 hash-pinned evidence manifest sidecar；worker 在返回 prepared evidence 前校验 observation、scene revision、frame、candidate-set、calibration 和 no-motion 绑定。
- [Completed] [Comm] [Feat] Added an independent hash-pinned evidence manifest sidecar to readiness replay; the worker validates observation, scene revision, frame, candidate-set, calibration, and no-motion bindings before returning prepared evidence.
- [完成] [eval] [test] 覆盖 manifest digest、路径/权限、证据引用缺失、身份漂移、时间戳和跨 case 越权，并完成架构集成、失败路径、权威边界、配置、可维护性五维审查。
- [Completed] [Eval] [Test] Covered manifest digest, path/permission, missing evidence references, identity drift, timestamps, and cross-case authorization, followed by the five-dimension review.

### 实际结果 / Actual Results

- [完成] [comm] [feat] `readiness_replay_worker.py` 新增严格 evidence manifest loader；每个 artifact reference 均绑定 observation、scene revision、frame、candidate-set、calibration、source 和带时区时间戳，fixture 与 manifest 均执行 no-motion、权限和 schema 校验。
- [Completed] [Comm] [Feat] `readiness_replay_worker.py` now has a strict evidence-manifest loader; every artifact reference binds observation, scene revision, frame, candidate-set, calibration, source, and a timezone-aware timestamp, while fixture and manifest enforce no-motion, permission, and schema checks.
- [完成] [comm] [feat] `readiness_profile.py` 增加 evidence manifest 路径、SHA-256 和重复 worker 参数门禁，并通过现有 JSONL process boundary 注入 manifest。
- [Completed] [Comm] [Feat] `readiness_profile.py` adds evidence-manifest path, SHA-256, and duplicate-worker-argument gates and injects the manifest through the existing JSONL process boundary.
- [完成] [eval] [review] 五维审查无 Blocker/Major；证据缺失、跨 case 漂移、无时区时间戳、digest/path/schema/identity 错误均 fail-closed；不连接真实 IK、碰撞、Action、Gateway、Dora 或硬件。
- [Completed] [Eval] [Review] Five-dimension review found no Blocker/Major; missing evidence, cross-case drift, naive timestamps, digest/path/schema/identity errors all fail closed; no real IK, collision, Action, Gateway, Dora, or hardware was connected.

### 文件变更详情 / File Change Details

- [修改 / Modified] `examples/forge-adapters/robotwin20/runtime/readiness_replay_worker.py:L18-L264`：新增 evidence manifest schema、绑定校验、时间戳校验和 no-motion replay gate。
- [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/readiness_profile.py:L79-L166`：增加 manifest 路径、权限、digest 和 worker 参数门禁。
- [修改 / Modified] `examples/forge-adapters/robotwin20/tests/test_readiness_replay.py:L49-L268`：新增 manifest digest、symlink、重复参数、缺失引用、identity drift 和 timezone failure cases。
- [修改 / Modified] `examples/forge-adapters/robotwin20/profiles/robotwin20/readiness-replay.yaml:L1-L17`、`examples/forge-adapters/robotwin20/README.md:L202-L218`：声明 manifest 配置与证据绑定边界。
- [修改 / Modified] `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L486-L505`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L450-L454`：更新下一步完成状态和五维审查结论。

### 验证 / Validation

- readiness/replay/process 专项 → `34 passed`；依赖隔离 adapter 子集 → `44 passed`；根仓库 → `161 passed`；pick-place → `256 passed`。
- Ruff、compileall、`git diff --check` → 通过。

## v4.3.1 (2026-09-04 21:30) - codex

### 预期修改 / Planned Changes

- [完成] [chore] [docs] 回写 v4.3.0 readiness evidence manifest 实现提交哈希并同步根 CHANGELOG；不修改实现、测试或执行顺序。
- [Completed] [Chore] [Docs] Recorded the v4.3.0 readiness evidence-manifest implementation commit hash and synchronized the root CHANGELOG; implementation, tests, and execution order were unchanged.

### 实际结果 / Actual Results

- [完成] [chore] [docs] v4.3.0 实现提交为 `23364de`；根 CHANGELOG 已同步，未修改实现、测试或执行顺序。
- [Completed] [Chore] [Docs] v4.3.0 implementation commit recorded as `23364de`; root CHANGELOG synchronized with no implementation, test, or execution-order changes.

### 文件变更详情 / File Change Details

- [修改 / Modified] `CHANGELOG.md:L10-L33`、`changelog/2026-09_part2.md:L1850-L1880`：补全 v4.3.0 提交元数据和维护记录。

### 验证 / Validation

- `git diff --check`、日志 UTF-8 显示和当前分支状态核对通过。

### Git 提交 / Git Commit

- Commit: `8833784`
- Branch: `feature/long-horizon-workflow`

## v4.3.2 (2026-09-04 21:45) - codex

### 预期修改 / Planned Changes

- [完成] [chore] [docs] 回写 v4.3.1 日志维护提交哈希；不修改实现、测试或执行顺序。
- [Completed] [Chore] [Docs] Recorded the v4.3.1 changelog-maintenance commit hash; implementation, tests, and execution order were unchanged.

### 实际结果 / Actual Results

- [完成] [chore] [docs] v4.3.1 维护提交为 `8833784`。
- [Completed] [Chore] [Docs] v4.3.1 maintenance commit recorded as `8833784`.

### 文件变更详情 / File Change Details

- [修改 / Modified] `changelog/2026-09_part2.md:L1903-L1919`：回写 v4.3.1 提交元数据。

### 验证 / Validation

- `git diff --check` 和当前分支状态核对通过。

### Git 提交 / Git Commit

- Commit: pending
- Branch: `feature/long-horizon-workflow`
## v4.7.4 (2026-09-04 22:00) - codex

### 预期修改 / Planned Changes

- [完成] [eval] [review] 审核 Franka `blocks_ranking_rgb` 的真实 readiness 输入是否完整；只在存在同一 scene revision 的 geometry、candidate 和外部 IK/碰撞 evidence 时运行 probe，否则记录 unavailable 并保持动作门禁关闭。
- [Completed] [Eval] [Review] Audited whether the Franka `blocks_ranking_rgb` readiness inputs are complete; a probe is run only when geometry, candidates, and external IK/collision evidence share one scene revision, otherwise unavailable is recorded and motion remains gated.

### 实际结果 / Actual Results

- [完成] [eval] [review] Franka capture 已确认包含 `rgb.png`、`depth.npy`、`state.json`、`calibration.json`，但没有 `derived/` geometry 或 candidate set；现有 GraspGen 结果绑定 `beat_block_hammer-0-1`，与 Franka 的 `blocks_ranking_rgb-0-1` 不一致，因此 readiness probe 安全地记录为 `unavailable`，未生成 prepared candidate 或 replay artifact。
- [Completed] [Eval] [Review] The Franka capture contains `rgb.png`, `depth.npy`, `state.json`, and `calibration.json`, but no `derived/` geometry or candidate set; the existing GraspGen result is bound to `beat_block_hammer-0-1`, not Franka's `blocks_ranking_rgb-0-1`, so the readiness probe was safely recorded as `unavailable` with no prepared candidate or replay artifact generated.
- [完成] [docs] [docs] 新增 Franka readiness 输入审计文档，并同步架构诊断、实现审查和 adapter README 的执行顺序；明确在同 revision geometry → GraspGen → 外部 IK/collision/workspace evidence → 人工审核前，不进入 Action/Gateway/Dora。
- [Completed] [Docs] [Docs] Added the Franka readiness-input audit and synchronized the architecture diagnosis, implementation review, and adapter README; explicitly kept Action/Gateway/Dora gated until same-revision geometry, GraspGen, external IK/collision/workspace evidence, and manual review are complete.

### 文件变更详情 / File Change Details

- [新增 / Added] `docs/forge/FRANKA_READINESS_INPUT_AUDIT_20260904.md:L1-L47`：记录 capture、scene revision、candidate provenance、缺失输入、安全边界和下一步门禁。
- [修改 / Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L536-L547`：追加 Franka readiness 输入审计与 `unavailable` 结论。
- [修改 / Modified] `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L553-L563`：追加五维审查后的 readiness 输入结论。
- [修改 / Modified] `examples/forge-adapters/robotwin20/README.md:L303-L309`：补充 Franka readiness gate 和同 revision 要求。

### 验证 / Validation

- 输入完整性审计：Franka capture 四类基础 artifact 存在，`derived/` 缺失，GraspGen source revision 为 `beat_block_hammer-0-1`，结论 `readiness_probe=unavailable`。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-adapters/robotwin20/src:. python -m pytest -p pytest_asyncio.plugin -q examples/forge-adapters/robotwin20/tests/test_readiness.py examples/forge-adapters/robotwin20/tests/test_readiness_replay.py examples/forge-adapters/robotwin20/tests/test_robotwin_backend_contract.py` → `46 passed`。
- `git diff --check` → 通过。未调用 Action、Gateway、Dora 或硬件运动。

### Git 提交 / Git Commit

- Commit: pending
- Branch: `feature/long-horizon-workflow`
## v4.7.5 (2026-09-04 22:10) - codex

### 预期修改 / Planned Changes

- [计划] [chore] [docs] 回写 v4.7.4 Franka readiness 输入审计提交哈希；不修改实现、审计结论或执行顺序。
- [Planned] [Chore] [Docs] Record the v4.7.4 Franka readiness-input audit commit hash without changing implementation, audit conclusions, or execution order.

### 实际结果 / Actual Results

- [完成] [chore] [docs] v4.7.4 审计提交为 `ee2144e`；实现、审计结论和执行顺序保持不变。
- [Completed] [Chore] [Docs] The v4.7.4 audit commit is `ee2144e`; implementation, audit conclusions, and execution order are unchanged.

### 文件变更详情 / File Change Details

- [修改 / Modified] `changelog/2026-09_part2.md:L1-L13`：记录 v4.7.4 提交元数据。

### 验证 / Validation

- `git diff --check` → 通过。

### Git 提交 / Git Commit

- Commit: pending
- Branch: `feature/long-horizon-workflow`
## v4.7.6 (2026-09-04 22:30) - codex

### 预期修改 / Planned Changes

- [完成] [comm] [feat] 新增 RoboTwin 外部 readiness worker：在独立 Python 进程中重建同一 profile 场景，使用 Curobo 对候选做 no-motion IK/碰撞/工作空间检查，并输出严格绑定的 evidence。
- [Completed] [Comm] [Feat] Add an external RoboTwin readiness worker that reconstructs the same profiled scene in an isolated Python process, evaluates candidates with Curobo for no-motion IK/collision/workspace checks, and emits strictly bound evidence.
- [完成] [eval] [test] 对 worker 的输入绑定、候选姿态、超时、planner 失败、no-motion 和 evidence digest 进行验证；仅在真实 worker evidence 通过后更新人工审核记录。
- [Completed] [Eval] [Test] Validate worker input binding, candidate poses, timeouts, planner failures, no-motion, and evidence digests; update the manual-review record only after real worker evidence passes.
- [完成] [fix] 将独立 live worker 的 response schema 与 replay worker 解耦，并在生成审核记录前校验每个 evidence 的 `motion_authorized` 必须为 `false`。
- [Completed] [Fix] Decouple the independent live-worker response schema from the replay worker and require every evidence item to have `motion_authorized=false` before producing the review record.
- [完成] [fix] 加强 live worker 的 freshness/max-age、candidate-set、request identity 校验，并把这些字段写入每个 evidence artifact。
- [Completed] [Fix] Strengthen live-worker freshness/max-age, candidate-set, and request-identity validation and persist those fields in every evidence artifact.

### 实际结果 / Actual Results

- [完成] [feat] `examples/forge-adapters/robotwin20/runtime/robotwin_readiness_worker.py:L26-L325`：独立 live worker 使用 `paos-robotwin20-readiness-live/v1`，校验 request identity、scene revision、candidate-set、calibration、freshness/max-age、候选 provenance/pose 和 workspace；每个 evidence 写入 request/candidate-set/observation/scene/frame/calibration/worker/profile/timestamp 绑定，固定 `motion_authorized=false`。
- [Completed] [Feat] `examples/forge-adapters/robotwin20/runtime/robotwin_readiness_worker.py:L26-L325`: the independent live worker uses `paos-robotwin20-readiness-live/v1`, validates request identity, scene revision, candidate set, calibration, freshness/max-age, candidate provenance/pose, and workspace; each evidence persists request/candidate-set/observation/scene/frame/calibration/worker/profile/timestamp bindings with `motion_authorized=false`.
- [完成] [feat] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/readiness_profile.py:L285-L355`、`profiles/robotwin20/readiness-live.yaml:L1-L38`：新增 profile-owned live process client/builder，复用 bounded JSONL lifecycle 并在 PAOS preparation 前校验 live schema、worker identity、embodiment binding 和 no-motion。
- [Completed] [Feat] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/readiness_profile.py:L285-L355`, `profiles/robotwin20/readiness-live.yaml:L1-L38`: added a profile-owned live process client/builder, reusing the bounded JSONL lifecycle and validating live schema, worker identity, embodiment binding, and no-motion before PAOS preparation.
- [完成] [eval] [exp] `/home/yanxu/robotwin20-runtime/artifacts/paos-franka-blocks-ranking-v470-20260904T/`：同一 `blocks_ranking_rgb-0-1/head_camera` 上 geometry 12 artifacts（4 point clouds，含 3 个 block）、GraspGen `72→72→71→71`、独立 Curobo readiness `50/71`、PAOS preparation 50、唯一 evidence 50；最终 manifest SHA-256 `b0cd2298b84bbc4be0470fb66da4b543928836dd026433ae7e0861cb691fec79`，链路校验记录为 `09_readiness_chain_verification.json`，人工审核仅批准下一 no-motion gate。
- [Completed] [Eval] [Exp] `/home/yanxu/robotwin20-runtime/artifacts/paos-franka-blocks-ranking-v470-20260904T/`: same `blocks_ranking_rgb-0-1/head_camera` geometry (12 artifacts, four point clouds including three blocks), GraspGen `72→72→71→71`, independent Curobo readiness `50/71`, PAOS preparation 50, and 50 unique evidence artifacts; final manifest SHA-256 `b0cd2298b84bbc4be0470fb66da4b543928836dd026433ae7e0861cb691fec79`, with chain verification in `09_readiness_chain_verification.json` and manual review limited to the next no-motion gate.

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-adapters/robotwin20/src:. python -m pytest -p pytest_asyncio.plugin -q examples/forge-adapters/robotwin20/tests/test_readiness.py examples/forge-adapters/robotwin20/tests/test_readiness_replay.py examples/forge-adapters/robotwin20/tests/test_robotwin_backend_contract.py` → `50 passed`；新增 live projection 测试包含 schema/motion drift fail-closed。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -p pytest_asyncio.plugin -q` → `161 passed`。
- Ruff、compileall、`git diff --check` → 通过；RoboTwin20 外部 live profile→JSONL worker→PAOS `manipulation.prepare` 实际运行 `available`, `50 prepared`, 三项 checks `pass`, `motion_authorized=false`。

### 文件变更详情 / File Change Details

- [修改 / Modified] `examples/forge-adapters/robotwin20/runtime/robotwin_readiness_worker.py:L67-L139,L231-L252`：补强 freshness、calibration、provenance 和证据身份绑定。
- [新增 / Added] `examples/forge-adapters/robotwin20/profiles/robotwin20/readiness-live.yaml:L1-L38`：新增 live worker 外部配置。
- [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/readiness_profile.py:L26-L355`、`src/robotwin20_adapter/__init__.py:L42-L56,L125-L145`：新增 live client/builder/export。
- [修改 / Modified] `examples/forge-adapters/robotwin20/tests/test_readiness_replay.py:L12-L15,L387-L438`：新增 live schema/no-motion drift conformance。
- [修改 / Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L548-L565`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L565-L577`、`examples/forge-adapters/robotwin20/README.md:L205-L245`：更新证据闭环完成状态、五维 review 与下一步顺序。

### Git 提交 / Git Commit

- Commit: `8a2e806`
- Branch: `feature/long-horizon-workflow`

## v4.7.7 (2026-09-04 22:58) - codex

### 预期修改 / Planned Changes

- [完成] [chore] [docs] 回写 v4.7.6 Franka readiness evidence 实现提交哈希并校正最终测试数量/行号；不修改运行逻辑或动作权限。
- [Completed] [Chore] [Docs] Record the v4.7.6 Franka readiness-evidence implementation commit and correct final test counts/line references without changing runtime logic or motion authority.

### 实际结果 / Actual Results

- [完成] [chore] [docs] v4.7.6 实现提交为 `8a2e806`，已推送 `feature/long-horizon-workflow`；专项最终为 `50 passed`，根仓库为 `161 passed`。
- [Completed] [Chore] [Docs] v4.7.6 implementation commit `8a2e806` was pushed to `feature/long-horizon-workflow`; focused tests finished at `50 passed` and the repository suite at `161 passed`.

### Git 提交 / Git Commit

- Commit: pending
- Branch: `feature/long-horizon-workflow`

## v4.7.8 (2026-09-04 23:12) - codex

### 预期修改 / Planned Changes

- [完成] [chore] [docs] 回写最终 live worker request/response/evidence 一致性校验后的 manifest digest；不修改实现逻辑。
- [Completed] [Chore] [Docs] Record the final manifest digest after request/response/evidence identity reconciliation without changing runtime logic.

### 实际结果 / Actual Results

- [完成] [chore] [docs] 最终 request_id 为 `1f836aea2afa4bd5b87255978bbfe214`，response 与 evidence 完全一致；manifest SHA-256 更新为 `b0cd2298b84bbc4be0470fb66da4b543928836dd026433ae7e0861cb691fec79`，所有 50 个 candidate review 条目 `motion_authorized=false`。
- [Completed] [Chore] [Docs] Final request_id `1f836aea2afa4bd5b87255978bbfe214` is consistent across request, response, and evidence; manifest SHA-256 is `b0cd2298b84bbc4be0470fb66da4b543928836dd026433ae7e0861cb691fec79`, and all 50 candidate review entries have `motion_authorized=false`.

### Git 提交 / Git Commit

- Commit: pending
- Branch: `feature/long-horizon-workflow`
## v4.7.9 (2026-09-05 00:00) - codex

### 预期修改 / Planned Changes

- [完成] [comm] [feat] 新增 provider-neutral 的 no-motion manipulation Action admission，要求已审核 readiness evidence、candidate/scene/calibration 身份完整绑定后才创建 Gateway invocation；不调用 RoboTwin `play_once`、Dora 或硬件。
- [Completed] [Comm] [Feat] Add provider-neutral no-motion manipulation Action admission that requires reviewed readiness evidence and complete candidate/scene/calibration identity binding before creating a Gateway invocation; never call RoboTwin `play_once`, Dora, or hardware.
- [完成] [eval] [test] 增加 acquire/place 的成功、身份漂移、缺失 evidence、超时、取消、失败和未知状态 conformance，并按架构集成、失败路径、权威边界、配置、可维护性五维审查。
- [Completed] [Eval] [Test] Add conformance for acquire/place success, identity drift, missing evidence, timeout, cancel, failure, and unknown states, followed by a five-dimension review covering architecture integration, failure paths, authority boundaries, configuration, and maintainability.

### 实际结果 / Actual Results

- [完成] [comm] [feat] `ReadinessEvidenceGate` 校验人工审核 decision、manifest/evidence SHA-256、同一 scene/candidate-set/frame/calibration、candidate/entity、三项 readiness checks、worker/embodiment identity 和 `motion_authorized=false`；`object.acquire`/`object.place` 在 invocation 创建前执行 gate，no-motion provider 返回 `world_change_started=true` 时拒绝。
- [Completed] [Comm] [Feat] `ReadinessEvidenceGate` validates the manual-review decision, manifest/evidence SHA-256, same scene/candidate-set/frame/calibration, candidate/entity, all readiness checks, worker/embodiment identity, and `motion_authorized=false`; `object.acquire`/`object.place` run the gate before invocation allocation and reject a no-motion provider that reports `world_change_started=true`.
- [完成] [comm] [feat] Fake Gateway Action context 显式返回 `motion_authorized=false`；新增外部 `action-readiness.yaml` profile loader，路径通过环境变量注入，未新增 RoboTwin/Dora/硬件依赖。
- [Completed] [Comm] [Feat] Fake Gateway Action contexts now explicitly return `motion_authorized=false`; added an external `action-readiness.yaml` profile loader with environment-injected paths and no new RoboTwin/Dora/hardware dependency.
- [完成] [eval] [test] 新增 gate 的 manifest/review/digest、身份漂移、证据缺失、过期、运动漂移、符号链接路径和 acquire/place Gateway Action conformance；专项测试 `52 passed`，实际 Franka manifest 加载 `50` 个 evidence candidate 全部通过。
- [Completed] [Eval] [Test] Added gate and Gateway Action conformance for manifest/review/digests, identity drift, missing evidence, staleness, motion drift, symlink paths, and acquire/place lifecycle; focused tests pass `52`, and the real Franka manifest loads all `50` evidence candidates successfully.

### 文件变更详情 / File Change Details

- [新增 / Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/action_readiness.py:L1-L274`：实现只读 readiness evidence gate 与 profile loader。
- [新增 / Added] `examples/forge-adapters/robotwin20/profiles/robotwin20/action-readiness.yaml:L1-L4`：外部 manifest/review/artifact root 配置。
- [新增 / Added] `examples/forge-adapters/robotwin20/tests/test_action_readiness_gate.py:L1-L273`：覆盖真实结构化 gate 和 no-motion acquire/place Action 失败路径。
- [修改 / Modified] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/object_acquire.py:L51-L60,L410-L488`、`object_place.py:L56-L65,L487-L565`：增加可注入 readiness gate 与 no-motion world-change 拒绝。
- [修改 / Modified] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/fake_gateway.py:L261-L297,L375-L410`：将 gate 传入 Action endpoint，并显式投影 no-motion context。
- [修改 / Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L563-L583`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L579-L604`、`examples/forge-adapters/robotwin20/README.md:L320-L339`：记录执行顺序、五维复审、配置和限制。

### 关键 Diff / Key Diff

```text
Before: object.acquire/place validated reference syntax and called the provider directly.
After:  reviewed ReadinessEvidenceGate validates immutable evidence identity before provider
        admission; Action context exposes motion_authorized=false and rejects world-change
        claims while the no-motion gate is installed.
```

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-adapters/robotwin20/src:examples/forge-skills/pick-place-workflow/src:. python -m pytest -p pytest_asyncio.plugin -q examples/forge-adapters/robotwin20/tests/test_action_readiness_gate.py examples/forge-skills/pick-place-workflow/tests/test_object_acquire.py examples/forge-skills/pick-place-workflow/tests/test_object_place.py tests/test_gateway_dora_no_motion_conformance.py` → `52 passed`。
- `ruff check`、`git diff --check` → 通过；真实 Franka readiness manifest gate load → `50 candidates`。
- 未调用 RoboTwin `play_once`、Dora、Action stepping 或硬件。

### Git 提交 / Git Commit

- Commit: `83c74ff`
- Branch: `feature/long-horizon-workflow`

## v4.7.10 (2026-09-05 00:35) - codex

### 预期修改 / Planned Changes

- [完成] [chore] [docs] 回写 v4.7.9 Action readiness gate 实现提交哈希；不修改运行逻辑。
- [Completed] [Chore] [Docs] Record the v4.7.9 Action readiness-gate implementation commit hash without changing runtime logic.
- [完成] [chore] [docs] 在本次日志提交后回写该维护提交的 commit hash；不修改运行逻辑。
- [Completed] [Chore] [Docs] Record this maintenance commit hash after the log commit; runtime logic remains unchanged.
- [完成] [chore] [docs] 将维护提交 `5cc58a0` 回写至月度日志；不修改运行逻辑。
- [Completed] [Chore] [Docs] Record maintenance commit `5cc58a0` in the monthly log; runtime logic remains unchanged.
- [完成] [chore] [docs] 已回写第二次维护提交 `e6883f8` 的 commit hash。
- [Completed] [Chore] [Docs] Recorded the second maintenance commit hash `e6883f8`.
- [完成] [chore] [docs] 校正 CHANGELOG.md 中 v4.7.10 的入口提交哈希为 `e6883f8`。
- [Completed] [Chore] [Docs] Correct the v4.7.10 entry commit hash in CHANGELOG.md to `e6883f8`.

### 实际结果 / Actual Results

- [完成] [chore] [docs] v4.7.9 实现提交为 `83c74ff`，未纳入 `.codegraph/` 或 `.cursor/` 用户目录。
- [Completed] [Chore] [Docs] v4.7.9 implementation commit is `83c74ff`; user directories `.codegraph/` and `.cursor/` were not included.

## v4.7.14 (2026-09-05 00:50) - codex

### 预期修改 / Planned Changes

- [完成] [docs] 将仿真 motion executor 阶段的生命周期、授权、attached-object readiness 和语义验收阻断点写入架构诊断与实现审查文档，并修订执行顺序。
- [Completed] [Docs] Record lifecycle, authorization, attached-object readiness, and semantic-verification blockers for the simulation motion-executor stage, and revise the execution order.
- [完成] [eval] 按修订顺序复核现有 no-motion gate 与 Action 生命周期；安全前置条件缺失，保持 fail-closed，未启动仿真运动。
- [Completed] [Eval] Re-checked the existing no-motion gate and Action lifecycle under the revised order; safety prerequisites remain absent, so the system stayed fail-closed and simulation motion was not started.

### 实际结果 / Actual Results

- [完成] [docs] 已更新架构诊断与实现审查文档，记录 invocation-first 生命周期、独立 simulation authorization、attached-object readiness、执行后快照和语义 Verifier 的前置条件，并修订执行顺序。
- [Completed] [Docs] Updated the architecture diagnosis and implementation review with invocation-first lifecycle, isolated simulation authorization, attached-object readiness, after-snapshot, and semantic Verifier prerequisites, and revised the execution order.
- [完成] [eval] no-motion Action/Gateway 专项 `52 passed`；根仓库回归 `161 passed`；未启动 RoboTwin motion stepping。
- [Completed] [Eval] No-motion Action/Gateway focus `52 passed`; repository regression `161 passed`; RoboTwin motion stepping was not started.
- [完成] [chore] [docs] 已回写 v4.7.14 文档提交 `bac5ad4` 的 Git commit hash。
- [Completed] [Chore] [Docs] Recorded the v4.7.14 documentation commit hash `bac5ad4`.

## v4.8.0 (2026-09-05 01:10) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 将通用 CapabilityRuntime 与 pick-place Fake Gateway 的 Action 生命周期改为 invocation-first：先分配 invocation/attempt，再启动 provider，并保留失败、取消、超时和 unknown 语义。
- [Planned] [Policy] [Feat] Make the generic CapabilityRuntime and pick-place Fake Gateway Action lifecycle invocation-first: allocate invocation/attempt before starting providers while preserving failure, cancel, timeout, and unknown semantics.
- [计划] [eval] 增加 provider 启动身份、启动失败、取消前启动和 deferred Action conformance；完成后按架构集成、失败路径、权威边界、配置、可维护性五维审查。
- [Planned] [Eval] Add conformance for provider-start identity, start failure, pre-start cancellation, and deferred Actions; then perform the five-dimension review across architecture, failure paths, authority, configuration, and maintainability.

### 文件变更详情 / File Change Details

- [修改 / Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L585-L619`：记录仿真 motion executor 的授权、生命周期、attached-object readiness、完整路线和语义验收阻断点，并修订执行顺序。
- [修改 / Modified] `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L606-L628`：记录五维专项审查的 Blocker/Major 结论与 invocation-first 前置条件。
- [修改 / Modified] `changelog/2026-09_part2.md:L2474-L2498`：补全本轮诊断更新、回归验证和 fail-closed 结果。

### Git 提交 / Git Commit

- Commit: `0447dab`
- Branch: `feature/long-horizon-workflow`

### 实际结果 / Actual Results

- [完成] [policy] [feat] `PhyAgentOS/forge/capability_runtime/ports.py:L17-L23`、`runtime.py:L204-L270` 增加 invocation-first `ActionAdmission.start` callback；invocation/attempt 先写入后才启动 deferred provider，启动失败和非法返回均形成可查询终态失败。
- [Completed] [Policy] [Feat] `PhyAgentOS/forge/capability_runtime/ports.py:L17-L23` and `runtime.py:L204-L270` add an invocation-first `ActionAdmission.start` callback; invocation/attempt identities are persisted before deferred providers start, and start failures or invalid returns become queryable terminal failures.
- [完成] [comm] [feat] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/object_acquire.py:L51-L60,L410-L488`、`object_place.py:L56-L65,L487-L565` 拆分 validate/execute/admit；`fake_gateway.py:L272-L307,L494-L795` 增加显式 deferred 生命周期、cancel/stop-before-start 和启动失败投影。
- [Completed] [Comm] [Feat] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/object_acquire.py:L51-L60,L410-L488`, `object_place.py:L56-L65,L487-L565`, and `fake_gateway.py:L272-L307,L494-L795` split validate/execute/admit and add explicit deferred lifecycle, cancel/stop-before-start, and start-failure projection.
- [完成] [eval] [test] `tests/test_gateway_dora_no_motion_conformance.py:L1-L194`、`examples/forge-adapters/robotwin20/tests/test_action_readiness_gate.py:L1-L339` 覆盖 provider identity/start failure/deferred cancel-stop/非法 deferred；专项 `58 passed`，根仓库 `164 passed`，pick-place `256 passed`，ruff、compileall、diff check 通过。
- [Completed] [Eval] [Test] `tests/test_gateway_dora_no_motion_conformance.py:L1-L194` and `examples/forge-adapters/robotwin20/tests/test_action_readiness_gate.py:L1-L339` cover provider identity, start failure, deferred cancel-stop, and invalid deferred admissions; focused tests pass `58`, the repository suite `164`, the pick-place suite `256`, and ruff/compileall/diff checks pass.
- [完成] [docs] [review] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L621-L630`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L630-L650` 完成五维验收；无 Blocker/Major，但明确仍不授权 simulation motion。
- [Completed] [Docs] [Review] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L621-L630` and `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L630-L650` complete the five-dimension review; no Blocker/Major was found, while simulation motion remains unauthorized.

### 文件变更详情 / File Change Details

- [修改 / Modified] `PhyAgentOS/forge/capability_runtime/ports.py:L17-L23`、`runtime.py:L204-L270`：增加 invocation-first deferred Action admission。
- [修改 / Modified] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/fake_gateway.py:L272-L307,L494-L795`：增加 deferred provider 生命周期和 cancel/stop/失败语义。
- [修改 / Modified] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/object_acquire.py:L51-L60,L410-L488`、`object_place.py:L56-L65,L487-L565`：分离输入校验和 provider execute。

## v4.9.0 (2026-09-05 02:00) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 新增与 no-motion readiness/action gate 隔离的 simulation-motion authorization profile/schema；仅描述仿真动作授权申请所需的证据、人工审批、停止策略和 before/after snapshot 要求，不启动 RoboTwin、Dora 或硬件动作。
- [Planned] [Policy] [Feat] Add a simulation-motion authorization profile/schema isolated from the no-motion readiness/action gate; describe only the evidence, human approval, stop policy, and before/after snapshot requirements for a simulation-motion authorization request, without starting RoboTwin, Dora, or hardware motion.
- [计划] [eval] [test] 为 profile 严格字段、路径/摘要、授权状态、证据覆盖、停止与快照策略增加 fail-closed conformance，并按架构集成、失败路径、权威边界、配置、可维护性五维审查。
- [Planned] [Eval] [Test] Add fail-closed conformance for strict fields, paths/digests, authorization state, evidence coverage, stop and snapshot policies, followed by a five-dimension review across architecture, failure paths, authority, configuration, and maintainability.

### 预期影响文件 / Expected Files

- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/simulation_authorization.py`
- `examples/forge-adapters/robotwin20/profiles/robotwin20/simulation-motion.yaml`
- `examples/forge-adapters/robotwin20/tests/test_simulation_authorization.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`
- `examples/forge-adapters/robotwin20/README.md`
- `CHANGELOG.md`
- `changelog/2026-09_part2.md`

### 实际结果 / Actual Results

- [完成] [policy] [feat] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/simulation_authorization.py:L1-L443` 新增严格的 simulation-motion profile/approval/evidence-manifest loader；profile 只声明授权申请条件，不启动 worker、RoboTwin、Dora、Gateway 或硬件。disabled 配置固定 `motion_authorized=false`，approved 配置必须绑定专用审批记录、profile identity、evidence manifest 摘要、完整四类证据 scope、停止策略和 before/after semantic snapshot 要求。
- [Completed] [Policy] [Feat] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/simulation_authorization.py:L1-L443` adds a strict simulation-motion profile/approval/evidence-manifest loader; the profile only declares authorization-request conditions and never starts a worker, RoboTwin, Dora, Gateway, or hardware. The disabled config fixes `motion_authorized=false`; an approved config must bind a dedicated approval record, profile identity, evidence-manifest digest, all four evidence scopes, stop policy, and before/after semantic snapshot requirements.
- [完成] [eval] [test] `examples/forge-adapters/robotwin20/tests/test_simulation_authorization.py:L1-L238` 覆盖 disabled/approved、环境变量、runtime/evidence digest、重复 YAML key、审批篡改、非法 scope、停止和快照约束；专项 `10 passed`，结合 readiness/action/Gateway 专项 `81 passed`，根仓库 `164 passed`。
- [Completed] [Eval] [Test] `examples/forge-adapters/robotwin20/tests/test_simulation_authorization.py:L1-L238` covers disabled/approved profiles, environment expansion, runtime/evidence digests, duplicate YAML keys, approval tampering, invalid scopes, stop and snapshot constraints; the focused suite passes `10`, readiness/action/Gateway combined passes `81`, and the repository suite passes `164`.
- [完成] [docs] [review] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L632-L652`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L652-L677`、`examples/forge-adapters/robotwin20/README.md:L341-L358` 记录实现边界和五维验收；结论无 Blocker/Major，下一步仍是外部 worker 的 attached-object/完整路线证据。
- [Completed] [Docs] [Review] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L632-L652`, `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L652-L677`, and `examples/forge-adapters/robotwin20/README.md:L341-L358` record the implementation boundary and five-dimension review; no Blocker/Major was found, and the next step remains external-worker attached-object/full-route evidence.

### 文件变更详情 / File Change Details

- [新增 / Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/simulation_authorization.py:L1-L443`：严格校验 profile、runtime/evidence/artifact 路径与 digest、四类 evidence scope、审批记录、停止策略和快照策略；拒绝重复 YAML key、符号链接和不安全审批文件。
- [新增 / Added] `examples/forge-adapters/robotwin20/profiles/robotwin20/simulation-motion.yaml:L1-L44`：新增 disabled 的 Franka `blocks_ranking_rgb` 仿真授权配置样例。
- [新增 / Added] `examples/forge-adapters/robotwin20/tests/test_simulation_authorization.py:L1-L238`：新增 10 个 profile/approval/evidence-manifest conformance tests。
- [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py:L55-L63,L143-L147`：导出 simulation authorization schema、异常、profile 和 loader。

### 关键 Diff / Key Diff

```text
Before: no-motion readiness/action profile was the only configuration boundary;
        simulation authorization conditions were undocumented and could be conflated.
After:  a separate strict profile declares approval/evidence/stop/snapshot requirements,
        defaults to disabled, and remains non-executing until a future Gateway/Runtime executor.
```

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-adapters/robotwin20/src:. python -m pytest -p pytest_asyncio.plugin -q examples/forge-adapters/robotwin20/tests/test_simulation_authorization.py` → `10 passed`。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-adapters/robotwin20/src:examples/forge-skills/pick-place-workflow/src:. python -m pytest -p pytest_asyncio.plugin -q ...`（readiness/action/Gateway 专项）→ `81 passed`。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -p pytest_asyncio.plugin -q` → `164 passed`。
- `ruff check`、`python -m compileall`、`git diff --check` → 通过。
- 未启动 RoboTwin `play_once`、Dora、Gateway motion executor 或硬件；profile loader 仅解析配置。

### Git 提交 / Git Commit

- Commit: `0447dab`
- Branch: `feature/long-horizon-workflow`
